---
title: "check-mise"
description: "The check-mise bed image — composes the mise-consumer layer (external_builder: mise + a mise: verb step) so the builder leg and the verb leg are proven end to end in one image."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Repo** | `box/github.com/opencharly/plugin-mise:v2026.242.1251` |
| **Base** | `quay.io/fedora/fedora:43` |

The check-mise bed image — composes the mise-consumer layer
(external_builder: mise + a mise: verb step) so the builder leg and the
verb leg are proven end to end in one image.

## Composition

This box composes:

- `mise-consumer`
