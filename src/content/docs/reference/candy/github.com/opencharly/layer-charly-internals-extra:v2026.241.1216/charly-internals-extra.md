---
title: "charly-internals-extra"
description: "Overflow skill entities for the charly-internals family (split from candy/charly-internals/charly.yml to stay under GitHub's 1 MiB contents-API cap)."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.218.1200` |
| **Repo** | `box/github.com/opencharly/layer-charly-internals-extra:v2026.241.1216` |

Overflow skill entities for the charly-internals family (split from
candy/charly-internals/charly.yml to stay under GitHub's 1 MiB
contents-API cap). The skills' owner field stays "charly-internals"
so the runtime association is unchanged.

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `run` | this documentation-only candy ships no install content |
| `check` | the overflow internals skill/agent entities are present and generated into the marketplace's internals/skills |
