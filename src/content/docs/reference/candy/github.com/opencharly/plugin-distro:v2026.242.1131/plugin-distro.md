---
title: "plugin-distro"
description: "The `distro` plugin KIND relocated into a candy: a KIND provider dispatching via the pb Invoke(OpLoad) envelope (decode the authored `distro:` entity into its core spec type, re-marshal as canonical JSON)."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.176.3203` |
| **Repo** | `box/github.com/opencharly/plugin-distro:v2026.242.1131` |
| **Plugin** | yes — see the [plugin reference](/reference/plugin/github.com/opencharly/plugin-distro:v2026.242.1131/plugin-distro/) |

The `distro` plugin KIND relocated into a candy: a KIND provider dispatching
via the pb Invoke(OpLoad) envelope (decode the authored `distro:` entity into
its core spec type, re-marshal as canonical JSON). Serves itself in BOTH
placements (compiled-in OR out-of-process) like the matching/exampleprobe
verbs + the init kind — no kit contract needed (kinds are pb-shape).

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `check` | the distro kind plugin ships a buildable Go module the host compiles in (default) or serves out-of-process; its entity decode is exercised by any box/deploy composing a `distro:` node |
