---
title: "plugin-service"
description: "The `service` TYPED-STEP state-provision verb relocated into a candy."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.242.2305` |
| **Repo** | `box/github.com/opencharly/plugin-service:v2026.242.2146` |
| **Plugin** | yes — see the [plugin reference](/reference/plugin/github.com/opencharly/plugin-service:v2026.242.2146/plugin-service/) |

The `service` TYPED-STEP state-provision verb relocated into a candy.
CHECK: probe running/enabled via supervisorctl/systemctl through the live
check engine. ACT (runtime): render the enable shell. ACT (build/deploy):
lower into a ServicePackagedStep (the host materializes the kit descriptor,
keeping the load-bearing Reverse() in package main). A HOST-COUPLED verb on
the sdk/kit contract (CheckVerbProvider + ProvisionActor +
StepProvider), so it is COMPILED-IN-ONLY.

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `check` | the service verb dispatches through the provider registry and probes that a service is running |
