---
title: "plugin-dns"
description: "The `dns` check verb relocated into a candy: a name-resolution probe (in-container `getent hosts` under charly check box, host-side `net.LookupIP` under charly check live, with optional required-address matching)."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.176.1900` |
| **Repo** | `box/github.com/opencharly/plugin-dns:v2026.242.0528` |
| **Plugin** | yes — see the [plugin reference](/reference/plugin/github.com/opencharly/plugin-dns:v2026.242.0528/plugin-dns/) |

The `dns` check verb relocated into a candy: a name-resolution probe
(in-container `getent hosts` under charly check box, host-side
`net.LookupIP` under charly check live, with optional required-address
matching). A HOST-COUPLED verb — its RunVerb runs against the live check
engine (sdk/kit.CheckContext), so it is COMPILED-IN-ONLY. A pure
check verb; a port/process-template relocation on the kit contract.

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `check` | the dns verb dispatches through the provider registry and probes name resolution on a live deployment |
