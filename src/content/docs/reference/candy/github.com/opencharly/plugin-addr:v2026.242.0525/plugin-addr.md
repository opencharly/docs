---
title: "plugin-addr"
description: "The `addr` check verb relocated into a candy: an outside-in TCP reachability probe of a host:port (in-container `nc` under charly check box, host-side dial under charly check live)."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.176.1800` |
| **Repo** | `box/github.com/opencharly/plugin-addr:v2026.242.0525` |
| **Plugin** | yes — see the [plugin reference](/reference/plugin/github.com/opencharly/plugin-addr:v2026.242.0525/plugin-addr/) |

The `addr` check verb relocated into a candy: an outside-in TCP
reachability probe of a host:port (in-container `nc` under charly check
box, host-side dial under charly check live). A HOST-COUPLED verb — its
RunVerb runs against the live check engine (sdk/kit.CheckContext),
so it is COMPILED-IN-ONLY. A pure check verb; a port/process-template
relocation on the kit contract.

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `check` | the addr verb dispatches through the provider registry and probes a host:port for reachability |
