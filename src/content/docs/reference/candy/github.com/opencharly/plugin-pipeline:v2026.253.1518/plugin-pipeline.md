---
title: "plugin-pipeline"
description: "The generic agent/workflow engine — domain-neutral: the bare agent runtime (plan agent stage + the standalone 'charly pipeline agent'), the plan executor ('charly pipeline run <entity>'), deterministic probe verbs, template/frontmatter rendering, redo + fail-hard, skills by @github candy refs."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

| | |
|---|---|
| **Version** | `2026.251.0000` |
| **Repo** | `box/github.com/opencharly/plugin-pipeline:v2026.253.1518` |
| **Plugin** | yes — see the [plugin reference](/reference/plugin/github.com/opencharly/plugin-pipeline:v2026.253.1518/plugin-pipeline/) |

The generic agent/workflow engine — domain-neutral: the bare agent
runtime (plan agent stage + the standalone 'charly pipeline agent'),
the plan executor ('charly pipeline run <entity>'), deterministic
probe verbs, template/frontmatter rendering, redo + fail-hard,
skills by @github candy refs. Every pipeline is a declared plan
(kind:pipeline entity, SDD CUE-first).

## Acceptance plan

This candy's `plan:` — the runnable spec `charly check` executes against a live deployment. `check:` steps are idempotent probes; `run:` steps change state.

| Intent | Step |
|---|---|
| `check` | the pipeline module ships (go.mod + the executor + generated params) |
