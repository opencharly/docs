---
title: "rpmfusion"
description: "RPM Fusion free and nonfree repository configuration for Fedora."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->

*Recipe card from the `charly-distros` plugin (Images — the deployable catalog).*

# rpmfusion -- RPM Fusion repository configuration

## Candy Properties

| Property | Value |
|----------|-------|
| Install files | `task:` |

## Usage

```yaml
# charly.yml
fedora-nonfree:
  base: fedora
  candy:
    - rpmfusion
```

## Used In Boxes

- [`/charly-distros:fedora-nonfree`](/recipes/distros/fedora-nonfree/) (base for immich and other boxes needing nonfree packages)
- [`/charly-distros:fedora-builder`](/recipes/distros/fedora-builder/) — applied **before** `build-toolchain` so cargo crates that
  need RPM Fusion free system libs (`libva-devel`, `x264-devel`, `ffmpeg-devel`) can install
  via dnf in the builder stage. Required for building pixelflux from source — see
  [`/charly-selkies:selkies`](/recipes/selkies/selkies/) (Patched pixelflux build pipeline).

## Related Candies
- [`/charly-selkies:ffmpeg`](/recipes/selkies/ffmpeg/) -- nonfree multimedia codecs installed from RPM Fusion
- [`/charly-immich:immich`](/recipes/immich/immich/) -- consumes nonfree codecs via fedora-nonfree

## Related Commands
- [`/charly-build:build`](/recipes/build/build/) -- build the fedora-nonfree base image
- [`/charly-image:image`](/recipes/image/image/) -- inspect base/box inheritance

## When to Use This Skill

Use when the user asks about:

- RPM Fusion repository setup
- Nonfree or multimedia packages on Fedora
- The `fedora-nonfree` base image
- Enabling additional Fedora repositories

## Related

- [`/charly-image:layer`](/recipes/image/layer/) — candy authoring reference (`charly.yml` schema, task verbs, service declarations)
- [`/charly-check:check`](/recipes/check/check/) — declarative testing (`check:` block, `charly check box`, `charly check live`)
