---
title: "omarchy-eval-work-lane"
description: "The work-lane fields — the per-PR identity carried through the lane and rendered into the report frontmatter."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

*Recipe card from the `charly-distros` plugin (Images — the deployable catalog).*

# omarchy-eval-work-lane

The work-lane fields: the per-PR identity (pr, head sha, channel, class,
tier) carried through the lane and rendered into the report frontmatter. The
frontmatter schema validates the report's identity fields.
