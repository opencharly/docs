---
title: "comfyui-layer"
description: "ComfyUI image generation service on port 8188 with CUDA GPU support."
---

<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->

*Recipe card from the `charly-comfyui` plugin (Images — the deployable catalog).*

# comfyui -- GPU-accelerated image generation service

## Candy Properties

| Property | Value |
|----------|-------|
| Dependencies | `cuda`, `supervisord` |
| Ports | 8188 |
| Volumes | `comfyui` -> `~/ComfyUI` |
| Service | `comfyui` (supervisord) |
| Install files | `plan:`, `pixi.toml` |

## Packages

The candy is multi-distro:

- `aria2` (RPM / PAC) -- download manager for model files
- `git-lfs` (RPM / PAC) -- large file support for model repos

On Arch/CachyOS the `distro.arch` section installs `aria2` and `git-lfs` from the
Arch repos (same package names).

## Environment Variables

| Variable | Value |
|----------|-------|
| (inherited from cuda) | CUDA toolkit paths |

## Usage

```yaml
# charly.yml — the comfyui box composes the comfyui candy via an inline candy: list
comfyui:
  candy:
    base: nvidia
    candy:
      - comfyui
```

## Used In Boxes

- [`/charly-comfyui:comfyui`](/recipes/comfyui/comfyui/)

## Related Candies

- [`/charly-distros:cuda`](/recipes/distros/cuda/) -- CUDA toolkit dependency
- [`/charly-infrastructure:supervisord`](/recipes/infrastructure/supervisord/) -- process manager dependency

## When to Use This Skill

Use when the user asks about:

- ComfyUI setup or configuration
- Image generation service
- Stable Diffusion workflows
- The comfyui volume or port 8188
- GPU-accelerated AI art

## Related

- [`/charly-image:layer`](/recipes/image/layer/) — candy authoring reference (`charly.yml` schema, plan steps, service declarations)
- [`/charly-check:check`](/recipes/check/check/) — declarative testing (`check:` block, `charly check box`, `charly check live`)
