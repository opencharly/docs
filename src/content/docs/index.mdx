---
title: OpenCharly
description: The fully stocked gourmet kitchen for you and your agents — a tiny core, an all-you-can-eat buffet.
# `doc` rather than `splash`, deliberately: Starlight's getToC hard-codes the table of contents
# to false for splash pages and ignores the `tableOfContents` frontmatter entirely, and
# `hasSidebar` is likewise `template !== 'splash'`. So a splash landing page cannot have either
# sidebar. The hero renders on whichever template carries a `hero:` block, so `doc` keeps the
# landing hero while gaining the nav sidebar and the on-page TOC.
template: doc
# Starlight appends the site title to every page title, so a home page called "OpenCharly" on a
# site called "OpenCharly" renders the tab as "OpenCharly | OpenCharly". Override the whole
# <title> for this page only; every other page still gets the useful "Page | OpenCharly" form.
head:
  - tag: title
    content: OpenCharly — the fully stocked gourmet kitchen for you and your agents
hero:
  tagline: |
    Every other tool hands an agent a sandbox and then spends its effort taking things away.
    OpenCharly does the opposite: it walls the room, then stocks it with everything.
  actions:
    - text: Get started
      link: /start/install/
      icon: right-arrow
    - text: Read the vision
      link: /vision/
      variant: minimal
    - text: View on GitHub
      link: https://github.com/opencharly/charly
      icon: external
      variant: minimal
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

## What it is

`charly` builds container images from a composable list of **candies**. That part will feel
familiar. The part that usually does not: *the same list* also installs onto a VM guest, a
Kubernetes cluster, your own workstation, or an Android device — and every piece of it carries a
runnable acceptance test, so you can prove a deployment works instead of assuming it.

## If you already run containers and VMs

| You already use | For | charly's equivalent |
|---|---|---|
| a Containerfile | building an image | a `candy:` list on a `base:` |
| docker-compose | several containers together | `kind: pod` — deployed as systemd quadlets |
| Packer / cloud-init | building a VM guest | `kind: vm` — *the same candy list* |
| Ansible / a setup script | configuring a host | `kind: local` — *the same candy list* |
| — nothing you have — | proving the deploy actually works | a disposable **check bed** |

That last row is the point. It is the line most toolchains leave empty.

## One candy, three roles

There is one `candy:` keyword, and what you put inside it decides what it is:

| What it declares | What it is |
|---|---|
| `package:` / `plan:` / `service:` | a **layer** — one concern, composed into any box |
| …plus `base:` or `from:` | a **box** — a buildable container image |
| …plus a `plugin:` block | a **plugin** — a new verb, kind or command for `charly` itself |

One recipe-card format describes an ingredient, a finished dish, and a new piece of kitchen
equipment. That is why the core stays tiny while the catalog grows — and why there is no second
vocabulary to learn. [The words →](/concepts/00-vocabulary/)

## Sixty seconds

The box below is real, shipped, and re-proven on every acceptance run — you can read it at
[`tutorial-shell`](/reference/box/fedora/tutorial-shell/):

```bash
charly -C box/fedora box validate                    # the schema gate — nothing runs until it passes
charly -C box/fedora box build tutorial-shell        # → multi-stage Containerfile → image
charly -C box/fedora shell tutorial-shell            # → you are inside the candybox
charly -C box/fedora check run check-tutorial-shell    # → build, deploy, probe, fresh rebuild, tear down
```

Then change the mold and keep the recipe — `charly bundle add host ripgrep` applies the same candy
to your workstation, with no container involved, and `charly bundle del host` reverses exactly what
it did.

<Aside type="tip" title="Yes, it comes with the kitchen sink">
The three-candy box above is the small end. At the other sit the **kitchen-sink dev boxes** —
[`fedora-coder`](/recipes/coder/fedora-coder/) and its [`arch`](/recipes/coder/arch-coder/),
[`debian`](/recipes/coder/debian-coder/) and [`ubuntu`](/recipes/coder/ubuntu-coder/) siblings —
around thirty candies each: five AI coding CLIs, every language runtime, DevOps tooling, nested
rootless containers, rootless VMs. Same recipe format, same four commands. A fully stocked kitchen
really does ship with the sink.
</Aside>

## The bet

<CardGrid>
  <Card title="Secure the room, not the candy" icon="approve-check">
    Safety lives at the walls of the running **candybox** — never in a shrunken toolset. Inside
    it, you and your agents get everything.
  </Card>
  <Card title="One recipe, many molds" icon="seti:config">
    The same candy list pours into a shell, a managed pod, a cluster, a bootable VM, a phone, or
    your own machine.
  </Card>
  <Card title="Built for two tasters" icon="star">
    The same surface serves you at the keyboard and your agents over RPC — the binary is also an
    MCP server, so nothing is automation-only.
  </Card>
  <Card title="The spec is the test" icon="puzzle">
    Every candy ships a runnable acceptance plan, baked into the image. What "working" means is
    executed, not asserted.
  </Card>
</CardGrid>

## Where to go next

- **[Install](/start/install/)** — get `charly` onto your machine.
- **[Quickstart](/start/quickstart/)** — read a real box, build it, prove it.
- **[The words](/concepts/00-vocabulary/)** — candy, box, candybox: five minutes well spent.
- **[The concepts tour](/concepts/01-the-box-is-the-boundary/)** — twelve short pages, theory and a
  runnable example each, in the order the project's own [vision](/vision/) states them.
- **[Recipe cards](/recipes/)** — every candy, box and verb has a dedicated page.
- **[Provider index](/reference/providers/)** — what implements `cdp:`? Look it up.

:::note[Most of this site writes itself]
The reference and recipe-card sections are generated from the source repository by
`charly docs generate` — from each candy's own `charly.yml`, each plugin's CUE schema, and the
skill corpus. Nothing here is a hand-maintained copy of something that lives in the code, because
a copy drifts and a projection cannot.
:::
