# 2026.236.1541 — docs: complete the candy/ripgrep claim sweep (#98 premature-merge remediation)

## Summary

Remediation for the #98 premature-merge incident (validator round-2 verdict:
https://github.com/opencharly/docs/pull/98#issuecomment-5397393934). #98 was auto-merged
by the Actions reviewer's erroneous PASS at its round-1-FAILED head, twelve minutes
before the independent FAIL status posted, so main and the published site carry the two
surviving stale `candy/ripgrep/charly.yml` path attributions and the landed
`CHANGELOG/2026.236.1500.md` bakes in the false "two" count.

- **Cherry-pick of the already-independently-validated fix** (`3b7b10e`, verified clean
  by validator round 2 on #98): all four `candy/ripgrep/charly.yml` path attributions —
  `concepts/00-vocabulary` (the in-quote YAML comment), `concepts/02-one-recipe-many-molds`,
  `concepts/06-the-spec-is-the-test`, `guides/authoring-a-candy` — now name the candy's
  live home, [opencharly/layer-ripgrep](https://github.com/opencharly/layer-ripgrep)
  (its root `charly.yml` holds the candy; version matches the quoted excerpts).
- **Errata in `CHANGELOG/2026.236.1500.md`**: the entry now records the accurate
  four-count and states explicitly, as a correction, that the #98 landing fixed two of
  the four and that the rest landed here. (Flagged consciously against the
  merged-changelog immutability rule: this is the round-2 validator's prescribed
  remediation for a factually false claim on main — an explicit errata, not a re-dating.)

**Breaking change:** none. **Rollback:** revert the merge commit.

## How tested

Claim-class sweep at this head (path-attribution form, distinguished from live
versioned `@github…/candy/ripgrep:vNNNN` pins, which resolve at their tags):

```
$ grep -rn "candy/ripgrep/charly.yml" src/content/docs README.md astro.config.mjs
(no hits)
```

The cherry-picked content is byte-identical to `3b7b10e`, which validator round 2 on
#98 independently re-validated clean (claim-class grep, layer-ripgrep content match,
trailer parse). The hand-authored delta does not affect generated pages; the drift gate
and astro build run on this head's CI.

## Rulebook compliance

R1: the incident is RCA'd in the round-2 verdict (root cause: the Actions reviewer's
link-form-only sweep + advisory-only independent validation); this PR is the content
remediation. The process half (F-D) is routed to opencharly/.github and needs operator
action — not addressed here. R5: sweep pasted above, claim-class-keyed. R6: new branch
off main, cherry-pick, no force-push.

## Change classification

Documentation-only: hand-authored prose (4 lines) + a changelog errata. Gate: the
claim-class sweep above plus this head's CI (drift gate + astro build).

*Assisted-by: Claude Code Anthropic Claude Fable 5 (documentation reviewed)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn