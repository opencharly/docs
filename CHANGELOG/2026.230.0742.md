# 2026.230.0742 — the two guards in `evidence-and-freshness.md` become able to fail

Projection of `opencharly/plugins#203` (merged `01edd45e`, tagged `v2026.230.0738`), which is
itself the projection of `opencharly/charly#289`. One page —
`recipes/internals/git-workflow/evidence-and-freshness.md` — **`+46/-7`** against `docs/main`.

The section titled *"A guard that cannot fail is worse than no guard"* published two guards that
could not run. **Six ways they were unable to fail**, each found by executing the page rather than
reading it:

| # | the guard could not fail because | found by |
|---|---|---|
| 1 | `<entry>` is a shell **redirection**, so the loop consumed empty stdout and read as clean | executing it |
| 2 | `.gitmodules` was read from the **cwd**, so from a submodule the list collapsed with empty stdout *and* empty stderr | executing it from a submodule |
| 3 | `\|\| continue` was the only miss path, so an unresolvable SHA **vanished** and the run exited 0 | a deliberately fake SHA |
| 4 | an entry citing **zero SHAs** ran the loop zero times and exited 0 | pointing it at a file with none |
| 5 | guard 1's subshell made its `entry=` unreachable, so **guard 2 could not run at all** | tracing the variable |
| 6 | `merge-base --is-ancestor` returns **128**, not 1, when `origin/main` is unresolvable — and `&&`/`\|\|` cannot separate them, so *"could not check"* printed as `UNMERGED` and exited 0 | a synthetic repo |

Both guards now resolve the root identically, refuse rather than narrow on every failure mode, and
say their empty cases. `UNMERGED` is information the reader judges; `UNRESOLVED` and `UNCHECKED`
set a nonzero exit.

*Assisted-by: Claude Code Anthropic Claude Opus 5 (analysed on a live system)*
