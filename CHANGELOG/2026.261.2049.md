# 2026.261.2049 — docs: bump the pinned charly + marketplace + plugin-docs and regenerate the site

## Summary of changes

Bumps the docs site's three input pins and regenerates the generated half of the content tree, catching the published site up with the merged charly / marketplace / plugin-docs work.

**171 files changed = 3 inputs + 168 generated pages.**

Inputs (exact values, as recorded in this tree):
- **`.github/workflows/deploy.yml`**
  - pinned charly CI-time commit `213e0945d19f3364d97ba22fbdf08b686bd92ab8` (2026-09-13) → **`d45132f338677a2c86a5dbed53eef53b8112a1e2`** (current `main`);
  - **plugin-docs** generator pin `v2026.256.1653` → **`v2026.261.1812`** (carries `opencharly/plugin-docs#9` — the skill dedupe that makes this regeneration duplicate-free);
  - the surrounding box-pin comment is made pin-agnostic (it hard-coded the old charly commit).
- **`marketplace`** submodule gitlink `7b196afb70ade6d0afb0918735540122fb8e021e` → **`54f77839528bfc63d3e4d5e8546af295e8101cce`** (current `main`, carrying `marketplace#316` + `#317`).
- **`.github/docs-corpus/pins.tsv`** — the four pins that are *not* overridden by charly's `go.mod` (the non-compiled repos) advance to their newest CalVer: `plugin-bpf` → `v2026.260.1008`, `plugin-cardwire` → `v2026.260.0911`, `plugin-herdr` → `v2026.251.0850`, `pod-herdr` → `v2026.251.0850`. (The other 61 entries resolve through charly's `go.mod` requires and are left untouched — changing them would fight the resolver.)

Generated (`src/content/docs/`, 168 files): the new `recipes/check/jetkvm.md` (JetKVM verb + on-device `charly-jetkvm` flow), the `recipes/{agent,authoring,bpf,cache,candy-cli,cardwire,feature,pipeline,pod-verbs,review}/` families, the version-path moves for `plugin-{build,docs,pipeline,review,vm,bpf,cardwire,herdr}` + `pod-herdr`, and the `reference/providers.md` / `reference/cli/*` updates. Seven stale `recipes/distros/omarchy-eval-*` pages are pruned.

Two projection defects the earlier revisions of this PR shipped are fixed at source and consumed here:
- `recipes/build/docs.md` no longer asserts the stale pins `v2026.254.1214` / `d507ca5a` (fixed in `opencharly/layer-charly-build#12`, projected via `opencharly/marketplace#316`) — the page now names the mechanism ("the `checkout <sha>` in its charly-clone step").
- `recipes/build/docs.md` no longer claims the corpus is "not internally consistent … the marketplace regeneration has not landed the card" (fixed in `opencharly/layer-charly-build#13`, projected via `opencharly/marketplace#317`).

**Closes / relates to:** the docs-side projection of the JetKVM chain (`plugin-jetkvm#5`, `layer-charly-check#15/#16`, `marketplace#312/#314/#315`), `opencharly/plugin-docs#9`, `opencharly/layer-charly-build#12`+`#13`, `opencharly/marketplace#316`+`#317`.

**Breaking change + rollback:** `none` — pins plus regenerated pages. Revert the merge to roll back.

## Change class

`docs` — the site's generation pins (workflow + pins file + submodule) plus the regenerated projection tree.

## How tested

The docs repo's own gate is `deploy.yml`'s drift gate, and its shipped recipe (step 2 of the drift-proof recipe in `recipes/build/docs.md`) is explicit that the regeneration must run against the docs repo's **recorded** marketplace gitlink, not a moving tip. I reproduced the exact CI pipeline against that recorded commit, then snapped the tree and regenerated again to prove the no-op:

```console
$ git -C marketplace rev-parse HEAD
54f77839528bfc63d3e4d5e8546af295e8101cce      # == the recorded gitlink (git ls-files -s marketplace)
$ cd /tmp/charly && /tmp/plugin-docs-bin generate \
    --root /tmp/charly --plugins <docs>/marketplace --out <docs>/src/content/docs
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 362 recipe pages, 176 plugin pages (164 provider words), 59 cli pages, 671 candy pages, 127 box pages
$ # snapshot the generated tree, regenerate, compare:
$ diff -rq <snap3> <docs>/src/content/docs | wc -l
0
```

**Regeneration at the recorded gitlink is a byte no-op** — the drift-gate contract.

**The defects of the earlier revisions are gone, and the new content is present:**

```console
$ grep -oE '\(/recipes/[^)]+\)' src/content/docs/recipes/index.md | sort | uniq -d | wc -l
0                                                               # duplicate-free index
$ grep -c 'v2026.254.1214\|d507ca5a' src/content/docs/recipes/build/docs.md
0                                                               # no stale pin literals
$ grep -c 'has not landed the card\|not internally consistent' src/content/docs/recipes/build/docs.md
0                                                               # no stale corpus-broken claim
$ test -f src/content/docs/recipes/check/jetkvm.md && echo present
present
$ grep -c "JETKVM_HOST\|common USB HID\|charly-jetkvm env" src/content/docs/recipes/check/jetkvm.md
6
```

Before the bump the site had no `jetkvm` page at all; it now carries the verb reference plus the on-device `charly-jetkvm env` flow. Before `plugin-docs#9`, the same regeneration produced `934` bullets for `335` distinct cards (`319` duplicated targets); it is now one bullet per card.

## Change classification

- **Change class:** `docs` (pins + generated projection).
- **Verification gate:** the deploy drift gate reproduced locally against the recorded gitlink (regenerate-then-compare is a no-op), plus the duplicate-free / no-stale-claims / new-page checks above.
- **Attribution tier:** `fully tested and validated` — the changed generation ran to completion with the exact CI flags against the recorded marketplace commit, and the committed projection reproduces byte-identically.

## Harness rulebook compliance

- **R0 skills:** `/charly-build:docs`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the lag was root-caused to the corpus advancing without the consumer regenerating; the duplicate-bullet and false-pin/stale-claim defects were root-caused to generator precedence and skill prose respectively, and fixed at source (`plugin-docs#9`, `layer-charly-build#12`/`#13`) so this PR consumes the fixes rather than shipping the defects.
- **R2 no out-of-scope:** the three pins and their projection are the whole change; every surfaced defect is fixed, none parked.
- **R3 no duplication:** pages are projected, never hand-edited; `pins.tsv` is the single corpus wiring input; the index is one bullet per card.
- **R4 no workaround:** the drift gate is satisfied by real regeneration at the recorded pins.
- **R5 hard cutover + grep:** the pruned `omarchy-eval-*` pages, the pin-agnostic workflow comment, and the corrected skill prose are all swept in the same cutover; the greps above prove it.
- **R6 git safety:** fresh branch off current `origin/main`; no force push, no hooks bypass, no direct push to `main`.
- **R7 runtime gate:** the generator executed the full pipeline and the committed tree regenerates as a no-op.
- **R8 artifact invariants:** every emitted page carries its `DO-NOT-EDIT` header; the projection is reproducible.
- **R9 binary == source + deps:** generation used the `plugin-docs` binary built at the newly-pinned tag (`v2026.261.1812`) against the pinned charly checkout; the marketplace submodule sits at the recorded gitlink (`54f778395…`), verified equal to `git ls-files -s marketplace`.
- **R10 disposable + coverage:** `N/A — a generated-corpus repo`; the deploy drift gate is the shipped coverage.
- **RDD / ADE / SDD:** `N/A — projection only`.
- **Provenance:** the four advanced `pins.tsv` entries are the only ones not resolved through charly's `go.mod`; the other 61 are deliberately left.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*