# 2026.218.0000 — sync the generated catalog to the liberation + policy + harness-surface changes

Regenerates the generated half after a coordinated multi-repo window:

- `recipes/build/docs.md` — the generator now counts six top-level entries, `liberation.md`
  included (the liberation producer + skill-count update landed).
- `recipes/internals/git-workflow.md` — force-push forbidden on any branch; amending a `feat/`
  branch is a normal authoring action, legal until the first push (policy update).
- `reference/candy/docs-site.md` — the docs-site candy re-pinned `DOCS_REF` and gained the
  `docs-site-liberation-page` check.
- `reference/providers.md` + `reference/candy/plugin-harness-kind.md` +
  `reference/plugin/plugin-harness-kind.md` — the in-tree `plugin-harness-kind` candy's provider
  words and pages.

CalVer finalized at merge by the fresh pr-validator.
