# 2026.218.0000 — the canonical glossary (bundle→fleet) mirrored + regenerated

The vocabulary page now carries the full glossary (candy, layer, base, from,
box, image, container image, OCI image, candybox, container, substrate, pod,
vm, vm image, k8s, local, android, host, deploy, fleet, plugin, provider,
kind, verb, command, step, builder, plan, install plan, IR, check bed)
mirroring the superproject README, with the fleet line in the concretely
block. Hand-authored concepts/guides rename charly bundle→fleet; the
generated half is regenerated from the renamed sources (reference/cli/fleet.md
replaces reference/cli/bundle.md). Placeholder CalVer — the pr-validator
finalizes it at merge.
