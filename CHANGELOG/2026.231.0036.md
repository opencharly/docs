# 2026.231.0036 — project the load page's composing section

`plugins#208` landed the `/charly-build:load` cold-reader cures — the default socket path
stated rather than referenced, a composable YAML snippet, the bed located as a
`disposable: true` deploy, and the real output that distinguishes a transfer from an
idempotency skip. This projects them onto the site.

It is deliberately a separate leg, and the reason is worth recording because the
alternative was to make it disappear. `docs#80` fixed one escape character in
`recipes/vm/vm.md`. The matching `load.md` update was **39 lines** at that moment, because
its snippet was projected from `plugins#208`, which had not landed — so including it would
have pushed unlanded content through a PR that needed one character. `#208` has since
merged as `7c48f573` (tag `v2026.231.0008`), and the content now reproduces from landed
state rather than from a pushed branch.

Verified rather than asserted: the section here is byte-identical to the body of
`build/skills/load/SKILL.md` at `plugins@7c48f573`, which is the tree `charly#315` pins.

The snippet carries the entity-kind cure that took three rounds to get right. It reads
`base:` marks it a BOX — the thing you build — and names the argument slots rather than
the noun: `<target>` is the running deploy, never the box, and `<image>` is the artifact
delivered into it. An earlier draft said "addresses the running deploy, never the image",
which collided with the page's own H1 ("deliver an image into a pod's NESTED store") and
its `<image>` positional; a cold reader parsed it as "this command has nothing to do with
images", the exact inverse.