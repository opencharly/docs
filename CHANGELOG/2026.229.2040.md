# 2026.229.2040 — the site catches up with the corrected skill corpus

**This is a REGENERATION, not an authored edit.** Every page carries
`DO-NOT-EDIT — generated by `charly docs generate``; the source is the skill corpus at
`opencharly/plugins@67219d44` (`v2026.229.2031`), itself projected from candy entities in
`opencharly/charly#289`.

## What arrives

Eight recipe pages, all two-hop projections (`candy → plugins SKILL.md → docs recipe`):

| page | what changed |
|---|---|
| `recipes/internals/git-workflow.md` + its four `references/` pages | the `pkg/*` submodules dropped from the tag-every-repo rule — removed from `charly/main` by `ca76c354` |
| `recipes/internals/cutover-policy.md`, `recipes/core/clean.md` | re-projected from the merged source |
| `recipes/ollama/ollama-layer.md` | loses a false `cuda` dependency claim, a `${CONTAINER_IP}` construction this corpus's own gotchas page rules wrong, and the sections documenting `OLLAMA_VERSION` — a variable no candy declares |

## What the site stops publishing

`plugins#197` pruned `ollama/skills/ollama-cli/SKILL.md` and removed two sections whose producing
source no longer exists — `plugins#191` landed them from `charly@04b89ced` on branch
`feat/ollama-plugin`, since deleted. Measured against `charly/main`: `OLLAMA_VERSION` occurs in **0**
`charly.yml` files (control: `HOST_PORT`, a variable that does exist, occurs in 60), `plugin-ollama`
has **0** references in `plugins_generated.go` and **0** `go.work` directives, and `charly ollama`
exits **80**.

**So the site was publishing instructions to set a build variable no candy declares and to run a CLI
verb no plugin provides.** R4a governs that. The removed text is quoted verbatim in
`plugins/CHANGELOG/2026.229.2031.md`, with `charly#291` as the tracked exit; this entry does not
repeat it, because the record belongs in one place and that repo is where the artifact lived.

`recipes/ollama/ollama-cli.md` was never published, so nothing is removed from the site for it.

## Scope

Five further pages drift at this base — `recipes/internals/{skills,vm-spec}.md` and
`recipes/vm/{arch-cloud-vm,vm,vms-catalog}.md`. They are **not** projections of anything
`plugins#197` changed; the site is behind `plugins/main` for them from an earlier leg, and they
belong to whichever cutover owns that content. They are left alone rather than absorbed under this
CalVer.

**The revert set was re-derived at this tree**, not carried forward, and it differs from every prior
regeneration in this cutover — as it has every time.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
