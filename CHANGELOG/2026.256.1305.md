# 2026.256.1305 — docs: retire the dead group kind from the corpus and concept pages

## Summary

Retires the **dead `group` entity kind** from the documentation corpus and concept pages.

The kernel's member-tree cutover already removed the `group` kind: the pinned `charly` rejects an authored `group:` node, and `plugin-group` is residual (not in `compiled_plugins:`, not in charly's `charly.yml`, not in the marketplace refs). Its **docs-corpus pin was the only thing keeping the dead word in the generated provider census**.

- `.github/docs-corpus/pins.tsv` — remove `plugin-group` (66 → 65 corpus refs).
- `concepts/00-vocabulary.md` — remove `group` from the `kind` term row and the vocabulary table.
- Regenerated projection: `group` is gone from `reference/providers.md`; the two `plugin-group` reference pages are deleted.
- Regeneration also advanced `plugin-pipeline` (a `release_repos` entry whose tag moved): beyond the version bump, its corpus comment now records the **ref-resolved** schema semantics. This is generator output, not a hand edit.

## How tested (real transcript)

```
$ cd /tmp/gtest && charly box validate      # fixture with an authored `group:` node
charly: error: command "validate": validation error: /tmp/gtest/charly.yml:doc0:
node "gtest": no kind discriminator — collections and plan steps live INLINE in
the kind value (the named child-node shape was removed); run: charly migrate

$ python3 build_wiring.py .github/docs-corpus/pins.tsv /tmp/charly/candy/docs-corpus/charly.yml
corpus wiring refs: 65                       # was 66
$ /tmp/plugin-docs-bin generate --root /tmp/charly --plugins <docs>/marketplace --out <docs>/src/content/docs
charly docs: 968 recipe pages, 174 plugin pages (165 provider words), 60 cli pages,
             668 candy pages, 127 box pages        # was 175 / 166 / 669
$ grep -n '^| `group`' src/content/docs/reference/providers.md
(no output)                                  # group GONE
$ git status --short | grep plugin-group
 D reference/candy/…plugin-group-v2026-242-0529/plugin-group.md
 D reference/plugin/…plugin-group-v2026-242-0529/plugin-group.md
```

## Rulebook compliance

- **R1** — the dead kind was proven with a live `box validate` (above); the fix removes it from every docs surface.
- **R2** — the retirement is split by owning repo; the siblings are **opencharly/charly#608** (registry_bootstrap comment) and **opencharly/opencharly#203** (submodule mount) — all open and named.
- **R3** — no duplication introduced.
- **R4a** — the `box validate` proof works from the binary alone.
- **R5** — claim-keyed sweep: `group` removed from the hand page and the regenerated census.
- **R6** — `feat/` branch + PR.
- **R7/R8/R9/R10** — N/A — no runtime path; the docs generator is the only "execution", and it ran live (output above).

## Change classification

- Class: docs + docs-corpus config (`.github/docs-corpus/pins.tsv` + `*.md` + regenerated projection).
- Gate: the docs generator ran live with the exact deploy recipe (pasted above); the CI `deploy` regeneration gate is the full gate.
- Confidence: **analysed on a live system** — the generator ran on this tree with retained output; the CI deploy gate itself was not run locally.

*Assisted-by: opencode deepseek-v4.1-flash (analysed on a live system)*