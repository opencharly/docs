# 2026.235.0856 — feat(docs)!: standalone docs repo — own charly submodule pin + the deploy workflow owns generation, build and publish

## Summary

The **docs de-submodule cutover's docs leg** (the charly leg landed as `charly#381`): this repo is now **standalone** — it pins `opencharly/charly` in `.gitmodules` (at `92df8e63`, the merged cutover commit; the recursive checkout brings charly's plugins + box/* submodules — the generator's exact sources), and `deploy.yml` is the **sole owner** of generation, building and publishing:

1. checkout with `submodules: recursive`
2. build the charly binary from the pinned checkout (`cd charly/charly && go build …`)
3. regenerate: `cd charly && ../bin/charly docs generate --root . --out ../src/content/docs` (the runtime plugin-docs binary builds on demand; the generator's link/sidebar gates run here)
4. **drift gate**: `git diff --exit-code -- src/content/docs` — fails when the committed content does not match the pinned charly (regeneration on a clean tree is a no-op; a diff means a docs PR must bump the pin and carry the regenerated pages)
5. `npm ci` + `astro build` + deploy to Cloudflare Pages (existing deploy steps kept)

Content regenerated at the pin: **912 generated pages cleared** — the printed per-family sum is 907 (330 recipe + 93 plugin + 56 cli + 346 candy + 82 box), and the generator's own contract explains the difference: five further generated pages (grievances, landing, liberation, vision, providers) are not counted in that sum, so "cleared" exceeds it by exactly five. Every generated page's `DO-NOT-EDIT` header now names the docs repo's workflow; the docs/docs-site skill pages, the git-workflow B-references, the parallel-bed-testing recipe (sdk-debt liquidation), the docs-site candy page and the provider index re-projected from the merged charly. The hand-authored cookbook page's sync/drift passage rewritten for the workflow (matching the charly test-fixture edit). README + CLAUDE rewritten for the standalone flow.

### Diff manifest (918 files, PR-base vs head)

**Infra + prose (6 files — full diffs below):**
.github/workflows/deploy.yml
.gitmodules
CLAUDE.md
README.md
charly

**Content-bearing generated pages (7 — diffs below):**
src/content/docs/recipes/build/docs.md
src/content/docs/recipes/internals/agents/parallel-bed-testing.md
src/content/docs/recipes/internals/git-workflow/branch-and-pr-loop.md
src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md
src/content/docs/recipes/internals/skills.md
src/content/docs/recipes/tools/docs-site.md
src/content/docs/reference/candy/docs-site.md

**Header-only regenerated pages (905 — every generated page except the 7 content-bearing above):** every one of these carries exactly ONE changed line — the DO-NOT-EDIT header — from `run `task docs:sync` to regenerate.` to `the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout.` — the mechanical consequence of the generator's header change in the pinned charly. Representative sample:
src/content/docs/grievances.md
src/content/docs/index.md
src/content/docs/liberation.md
src/content/docs/recipes/agentteams/agentteams-cli.md
src/content/docs/recipes/agentteams/agentteams.md
...

**Manifest accounting (918 files):** 5 infra files + 1 hand-authored page (cookbook) + 7 content-bearing generated pages = 13 non-mechanical files (full diffs below); the remaining **905** generated pages change exactly one line each — the DO-NOT-EDIT header (from \`run \`task docs:sync\` to regenerate.\` to \`the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout.\`) — the mechanical consequence of the generator's header change in the pinned charly.


---
### Complete diffs (every non-header-only file)

```diff
diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index e888c45..8fc074d 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -1,5 +1,18 @@
 # Build and deploy the site to Cloudflare Pages.
 #
+# THIS WORKFLOW IS THE SOLE OWNER of generation, building and publishing for the
+# opencharly.ai site. The site is STANDALONE: it pins the charly repository as a
+# submodule (.gitmodules), and this workflow
+#
+#   1. checks out the pinned charly (with its plugins/box submodules),
+#   2. builds the charly binary from that checkout,
+#   3. regenerates the generated half of the content tree with `charly docs generate`
+#      (the runtime plugin-docs generator, built on demand from the checkout),
+#   4. FAILS if the committed content does not match what the pinned charly generates
+#      (the drift gate: regeneration on a clean tree is a no-op, so any diff means a
+#      docs PR must bump the charly pin and carry the regenerated pages),
+#   5. builds the Astro site and deploys to Cloudflare Pages.
+#
 # WHY A WORKFLOW RATHER THAN CLOUDFLARE'S GIT INTEGRATION: connecting a Cloudflare Pages project
 # to a GitHub org requires an OAuth handshake that Cloudflare must initiate — there is no API for
 # it (POST .../pages/connections is 405, GET-only), just as GitHub exposes no API to create an App
@@ -9,7 +22,7 @@
 #
 # The trade-off is explicit: the Cloudflare token lives in this repo's Actions secrets. It is
 # scoped to Cloudflare Pages edit plus DNS edit on the opencharly.ai zone, and nothing else.
-name: deploy
+name: docs
 
 on:
   push:
@@ -33,12 +46,46 @@ jobs:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v7
+        with:
+          # The pinned charly (with its plugins/ + box/* submodules) is the generator's
+          # source tree — this repo's committed content is a projection of it.
+          submodules: recursive
 
       - uses: actions/setup-node@v7
         with:
           node-version-file: .node-version
           cache: npm
 
+      - uses: actions/setup-go@v5
+        with:
+          go-version-file: charly/charly/go.mod
+          cache-dependency-path: charly/charly/go.sum
+
+      - name: Build the charly binary from the pinned checkout
+        run: |
+          cd charly/charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=docs-ci" -o ../../bin/charly .
+
+      - name: Regenerate the generated content from the pinned charly
+        # The project root must be the charly checkout (the runtime plugin-docs binary
+        # builds on demand from the project's candy source); --root points at it too,
+        # --out at this repo's content tree. The generator's link/sidebar gates run
+        # inside this step: a dead link fails the run.
+        run: |
+          cd charly && ../bin/charly docs generate --root . --out ../src/content/docs
+
+      - name: Drift gate — the committed content must match the pinned charly
+        # Regeneration on a clean tree is a no-op by contract. A diff here means a
+        # docs PR must bump the charly pin in .gitmodules and commit the regenerated
+        # pages in the same change.
+        run: |
+          if ! git diff --exit-code -- src/content/docs; then
+            echo "DRIFT: the committed site does not match the pinned charly checkout."
+            echo "Bump the charly submodule in .gitmodules and commit the regenerated"
+            echo "content in the same docs PR (this workflow is the gate)."
+            exit 1
+          fi
+          echo "content: committed tree matches the pinned charly (regeneration is a no-op)."
+
       # `npm ci` rather than `npm install`: it installs strictly from the committed lockfile and
       # FAILS if package.json and the lockfile disagree. That is deliberate — a lockfile missing
       # one of rolldown's optional platform bindings once shipped here and would have broken the
@@ -55,9 +102,9 @@ jobs:
       - name: Build
         run: npm run build
 
-      # The site's own link-integrity gate runs in the generator (`charly docs generate`), which
-      # lives in the charly superproject and is not available here. What this job can still prove
-      # is that the committed content builds cleanly from a clean install on a clean machine.
+      # The site's own link-integrity gate ran in the generator step above (now available here,
+      # since the pinned charly checkout carries it). What this job can still prove is that the
+      # committed content builds cleanly from a clean install on a clean machine.
       - name: Verify the build produced a site
         run: |
           test -f dist/index.html
diff --git a/.gitmodules b/.gitmodules
new file mode 100644
index 0000000..8abb876
--- /dev/null
+++ b/.gitmodules
@@ -0,0 +1,3 @@
+[submodule "charly"]
+	path = charly
+	url = https://github.com/opencharly/charly.git
diff --git a/CLAUDE.md b/CLAUDE.md
index a3dd0bd..5322554 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -1,6 +1,8 @@
-# docs/ — signpost (not the rule-set)
+# docs — signpost (not the rule-set)
 
-You are in the **opencharly/docs** submodule: the Starlight site published at opencharly.ai.
+You are in the **opencharly/docs** repository: the standalone Starlight site published at
+opencharly.ai. This repo pins the [charly](https://github.com/opencharly/charly) repository as a
+submodule and its `deploy.yml` workflow is the SOLE owner of generation, building and publishing.
 
 **Load these skills FIRST (R0):**
 
@@ -11,19 +13,19 @@ You are in the **opencharly/docs** submodule: the Starlight site published at op
 
 **Generated pages are not editable here.** `index.md`, `vision.md`, `grievances.md`,
 `liberation.md`, `reference/**` and `recipes/**` are emitted by `charly docs generate` and carry a
-`DO-NOT-EDIT`
-header; edit the SOURCE in the superproject (or the plugins submodule) and re-run `task docs:sync`.
-Only `start/`, `concepts/` and `guides/` are edited in this repo.
+`DO-NOT-EDIT` header; edit the SOURCE in the charly repository (or its plugins submodule) — the
+regeneration is a PR here that bumps the `charly` submodule pin in `.gitmodules` and commits the
+regenerated pages, and this repo's deploy workflow enforces it (its drift gate fails when the
+committed content does not match the pinned charly).
+Only `start/`, `concepts/` and `guides/` are edited directly in this repo.
 
-**The home page is one of the generated ones** — it is projected from the superproject's
-`README.md`. It was hand-authored until two thirds of it turned out to be README prose maintained
-twice across this submodule boundary, which had already drifted. To change the front page, change
-the README.
+**The home page is one of the generated ones** — it is projected from the pinned charly's
+`README.md`. To change the front page, change the README.
 
 **The Astro config is checked too.** A `link:` in `astro.config.mjs`'s sidebar is resolved against
 the emitted routes by `charly docs generate`; a dead one fails the run rather than rendering on
 every page of the site.
 
-**Authoritative rules live in the superproject's root `CLAUDE.md`.** R0–R10, the hard-cutover
+**Authoritative rules live in the charly repo's root `CLAUDE.md`.** R0–R10, the hard-cutover
 policy, and AI attribution are defined there — this file only signposts and restates no rule.
 History lives in this repo's `CHANGELOG/` (one file per CalVer version).
diff --git a/README.md b/README.md
index 2dbdc23..125dc2f 100644
--- a/README.md
+++ b/README.md
@@ -6,6 +6,13 @@ equipped factory floor for you and your agents. Published at **[opencharly.ai](h
 Built with [Starlight](https://starlight.astro.build/) on Astro, deployed by Cloudflare Pages on
 every push to `main`.
 
+**This repo is standalone.** It is not a submodule of any other repository — it PINS the
+[charly](https://github.com/opencharly/charly) repository as a submodule (`.gitmodules`), and its
+[`deploy.yml`](.github/workflows/deploy.yml) workflow is the SOLE owner of generation, building
+and publishing: it checks out the pinned charly, builds the charly binary, regenerates the
+generated half of the site with `charly docs generate`, fails if the committed content does not
+match the pinned charly (the drift gate), builds the Astro site and deploys to Cloudflare Pages.
+
 ## Half of this repo is generated
 
 The site has two halves, and they are maintained very differently.
@@ -18,22 +25,19 @@ src/content/docs/concepts/         candies & boxes, candyboxing, lifecycle, RDD/
 src/content/docs/guides/           authoring a plugin, the CLI
 ```
 
-The home page used to be here. It is now generated from the superproject's `README.md`, because
-two thirds of it was README prose maintained twice across a submodule boundary — and had already
-drifted. To change the front page, change the README.
-
-**Generated** — everything that already exists in the source repository, projected rather than
-copied, because a copy drifts and a projection cannot:
+**Generated** — everything that already exists in the pinned charly checkout, projected rather
+than copied, because a copy drifts and a projection cannot:
 
 ```
-src/content/docs/vision.md               VISION.md, verbatim
-src/content/docs/liberation.md           LIBERATION.md, verbatim
-src/content/docs/reference/cli/          one page per command word
-src/content/docs/reference/candy/        every defined candy
-src/content/docs/reference/box/          every defined box
-src/content/docs/reference/plugin/       every plugin candy: providers, placement, CUE schema
+src/content/docs/index.md               charly's README.md (the home page)
+src/content/docs/vision.md              charly's VISION.md, verbatim
+src/content/docs/liberation.md          charly's LIBERATION.md, verbatim
+src/content/docs/reference/cli/         one page per command word
+src/content/docs/reference/candy/       every defined candy
+src/content/docs/reference/box/         every defined box
+src/content/docs/reference/plugin/      every plugin candy: providers, placement, CUE schema
 src/content/docs/reference/providers.md  every reserved word → the plugin serving it
-src/content/docs/recipes/                every skill in the plugins corpus
+src/content/docs/recipes/               every skill in the plugins corpus
 ```
 
 Every generated file carries a `DO-NOT-EDIT` header. **Do not edit them** — the next
@@ -41,28 +45,25 @@ regeneration overwrites the whole tree.
 
 ## Regenerating
 
-From a checkout of the [charly superproject](https://github.com/opencharly/charly), where this
-repo is the `docs/` submodule:
-
-```bash
-task docs:sync     # regenerate into docs/
-task docs:dev      # local preview at http://localhost:4321
-```
-
-`task docs:sync` runs `charly docs generate`, served by the `candy/plugin-docs` runtime plugin.
-Regeneration on a clean tree is a no-op: if `git status` is dirty afterwards, a source changed.
+The generated half is produced by `charly docs generate` (served by charly's
+`candy/plugin-docs` runtime plugin) **from the charly commit this repo pins in `.gitmodules`**.
+The deploy workflow runs the regeneration on every main push and every PR, and **fails when the
+committed content does not match the pinned charly** — regeneration on a clean tree is a no-op,
+so a diff means the tree needs a regeneration landing:
 
-## Local development
+1. edit the source in the [charly repo](https://github.com/opencharly/charly) (a candy's
+   `description:`, a skill's prose, the README), which lands through charly's own PR flow;
+2. open a PR here that **bumps the charly submodule pin** to the merged charly commit and
+   **commits the regenerated pages** — run the generator locally with the charly checkout:
 
-```bash
-npm install
-npm run dev      # http://localhost:4321
-npm run build    # production build into dist/
-npm run preview  # serve the production build
-```
+   ```bash
+   cd charly && ../bin/charly docs generate --root . --out ../src/content/docs
+   ```
 
-Node 22 (see `.node-version`).
+   (or simply push the pin bump and let this workflow's drift gate list exactly what changed).
 
-## License
+Hand-written pages (`start/`, `concepts/`, `guides/`) are edited directly in this repo; the
+drift gate keeps proving the generated half still matches the pinned charly.
 
-MIT — see [LICENSE](LICENSE).
+The home page is one of the generated ones — it is projected from charly's `README.md`. To
+change the front page, change the README.
diff --git a/charly b/charly
new file mode 160000
index 0000000..92df8e6
--- /dev/null
+++ b/charly
@@ -0,0 +1 @@
+Subproject commit 92df8e639eca379835f3599b130a5aeda519f1fc
diff --git a/src/content/docs/concepts/10-the-cookbook-never-lies.md b/src/content/docs/concepts/10-the-cookbook-never-lies.md
index 2228382..b896a13 100644
--- a/src/content/docs/concepts/10-the-cookbook-never-lies.md
+++ b/src/content/docs/concepts/10-the-cookbook-never-lies.md
@@ -40,16 +40,15 @@ going stale.
 
 ## In practice
 
-The mechanism is a regeneration step and a drift gate. In *this* repository they are
-`task docs:sync` and `task docs:drift` — repository-maintenance commands, run by whoever changes a
-source, from a charly checkout. They are named here because the tenet is about how this site stays
-honest, not because you run them: `charly docs` is an out-of-process plugin supplied by the charly
-project itself, so it is not part of an installed `charly`.
-
-`docs:sync` re-emits every generated page from its source. `docs:drift` re-emits into a scratch
-tree and fails if anything differs — regeneration on a clean tree must be a no-op. If someone edits a generated page by hand, or
-changes a candy's `description:` without regenerating, `docs:drift` fails. Staleness becomes a
-build failure rather than a discovery someone makes months later.
+The mechanism is a regeneration step and a drift gate. In *this* site's repository the
+`deploy.yml` workflow is the sole owner: it checks out the pinned charly checkout, re-emits every
+generated page from its source (`charly docs generate`), and fails if the committed tree differs —
+regeneration on a clean tree must be a no-op. If someone edits a generated page by hand, or changes
+a candy's `description:` without regenerating, the drift gate fails. Staleness becomes a build
+failure rather than a discovery someone makes months later. The commands are named here because
+the tenet is about how this site stays honest, not because you run them: `charly docs` is an
+out-of-process plugin supplied by the charly project itself, so it is not part of an installed
+`charly`.
 
 The same discipline covers the examples on this site. The box quoted throughout these pages —
 [`tutorial-shell`](/reference/box/fedora/tutorial-shell/) — is not illustrative YAML; it is a real
diff --git a/src/content/docs/recipes/build/docs.md b/src/content/docs/recipes/build/docs.md
index 73db94d..f25f525 100644
--- a/src/content/docs/recipes/build/docs.md
+++ b/src/content/docs/recipes/build/docs.md
@@ -3,7 +3,7 @@ title: "docs"
 description: "The `charly docs generate` verb — the generator that renders the reference half of the opencharly.ai documentation site from this repo's canonical sources."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Recipe card from the `charly-build` plugin (Commands — runtime CLI verbs).*
 
@@ -16,8 +16,9 @@ description: "The `charly docs generate` verb — the generator that renders the
 ## Placement: a RUNTIME plugin, deliberately
 
 `plugin-docs` is **not** listed in `charly/charly.yml`'s `compiled_plugins:`. A dev-time
-documentation generator has no business inside every shipped `charly` binary — it is run on a
-contributor's machine to regenerate the site and nowhere else.
+documentation generator has no business inside every shipped `charly` binary — it is run by
+the opencharly/docs repo's deploy workflow against a pinned charly checkout, and on a
+contributor's machine for local preview, and nowhere else.
 
 charly prescans the declared `docs` word into the Kong grammar before parse and syscall.Exec's
 the plugin binary in CLI mode on the first actual `charly docs` invocation. Nothing about the
@@ -25,10 +26,10 @@ generator needs the host reverse channel (it reads files and writes markdown), w
 what makes the out-of-process placement free here — the same property that lets `charly candy`
 and `charly migrate` run in either placement.
 
-Because it is not in `compiled_plugins:`, the plugin is **not in `go.work`**. The repository's
-checked-in automation owns generator synchronization, drift checks, plugin tests, dependency
-installation, local preview, and production builds; those are maintainer workflows rather than
-public CLI instructions.
+Because it is not in `compiled_plugins:`, the plugin is **not in `go.work`**. Its Go tests
+and the docs-site R10 bed live in charly; generation, the content-drift gate, the Astro
+build and the publish all live in the opencharly/docs repo's `deploy.yml` workflow, which
+runs this verb against the charly commit its `.gitmodules` pins.
 
 ## Usage
 
@@ -86,18 +87,18 @@ no-op** — the same drift gate SDD applies to generated Go.
 Everything in that table is a PROJECTION, and every one of its sources is an ordinary
 file people edit for reasons that have nothing to do with the site — a candy's
 `description:`, a plan step, a skill's prose. Each such edit re-projects its page, and
-from that moment the committed site no longer matches its sources, so this project's
-drift gate over the `docs` submodule stays red until the regenerated pages are committed
-there. **A one-line prose fix is therefore never a one-repo change.** Plan it as a
-multi-repo landing before starting, not after a gate says so.
+from that moment the committed site no longer matches its sources, so the docs repo's
+drift gate (its deploy workflow regenerates and fails on any diff) stays red until a docs
+PR lands the regenerated pages. **A one-line prose fix is therefore never a one-repo
+change.** Plan it as a multi-repo landing before starting, not after a gate says so.
 
 Two chains reach the site, and they are not the same length.
 
 **Candy and box prose — ONE hop.** `reference/candy/` and `reference/box/` are read
 straight off disk, walking each repo as its own project root. Editing a
 `candy/<name>/charly.yml` `description:` or `plan:` re-projects
-`reference/candy/<name>.md` in the same tree, immediately. The `docs` commit belongs to
-the same cutover as the candy edit.
+`reference/candy/<name>.md` in the same tree, immediately. The docs PR (regenerating at
+the pinned charly) belongs to the same cutover as the candy edit.
 
 **Skill prose — TWO hops.** `recipes/` is NOT read from `candy/*/charly.yml`. A skill is
 authored as a `skill:` entity in its owning candy; `charly marketplace generate` projects
@@ -107,14 +108,14 @@ that into `plugins/<plugin>/skills/<name>/SKILL.md`, and `charly docs generate`
 has run, and on any CLEAN checkout the tree it reads is the pinned gitlink, so the change
 reaches readers only once the `plugins` landing merges and the superproject's `plugins`
 gitlink advances. **Advancing that gitlink without regenerating `docs` in the same change
-is what leaves the published site stale**, and nothing except this project's drift gate
+is what leaves the published site stale**, and nothing except the docs repo's drift gate
 will say so — which is precisely how the mirror has fallen behind before.
 
 The ordering this forces — regenerate the projection, land the producer repo, then the
-consumer and its pin — is [`/charly-internals:git-workflow`](/recipes/internals/git-workflow/) B6's producer→consumer rule
-applied to documentation. That skill owns the landing sequence, including the
-maintenance commands this project runs for it; the mechanism above is what makes the
-sequence necessary, and is all this page needs to carry.
+consumer (docs) and its charly pin — is [`/charly-internals:git-workflow`](/recipes/internals/git-workflow/) B6's
+producer→consumer rule applied to documentation. That skill owns the landing sequence;
+the mechanism above is what makes the sequence necessary, and is all this page needs to
+carry.
 
 ## Defined, not default-active
 
@@ -199,5 +200,5 @@ core-spine words (`box`, `version`, `reap-orphans`) that are not `command:` prov
 ## When to Use This Skill
 
 Invoke before running or modifying `charly docs`, editing `candy/plugin-docs`, touching the
-`docs/` submodule's generated trees, or changing anything the generator reads (a candy's
+generated trees in the opencharly/docs repo, or changing anything the generator reads (a candy's
 `description:`, a plugin's `providers:` or CUE schema, a skill's frontmatter or cross-references).
diff --git a/src/content/docs/recipes/internals/agents/parallel-bed-testing.md b/src/content/docs/recipes/internals/agents/parallel-bed-testing.md
index 3649224..3446346 100644
--- a/src/content/docs/recipes/internals/agents/parallel-bed-testing.md
+++ b/src/content/docs/recipes/internals/agents/parallel-bed-testing.md
@@ -3,7 +3,7 @@ title: "agents — parallel-bed-testing"
 description: "Detail page of the agents recipe card."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Detail page of the [agents](/recipes/internals/agents/) recipe card.*
 
@@ -537,8 +537,9 @@ The playbook:
    trees ran overlapping (one finishing in roughly 79s, nested inside a
    sibling's roughly 265s window), with separate per-tree
    `.check/`/`.build/` outputs, zero cross-contamination, zero leftover
-   deploys. Requirements: the worktree needs the `sdk` submodule inited
-   (`task build:binary` reads `scripts/calver.sh`) but
+   deploys. Requirements: the worktree needs NO sdk/spec checkout
+   (`task build:binary` reads `scripts/calver.sh`; the sdk + spec contract
+   modules resolve from the module proxy at the pinned require versions) but
    not the `box/<distro>` submodules for the root disposable roster: root
    `charly.yml` imports the distro namespaces (arch/cachyos/fedora) via
    remote `@github.com/opencharly/distro-*` refs (resolver-fetched into
diff --git a/src/content/docs/recipes/internals/git-workflow/branch-and-pr-loop.md b/src/content/docs/recipes/internals/git-workflow/branch-and-pr-loop.md
index 47fd083..8ca8292 100644
--- a/src/content/docs/recipes/internals/git-workflow/branch-and-pr-loop.md
+++ b/src/content/docs/recipes/internals/git-workflow/branch-and-pr-loop.md
@@ -3,7 +3,7 @@ title: "git-workflow — branch-and-pr-loop"
 description: "Detail page of the git-workflow recipe card."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Detail page of the [git-workflow](/recipes/internals/git-workflow/) recipe card.*
 
@@ -170,21 +170,21 @@ git's own resolution instead, which covers every path at once:
 merge without ever conflicting". Use the per-path form above only as a quick
 confirmation once the whole-tree comparison has already told you where to look.
 
-**The `docs` gitlink has a TWIN in candy config, and no gitlink check can see
-it.** Both checks above compare gitlinks. `candy/docs-site/charly.yml` records
-the same `docs` sha twice as ordinary text — once as the `DOCS_REF` var (the
-clone step's ENV, hence that layer's cache key) and once as the literal matcher
-in the `docs-site-pinned-commit` check — and a merge resolves plain text
-line-wise, with no notion that those strings name a commit. So a recovery can
-satisfy the descendant-wins check AND the whole-tree comparison while leaving the
-candy pinned at the older commit: the same regression this class produces, one
-surface over, invisible to every instrument aimed at it. The two gates are
-complementary and neither substitutes for the other — the gitlink checks above
-catch a gitlink that went backwards; `task docs:pin` catches the gitlink and the
-candy pins DISAGREEING, and passes silently when both regressed together. So run
-BOTH whenever a recovery touches the `docs` gitlink. The rule is lockstep: the
-gitlink and its candy pins move together, on a recovery exactly as on the forward
-landing (B6a step 4, which also owns that re-pin's change class).
+**The docs site's pin lives ONLY in candy config, and no gitlink check can see
+it.** Since the docs de-submodule cutover, charly carries no `docs` gitlink;
+`candy/docs-site/charly.yml` records the tested `docs` sha twice in two plain-text
+surfaces — once as the `DOCS_REF` var (the clone step's ENV, hence that layer's
+cache key) and once as the literal matcher in the `docs-site-pinned-commit` check
+— and a merge resolves plain text line-wise, with no notion that those strings
+name a commit. So a recovery can leave the candy pinned at an older commit than
+the docs `main` the forward landing published: the same regression this class
+produces, one surface over, invisible to every instrument aimed at gitlinks. The
+gates are complementary — the gitlink checks compare checked-out repos;
+`task docs:pin` compares `DOCS_REF` against the docs repo's CURRENT `main` head
+(fetching `ls-remote`) and fails when they disagree, passing silently only when
+both regressed together. So run BOTH whenever a change touches the docs site. The
+rule is lockstep: the candy pins move with the docs `main`, on a recovery exactly
+as on the forward landing (B6a step 4, which also owns that re-pin's change class).
 
 **Multi-committer main advances — out-of-tree PRs from other committers.** The
 orchestrator is NOT the sole source of `main` advances: another committer (a human
diff --git a/src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md b/src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md
index b3b3764..51372ed 100644
--- a/src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md
+++ b/src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md
@@ -3,7 +3,7 @@ title: "git-workflow — multi-repo-coordination"
 description: "Detail page of the git-workflow recipe card."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Detail page of the [git-workflow](/recipes/internals/git-workflow/) recipe card.*
 
@@ -214,14 +214,16 @@ not steps an charly user runs, and they never belong on a reader-facing page.
    necessarily still pins the old gitlink until the projection merges — so that
    redness is not the tagging bar for the projection PR itself: its gate is the
    `plugins` tree, and the superproject catches up at step 5.
-3. **Regenerate and land `docs`** — `task docs:sync`, review the emitted pages,
-   commit them in the `docs` submodule; `task docs:drift` is the gate, re-syncing
-   and failing on any dirty path. Regeneration rewrites the generated trees
-   WHOLESALE, so a mirror already behind cannot be brought forward selectively:
-   every pending page lands with the next commit or none of them do. Budget for
-   carrying someone else's backlog when the mirror has drifted.
+3. **Regenerate and land `docs`** — a docs PR that bumps the charly pin in
+   `docs/.gitmodules` and carries the regenerated pages; the docs repo's deploy
+   workflow is the gate (it regenerates on every run and fails on any diff).
+   Regeneration rewrites the generated trees WHOLESALE, so a mirror already
+   behind cannot be brought forward selectively: every pending page lands with
+   the next commit or none of them do. Budget for carrying someone else's
+   backlog when the mirror has drifted.
 4. **Re-pin the new `docs` merge** in `candy/docs-site/charly.yml`; `task
-   docs:pin` is the gate and asserts more than one occurrence
+   docs:pin` is the gate (it compares `DOCS_REF` against the docs repo's current
+   `main` head) and asserts more than one occurrence
    ([`/charly-tools:docs-site`](/recipes/tools/docs-site/) owns the pin contract). This step edits candy
    CONFIG rather than prose, so its own gate is the `check-docs` bed — a
    documentation-only change class does NOT cover it, and the commit that carries
diff --git a/src/content/docs/recipes/internals/skills.md b/src/content/docs/recipes/internals/skills.md
index 5ed65c9..c5923fb 100644
--- a/src/content/docs/recipes/internals/skills.md
+++ b/src/content/docs/recipes/internals/skills.md
@@ -3,7 +3,7 @@ title: "skills"
 description: "Skill maintenance guidelines: when and how to update skills, root rulebooks, and README.md."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Recipe card from the `charly-internals` plugin (Development — contributor internals).*
 
@@ -56,8 +56,9 @@ Every skill in this repo is rendered as a page on **opencharly.ai** by `charly d
 - **`references/*.md` split files become child pages**, linked from their entry card. They carry
   no frontmatter by contract; only the entry `SKILL.md` does.
 
-The upside is that the corpus's internal consistency is now machine-checked: `task docs:drift`
-and the docs build together catch a stale cross-reference that a `git grep` sweep missed.
+The upside is that the corpus's internal consistency is now machine-checked: the docs
+repo's drift gate (its deploy workflow regenerates and fails on any diff) and the docs
+build together catch a stale cross-reference that a `git grep` sweep missed.
 
 ## When NOT to Update Skills
 
@@ -70,7 +71,7 @@ and the docs build together catch a stale cross-reference that a `git grep` swee
 ## How to Update
 
 1. **Edit the candy's `skill:` entity in `candy/<candy>/charly.yml`, then regenerate**
-   (`charly marketplace generate` → `task docs:sync`). Most of the corpus under
+   (`charly marketplace generate` → a docs PR carrying the regenerated pages). Most of the corpus under
    `plugins/**/SKILL.md` is a PROJECTION and carries a DO-NOT-EDIT banner: an edit
    there is reverted by the next regeneration, silently and without conflict. `git grep
    -l 'DO[- ]NOT[- ]EDIT'` tells you which files are generated — but triage the hits
diff --git a/src/content/docs/recipes/tools/docs-site.md b/src/content/docs/recipes/tools/docs-site.md
index 2ec6009..532b472 100644
--- a/src/content/docs/recipes/tools/docs-site.md
+++ b/src/content/docs/recipes/tools/docs-site.md
@@ -3,7 +3,7 @@ title: "docs-site"
 description: "The Astro + Starlight toolchain candy for the opencharly.ai documentation site, and the check-docs R10 bed that builds and proves it."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 *Recipe card from the `charly-tools` plugin (Images — the deployable catalog).*
 
@@ -30,10 +30,10 @@ scalar, a Starlight config a version bump invalidated, a page whose markdown wil
 fails the **image build**, from the same input Cloudflare Pages builds, so the box catches it
 before a deploy can.
 
-## Why it clones instead of copying the submodule
+## Why it clones the published repo
 
-The `docs/` submodule sits right beside `candy/docs-site/` in the working tree, and the candy
-still cannot read it. Both escapes are closed by design:
+The docs repo is standalone — charly no longer carries it as a submodule — and the candy
+cannot read another directory even if it did. The escapes are closed by design:
 
 - `copy:` rejects `..` — `copy: "../../docs/x" may not contain .. (no traversal)` at validate.
 - There is **no** field for pointing a candy at another directory. (An older revision of
@@ -46,8 +46,10 @@ exact source Cloudflare builds.
 
 **Ordering consequence.** The candy fetches an IMMUTABLE COMMIT, never a branch, so `DOCS_REF`
 must be re-pinned to each new docs merge before this box — and the `check-docs` bed above it —
-prove the new content. `task docs:pin` fails if it drifts from the `docs` gitlink. That re-pin
-is a CONFIG edit and changes the landing's gate and attribution tier — see
+prove the new content. `task docs:pin` fails when `DOCS_REF` is not the docs repo's current
+`main` head (it fetches `ls-remote` and compares — charly has no `docs` gitlink anymore: the
+docs repo is standalone, and this candy's pin is the only thing that fixes what the bed tests).
+That re-pin is a CONFIG edit and changes the landing's gate and attribution tier — see
 [`/charly-internals:git-workflow`](/recipes/internals/git-workflow/) (B6a step 4, and B2 "the derivation stops at the gitlink" for
 the general rule), which owns landing mechanics.
 
@@ -57,10 +59,10 @@ the general rule), which owns landing mechanics.
 substitution does not resolve a candy `var:`, so writing `${DOCS_REF}` in the matcher makes
 charly SKIP the step with "unresolved variables: DOCS_REF" — turning the anti-staleness guard
 into a non-event while the bed still reports PASS, which is the exact failure the guard exists
-to catch. `task docs:pin` therefore asserts BOTH: it compares `DOCS_REF` against the gitlink
-AND requires the sha to occur at least twice in the file, so a re-pin that updates only one of
-them fails the gate instead of leaving the check asserting a commit nobody builds. Re-pinning
-means editing both.
+to catch. `task docs:pin` therefore asserts BOTH: it compares `DOCS_REF` against the docs
+repo's current `main` head AND requires the sha to occur at least twice in the file, so a
+re-pin that updates only one of them fails the gate instead of leaving the check asserting a
+commit nobody builds. Re-pinning means editing both.
 
 A branch name here is a correctness bug, not a convenience: `var:` values are emitted as `ENV`
 above the steps, so with `main` the clone layer's cache key never moves and the bed silently
@@ -70,7 +72,7 @@ proving stale content until review caught it.
 | Var | Default |
 |---|---|
 | `DOCS_REPO` | `https://github.com/opencharly/docs.git` |
-| `DOCS_REF` | the `docs` gitlink sha — an immutable commit, **never a branch** (see above) |
+| `DOCS_REF` | the docs repo commit this bed tests — an immutable commit, **never a branch** (see above) |
 
 ## Verification
 
@@ -108,19 +110,20 @@ A `disposable: true` pod deploy of `docs-site-app`. Reaching steady state proves
 starts and stays up; the shape assertions already ran at image build.
 
 The generator's own cross-reference integrity gate (an unresolvable
-`/charly-<plugin>:<skill>` reference fails generation) is covered by the plugin's Go tests plus
-`task docs:drift` on the host — this bed covers the artifact those produce.
+`/charly-<plugin>:<skill>` reference fails generation) is covered by the plugin's Go tests —
+this bed covers the artifact those produce. The committed content's staleness against the
+pinned charly is gated by the docs repo's own deploy workflow, not here.
 
 ## Troubleshooting
 
 | Symptom | Cause |
 |---|---|
 | `docs-site-pinned-commit` FAILS | the clone layer is stale, or `DOCS_REF` was re-pinned without rebuilding |
-| `task docs:pin` fails | `DOCS_REF` (or the check's literal sha) drifted from the `docs` gitlink |
+| `task docs:pin` fails | `DOCS_REF` (or the check's literal sha) is not the docs repo's current `main` head — bump it in a charly PR |
 | fetch fails `upload-pack: not our ref <sha>` | `DOCS_REF` names a commit not reachable from ANY ref in the docs repo — never pushed, or garbage-collected |
 | fetch fails `couldn't find remote ref <x>` | `DOCS_REF` is not a FULL 40-hex sha — either a ref name (which the pinned-commit contract forbids) or an abbreviated sha, which `git fetch` rejects even when the commit exists |
 | `npm ci` fails on a lockfile mismatch | `package.json` and `package-lock.json` disagree — regenerate the lockfile in the docs repo |
-| a shape check fails but `index.html` exists | the generator emitted a different tree layout; re-run `task docs:sync` and check the diff |
+| a shape check fails but `index.html` exists | the generator emitted a different tree layout; regenerate in the docs repo (its deploy workflow) and check the diff in a docs PR |
 
 ## Cross-References
 
diff --git a/src/content/docs/reference/candy/docs-site.md b/src/content/docs/reference/candy/docs-site.md
index f956270..f032573 100644
--- a/src/content/docs/reference/candy/docs-site.md
+++ b/src/content/docs/reference/candy/docs-site.md
@@ -3,7 +3,7 @@ title: "docs-site"
 description: "The Astro + Starlight toolchain for the opencharly.ai documentation site, plus a production build of the published site itself, built inside the box."
 ---
 
-<!-- DO-NOT-EDIT — generated by `charly docs generate`; run `task docs:sync` to regenerate. -->
+<!-- DO-NOT-EDIT — generated by `charly docs generate`; the opencharly/docs repo's deploy workflow regenerates this tree against its pinned charly checkout. -->
 
 | | |
 |---|---|
@@ -20,15 +20,16 @@ frontmatter scalar, a Starlight config a version bump invalidated, a page whose
 markdown will not parse — fails the IMAGE BUILD, which builds from the same input
 Cloudflare Pages does, so the box catches it before a deploy can.
 
-WHY IT CLONES rather than copying the docs/ submodule out of the working tree: a
-candy's copy: source may not traverse above its own directory (`..` is rejected at
-validate time), and there is no schema field for pointing a candy at a sibling
-directory. Cloning the published repository is the schema-legal path, and it happens
-to test the more useful thing — the exact source Cloudflare builds. It fetches an
-IMMUTABLE COMMIT (`DOCS_REF` below), never a branch: that is what keeps the clone
-layer's cache key moving with the pin. The consequence is an ordering dependency —
-docs content must be merged, and `DOCS_REF` re-pinned to that merge, before this box
-(and the check-docs bed above it) prove the new content.
+WHY IT CLONES the published repo rather than reading a local checkout: charly no
+longer carries the docs/ submodule (the docs de-submodule cutover made the docs repo
+standalone), and even with a checkout a candy's copy: source may not traverse above
+its own directory (`..` is rejected at validate time) — there is no schema field for
+pointing a candy at a sibling directory. Cloning the published repository is the
+schema-legal path, and it happens to test the more useful thing — the exact source
+Cloudflare builds. It fetches an IMMUTABLE COMMIT (`DOCS_REF` below), never a branch:
+that is what keeps the clone layer's cache key moving with the pin. The consequence is
+an ordering dependency — docs content must be merged, and `DOCS_REF` re-pinned to that
+merge, before this box (and the check-docs bed above it) prove the new content.
 
 The checks below assert the site's SHAPE, not merely its existence: a generated CLI
 page, a candy page carrying its published acceptance plan, a recipe card from the
```


## Hand-authored sweep (B14(d) evidence — executed on the final committed tree)

```
$ grep -rn 'docs:sync\|docs:drift\|docs/ submodule' src/content/docs/start src/content/docs/concepts src/content/docs/guides
(no output — exit 1, no matches; the cookbook page was the ONLY hit before this PR fixed it)
$ grep -rl 'task docs:sync' src/content/docs/recipes src/content/docs/reference src/content/docs/index.md src/content/docs/vision.md src/content/docs/liberation.md src/content/docs/grievances.md | wc -l
0
```

## How tested

**The workflow's exact steps, end-to-end on a FRESH clone of this branch (pasted):**

```
$ git clone /tmp/docs-rdd/docs  &&  git checkout feat/docs-standalone
$ git submodule update --init --recursive
Submodule path 'charly/plugins': checked out 'f4f5980…'
$ cd charly/charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=docs-ci" -o ../../bin/charly .
$ cd charly && ../bin/charly docs generate --root . --out ../src/content/docs
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 346 candy pages, 82 box pages (912 generated pages cleared before regeneration)
$ cd .. && git diff --exit-code -- src/content/docs && echo "DRIFT GATE: no-op (PASS)"
DRIFT GATE: no-op (PASS)
$ npm ci --no-audit --no-fund && npm run build
[build] 932 page(s) built in 8.09s
[build] Complete!
$ test -f dist/index.html && echo "pages built: $(find dist -name '*.html' | wc -l)"
pages built: 932
```

**The generator's own gates ran inside the generate step** (an unresolvable `/charly-x:y` reference or a dead sidebar link fails the run — both passed).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + docs skills loaded before editing |
| R2 | no "pre-existing" — the cookbook page + every hand-authored stale reference fixed in this PR |
| R1 | the hand-authored sweep found + fixed the cookbook page's stale `task docs:sync`/`docs:drift` passage (the only hand-authored stale reference); the generated pages carry the new header by construction |
| R2 | no "out of scope" — the cookbook fix is in this PR |
| R3 | `deploy.yml` is the single pipeline owner; the drift gate is the single content gate |
| R4 | no workarounds — the pinned submodule + workflow is the mechanism |
| R5 | the old pipeline's references (deploy.yml comments, README, CLAUDE, cookbook) all replaced in this commit |
| R6 | no force-push; PR-only landing; status checked |
| R7 | the full workflow simulation pasted above, on a fresh clone of the final branch tree |
| R8 | N/A — no generation/emitted-artifact code changed in this repo (the generator lives in charly, pinned here; its output IS the content this PR commits) |
| R9 | N/A — no binary built by this repo (the charly binary is built from the pinned checkout inside the workflow) |
| R10 | the fresh-clone simulation IS the R10 for this repo (the workflow is the runtime path; it ran end-to-end with retained output) |

## Change classification

- **Change class**: CI/infra cutover (workflow + submodule pin + generated content)
- **Verification gate**: the workflow's own steps on a fresh clone — build, generate, drift no-op, astro build 932 pages, dist artifact present
- **Attribution tier**: fully tested and validated — the exact workflow steps ran live end-to-end with retained output on the final committed tree
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*