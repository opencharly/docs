# 2026.234.1200 — spec de-submodule: regenerated reference pages

Regeneration from the charly superproject's de-submodule cutover
(opencharly/charly#371) + the plugins leg (opencharly/plugins#221): the spec
contract module resolves from the module proxy at the pinned require version —
no submodule, no local checkout.

Updated pages (spec-claim hunks only, exact `charly docs generate` output):
- `recipes/build/migrate.md` — cross-repo schema cutover consuming the
  published module at the bumped require version.
- `recipes/internals/egress.md` — the superproject `task cue:gen` regenerates
  only the plugin params.
- `recipes/internals/go/schema-change-recipe.md` — spec-repo-first sequence
  with a require-version bump in this repo.
- `recipes/internals/go/source-map.md` — spec resolves from the module proxy;
  no spec entry in `go.work`.

Documentation-only change class. The site's remaining staleness (accumulated
source drift since the last full sync) rides the standing docs sync.
