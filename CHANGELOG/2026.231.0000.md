# 2026.231.0000 — regenerate the box pages at the reconciled candy pins

The `box/fedora` and `box/cachyos` submodules advanced to their merged mains
(`b637b817`, `e6aa5ee6`), which reconcile every candy pin to `v2026.231.0714`
and add the `cachyos-base-check` composition. Regenerating the reference pages
carries those changes: 55 box pages updated, plus the new
`reference/candy/cachyos/cachyos-base-check.md` page.

The four inherited baseline entries (`recipes/build/generate.md`,
`recipes/internals/git-workflow/evidence-and-freshness.md`,
`recipes/internals/install-plan/step-kinds.md`, `reference/providers.md`) are
deliberately NOT committed — they project unlanded `charly#315` sources and
stay recorded in the drift baseline.

Generated by `charly docs generate`; never hand-edited.
