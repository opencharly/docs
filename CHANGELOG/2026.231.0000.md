# 2026.231.0000 — ollama projection pages + the libvirt-renderer LOC cell

Five generated pages land, each the projection of a source that has now landed:

- `reference/cli/ollama.md` — projects `candy/plugin-ollama`, which existed on no
  landed tree when docs#83 removed this page; `charly#309` has since landed, so the
  page can ship. Title corrected to `ollama` and the help text now names the command
  tree rather than a `charly ollama --help` that does not exist.
- `reference/box/fedora/ollama.md` and `reference/box/cachyos/ollama.md` — the two
  ollama boxes now compose `ollama-cuda` (the split-GPU-backend packaging from
  `charly#309`) and their candy pins reconcile to `v2026.231.0714`.
- `reference/box/cachyos/ollama-rocm.md` — the new `ollama-rocm` box from the
  cachyos#15 landing.
- `recipes/internals/libvirt-renderer.md` — the `spec/schema/vm.cue` row gains its
  `~720` LOC count. This is the docs half of a two-hop chain that reads the plugins
  tree at the pinned gitlink; the source landed in plugins#212 (`cb804e7b`), which
  `charly/main` now pins.

Generated by `charly docs generate`; never hand-edited.
