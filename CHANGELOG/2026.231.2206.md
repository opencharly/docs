# 2026.231.2206 — batch-2.1 charly-internals prose fixes

Projection of the six batch-2.1 `candy/charly-internals/charly.yml` skill edits
into the docs recipes (`charly docs generate` reads the `plugins` tree):

- `recipes/internals/git-workflow/evidence-and-freshness.md` — the guard
  symptom-row entry; the content-scoping rule now names the rule explicitly
  instead of pointing "two paragraphs up"; the page-wide NONZERO-BEFORE detector
  paragraph.
- `recipes/internals/git-workflow/multi-repo-coordination.md` — B6a step 2 notes
  superproject-facing drift is EXPECTED while a projection PR is in flight.
- `recipes/internals/libvirt-renderer.md`, `recipes/internals/vm-spec.md`,
  `recipes/internals/go/source-map.md` — `sdk/vmshared/libvirt_yaml.go` marked as
  the stub it is; LOC corrected.

Landing order: plugins projection merged first (`437dd282`), then this docs
projection, then the superproject pin (gitlinks + `DOC-REF`).
