# 2026.254.1352 — chore(docs): sync the marketplace corpus gitlink (wonky manifests + swept corpus)

## Summary

Syncs the docs repo's marketplace submodule gitlink to the current marketplace main (post-#296 corpus sweep + #297 wonky-by-generation manifests) and regenerates: the gitlink'd corpus is now the generator input, so the site carries the swept corpus and the wonky manifest metadata.

- marketplace gitlink `5b6833a3…` → `bd58a38e…` (marketplace main), charly pin (d507ca5a) + plugin-docs pin (v2026.254.1214) unchanged.
- Regenerated with the proven recipe (marketplace at the new gitlink, corpus wiring from pins.tsv, astro seed); drift-gate green by construction (byte-identical rerun, 1403/1403 sha1).

## Residual (named, not silent)

The docs-recipe page TEXT is driven by the charly-side corpus candy pin (`layer-charly-build@v2026.243.0949`, declared in the box-distro's charly-marketplace candy), which last-write-wins over the marketplace corpus file at the same page path — so the swept skill's prose reaches the live recipes when that charly-side layer pin bumps (the org's corpus-refresh/sync cycle carries it). The gitlink'd corpus and wonky manifests are live in the emitted tree now.

## How tested

From the pushed tree (branch feat/corpus-sync):

```
$ generate (marketplace @ bd58a38e) → rc 0; rerun byte-identical (1403/1403 sha1)
$ drift: regenerated == committed → gate green by construction
```

## Rulebook compliance

- R1 — the projection-lag root cause (charly-side pin last-write-wins) RCA'd and named, not swept under.
- R3 — the gitlink is the corpus input; the emitted tree mirrors it.
- R6 — no force-push/amend; single clean commit; hand changes confined to the gitlink + regenerated content.

## Change classification

- **Change class:** docs content regeneration + submodule gitlink sync.
- **Verification gate:** generation rc 0 + byte-identical drift.
- **Attribution tier:** analysed on a live system — the regeneration ran live with retained output; deploy runs the drift gate at merge.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*