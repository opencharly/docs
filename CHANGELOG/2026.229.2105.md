# 2026.229.2105 — the site catches up with the corrected skill corpus

**This is a REGENERATION, not an authored edit.** Every page carries
`DO-NOT-EDIT — generated by `charly docs generate``; the source is the skill corpus at
`opencharly/plugins@67219d44` (`v2026.229.2031`), itself projected from candy entities in
`opencharly/charly#289`.

## What arrives

Eight recipe pages, all two-hop projections (`candy → plugins SKILL.md → docs recipe`):

| page | what changed |
|---|---|
| `recipes/internals/git-workflow.md`, `references/validator-and-calver.md` | the `pkg/*` submodules dropped from the tag-every-repo rule — removed from `charly/main` by `ca76c354` |
| `references/branch-and-pr-loop.md` (+26), `references/evidence-and-freshness.md` (+55) | **not** the `pkg/*` change — these carry new sections that reached the source when `charly#289` merged `charly/main`: a gitlink-ancestry verification block, and *"a guard that cannot fail is worse than no guard"*. Neither adds a line mentioning `pkg` |
| `references/multi-repo-coordination.md`, `recipes/internals/cutover-policy.md`, `recipes/core/clean.md` | re-projected from the merged source |
| `recipes/ollama/ollama-layer.md` | loses a false `cuda` dependency claim (description and table), a `task:` install mechanism the candy does not define, and a `${CONTAINER_IP}` construction this corpus's own gotchas page rules `❌ WRONG` |

## What this leg does NOT carry, and a claim this entry first got wrong

`plugins#197` pruned `ollama/skills/ollama-cli/SKILL.md` and removed two sections whose producing
source no longer exists. **None of that content ever reached this site**, and this entry originally
claimed otherwise. Measured across the whole history of `docs`:

```
git log -S'OLLAMA_VERSION' -- src/content/docs      0 commits, ever
git log -S'HOST_PORT'      -- src/content/docs      3 commits   ← the search discriminates
"charly ollama" on docs/main                        0 files
```

So the false-capability problem was real **in `plugins`**, and this leg carries none of it. The
removed text is quoted verbatim in `plugins/CHANGELOG/2026.229.2031.md`, with `charly#291` as the
tracked exit; this entry does not repeat it, because the record belongs in one place and that repo is
where the artifact lived.

`recipes/ollama/ollama-cli.md` was never published either, so nothing is removed from the site for
it. The still-open `docs#70` is the leg that *would* have published that content — which is where
the claim this entry first made would have been true.

## Scope

Five further pages drift at this base — `recipes/internals/{skills,vm-spec}.md` and
`recipes/vm/{arch-cloud-vm,vm,vms-catalog}.md`. They are **not** projections of anything
`plugins#197` changed; the site is behind `plugins/main` for them from an earlier leg, and they
belong to whichever cutover owns that content. They are left alone rather than absorbed under this
CalVer.

**The revert set was re-derived at this tree**, not carried forward, and it differs from every prior
regeneration in this cutover — as it has every time.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
