# 2026.227.0845 — docs: regenerate the rpmfusion candy page

## Summary of changes

Regenerates `reference/candy/rpmfusion.md` from the superproject's `candy/rpmfusion/charly.yml`, whose description and acceptance plan changed when the RPM Fusion release packages became signature-verified.

- **`src/content/docs/reference/candy/rpmfusion.md`** — generated output, +6/-3.
- **`CHANGELOG/2026.227.0845.md`** — new entry (PLACEHOLDER CalVer).

Generated output only. No hand-authored page changed, no route moved.

## Change class

**docs-only** — one generated page plus a CHANGELOG entry.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** the non-runtime standards — `task docs:drift` (regeneration must be a no-op once committed) and the production site build.
- **Eval beds run:** `N/A (docs-only)`. Note the superproject change that produced this page WAS bed-gated: `charly check run check-docs` PASS 13/13 with **zero warnings**, which is the run that proved the underlying `rpmfusion` fix.
- **Fresh rebuild:** the page was produced by `task docs:sync`, which rebuilds the binary first (`task build:binary`).
- **Did the CHANGED code path execute live?** `N/A` — no code path; this is generator output.

```
$ task docs:sync
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 325 recipe pages, 91 plugin pages (147 provider words), 49 cli pages,
             338 candy pages, 81 box pages (889 generated pages cleared before regeneration)
docs: regeneration changed the site — review and commit in the docs submodule:
 M src/content/docs/reference/candy/rpmfusion.md

$ git diff --stat
 src/content/docs/reference/candy/rpmfusion.md | 9 ++++++---
 1 file changed, 6 insertions(+), 3 deletions(-)
```

## Attribution tier

**`documentation reviewed`** — the whole diff is one generated markdown page plus a CHANGELOG entry. No Go, no config, no runtime path.

## Harness rulebook compliance

- **R0 skills:** `/charly-build:docs` (the generator and its DO-NOT-EDIT contract), `/charly-tools:docs-site` (the site + its bed).
- **R1 RCA + zero warnings:** the drift was root-caused to the superproject candy edit, not patched by hand-editing the generated page.
- **R2 no out-of-scope:** this is the drift that superproject change creates; it lands immediately rather than being parked.
- **R3 no duplication:** the page is a projection — the candy remains the single source.
- **R4 no workaround:** the page was regenerated, never hand-edited (it carries a DO-NOT-EDIT header).
- **R5 hard cutover + grep:** `N/A` — nothing removed or renamed.
- **R6 git safety:** branch cut from fresh `origin/main`; no force-push, no history rewrite.
- **R7 runtime gate / R8 artifacts / R9 binary+deps:** `N/A — docs-only`.
- **R10 disposable + coverage:** `N/A (docs-only)` for this repo. The behaviour the page describes is covered by the superproject's new `rpmfusion-key-imported` check.
- **RDD / ADE / SDD:** `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law / Disposable-only autonomy / Go gates:** `N/A`.
- **CHANGELOG:** `CHANGELOG/2026.227.0845.md` with a placeholder CalVer for the validator to finalize.

## Ordering note

This lands **before** the superproject cutover that produced it: that cutover's `task docs:drift` gate fails until this page is committed here, after which it re-pins `DOCS_REF` and the docs gitlink to this merge.

---

*Assisted-by: Claude Code Anthropic Claude Opus 5 (documentation reviewed)*