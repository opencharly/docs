# 2026.218.0050 — the landing page catches up with the corrected README

A pure `task docs:sync` — one generated page, `index.md`, the projection of `README.md`.

The excerpt checker (charly#221, `v2026.217.2138`) made the README's YAML blocks tell the truth
— the check-group entry as the canonical sibling form, the box/fedora comments as comment-only
lines. The landing page is the projection of that README, so it must carry the same correction.
It could not be regenerated with docs#14 (at that PR's base the README still had the old blocks);
now that the README fix is on super main, this sync carries it.

## Gate

`task docs:drift` is a no-op after this commit.

## Provenance

Generated from the superproject at `c705365a` with `plugins` at `567719c3`. One file changed.
