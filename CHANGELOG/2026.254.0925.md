# 2026.254.0925 — docs: regenerate at charly d507ca5a + plugin-docs v2026.254.0559 — config-driven catalog view + wonky rebrand

## Summary

Regenerates the opencharly.ai site at the new producer pins with the **config-driven catalog view** generator, completing the docs-generation rearchitecture (everything-is-a-plugin):

- **`deploy.yml` pins:** CI-time charly commit `e19198ca` → `d507ca5a` (charly main now carries the top-level `docs:` config node: compiled corpus + release repos plugin-review/plugin-pipeline + extra repos plugin-gh/plugin-docs); plugin-docs `v2026.242.0527` → `v2026.254.0559` (the config-driven catalog view release). Marketplace mechanism unchanged.
- **Hand-authored surfaces only:** `astro.config.mjs` site description → the wonky tagline; `guides/the-cli.md` `/reference/cli/fleet/` → `/reference/cli/deploy/` (the fleet word retired to deploy, cutover C2); `concepts/00-vocabulary.md` + `02-one-recipe-many-molds.md` plugin-example links → the split repos' emitted version-qualified plugin-example-command page. All DO-NOT-EDIT pages regenerated, none hand-edited.
- **Regenerated content** (single commit `dd28af5c`): full site at the merged main; 132 stale versioned tag-dir pages cleared by the generator's prune; recipes refreshed from the moved corpus.

## How tested

Real output from the final committed tree + the deploy-drift simulation (verbatim):

```
$ /tmp/pd-bin generate --out /tmp/regen5 --root /tmp/charly6 --plugins /tmp/market6   (plugin-docs v2026.254.0559)
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 963 recipe pages, 119 plugin pages (162 provider words), 58 cli pages, 611 candy pages, 126 box pages
GEN8_RC=0 (whole-site link + sidebar gates PASS)
```

- **Drift no-op (the deploy drift-gate guarantee):** regeneration on the final committed tree produces a byte-level no-op (post-generation diff vs the committed tree = only the seeded astro config, exactly the deploy flow's seed); diff vs the pre-merge validated tree: EMPTY.
- **reference/cli = 58 top-level words** — compiled corpus words (check, deploy, build, vm, cmd, doctor, ...), release repos (review, pipeline) and extra repos (gh, docs) all emit.
- **reference/providers.md** carries the `docs` word → plugin-docs row (162 provider words total).
- **recipes = 319/319** of the marketplace corpus (963 pages incl. references children; the two family-README card pages match the corpus's 321).
- `git grep 'open infrastructure compiler'` in src/ + astro.config.mjs → 0 hits; full-repo only CHANGELOG history.
- **index.md**: title OpenCharly; description, head title and hero.tagline all carry "The wonky DevOps tool for you and your agents.".
- Removed stale artifact check: the old `recipes/check/openclaw`/`tools/crabbox` entries verified — crabbox is live-emitted (refreshed skill v0.51.0) and openclaw is emitter-confirmed; both are real corpus pages, not orphans.
- Branch `feat/docs-wonky-rebrand` pushed @ `dd28af5c` (verified upstream; clean tree).

## Rulebook compliance

- R3/SDD — the site is a projection of the single catalog assembly; the generator is the config-driven plugin-docs release; no parallel resolution path.
- R1 — pipeline defects surfaced during this landing (go_mod_path default, config node location, a skipped CalVer twin tag, generator self-documentation) were all RCA'd and fixed upstream before this regen; none patched here.
- R5 — the 132 stale versioned dirs removed by regeneration, not hand-deleted; hand edits confined to guides/, concepts/, astro.config.mjs.
- R6/Git — no force-push, no amends; single clean commit; PR-only landing via the org validator.

## Change classification

- **Change class:** docs content regeneration + deploy pins + hand-authored narrative fixes.
- **Verification gate:** the drift simulation (byte no-op), generation rc 0 + gate PASS, and the full emission ledger.
- **Attribution tier:** analysed on a live system — the full generation ran live on the committed tree with retained output; the deploy pipeline (drift gate → Astro build → publish) runs at merge.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*