# 2026.236.1500 — feat: the Open Infrastructure Compiler reaches the site

## Summary

The docs leg of the charly rebrand (charly #394, merged `52798e8c`, tag `v2026.236.1453`):

- **charly submodule pin** advances to that main (`57167066`); the generated half of the
  site regenerates from it, so the landing page's hero, `<title>` and description all
  carry **"The open infrastructure compiler — for you and your agents."** Regeneration
  also prunes `reference/candy/ripgrep.md` (the ripgrep candy moved to
  `opencharly/layer-ripgrep` in charly `894fa0af` #384 — its reference page is no longer
  emitted).
- **`astro.config.mjs`** — the site-wide meta description moves to the new positioning
  (hand-authored; the drift gate never touches it). **`README.md`** follows.
- **The dead `/reference/candy/ripgrep/` links are fixed**: the `ripgrep`-labelled
  teaching links in `concepts/00-vocabulary`, `concepts/02-one-recipe-many-molds`,
  `concepts/03-every-piece-has-a-card` and `guides/authoring-a-candy` now point at the
  living recipe card `/recipes/tools/ripgrep/`; the generic "Candy reference"
  entry-point link points at `/reference/candy/sshd/`; the four stale
  `candy/ripgrep/charly.yml` path attributions now name the candy's live home,
  [opencharly/layer-ripgrep](https://github.com/opencharly/layer-ripgrep) —
  correction: this landing itself fixed only two of the four and miscounted them
  as "two"; the remaining two and this count fix landed in the follow-up
  claim-sweep PR (#99). The deeper re-anchoring of the teaching examples belongs to
  the candy-extraction program (charly #385, in flight).

## How tested

Reproduced the deploy workflow's own sequence locally on the pinned checkout:

```
$ cd charly/charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=docs-ci" -o ../../bin/charly .
$ cd charly && ../bin/charly docs generate --root . --plugins ../marketplace --out ../src/content/docs
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 345 candy pages, 82 box pages (912 generated pages cleared before regeneration)
exit: 0   # the generator's link + sidebar gates run inside this step
```

Post-regeneration assertions:

```
$ grep -m1 tagline src/content/docs/index.md
  tagline: "The open infrastructure compiler — for you and your agents."
$ grep -rlE "factory floor|fully equipped|Agent-in-the-Loop Factory" src/content/docs README.md astro.config.mjs
(no hits)
$ grep -rn "reference/candy/ripgrep" src/content/docs
(no hits)
```

The committed tree is byte-identical to what the pinned charly regenerates, so the
deploy workflow's drift gate re-proves this on CI.

## Rulebook compliance

- R1: the dead-link divergence was RCA'd in charly #394 (the #384 extraction's docs
  leg); this PR is its docs-side remediation.
- R4a: no workaround prose — links point at pages the generator actually emits.
- R5: residue sweep pasted above; no changelog file is authored (the PR body is the
  changelog; tag-on-merge writes `CHANGELOG/<CalVer>.md` at merge).
- R6: fresh clone, feature branch, no force-push.

## Change classification

Documentation: hand-authored site prose/config + a charly pin bump consumed only by the
generator, with the regenerated pages committed alongside. Gate: the full regeneration
ran live with the pinned binary (link/sidebar gates included) and the committed tree
matches it; the deploy workflow's drift gate re-runs the identical sequence on CI.

*Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn