# 2026.244.0747 — docs(install): add the Install with mise path

## Summary

The install page (src/content/docs/start/install.md) gains the **Install with mise** path: `mise use github:opencharly/charly` installs the published release binary (no Go toolchain, no source build), pinning a CalVer, using it via the shim or `mise x -- charly version`, and the runtime-deps note (`charly doctor`). The frontmatter description and intro now cover both install paths (mise + source build).

The GitHub backend (`github:org/repo`) is the modern replacement for the deprecated `ubi:` backend; the full backend spec is what resolves (a bare `charly` shorthand is not in mise's tool registry).

## How tested

- The exact commands were spike-verified against the live release (mise v2026.8.16, charly v2026.244.0836): `mise use github:opencharly/charly` installs the release binary, `charly version` prints the CalVer, `mise x -- charly version` runs, shims resolve.
- Docs-only change; no regeneration needed (hand-authored page).

## Rulebook compliance

- Docs-only change class; no pins/scripts changed.
- PR-only landing via feat/ branch.

## Change classification

- Change class: documentation.
- Verification gate: documentation reviewed.
- Attribution tier: documentation reviewed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*