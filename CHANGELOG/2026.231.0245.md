# 2026.231.0245 — catch up the plugin-check reference page

`reference/candy/plugin-check.md` was stale against landed `charly/main`: `charly#315`
(`c8ba494f`) added a `charly check stop` row to that candy's plan and the projection never
followed. `task docs:drift` reds on it from any superproject tree carrying the landed source.
(`skills:drift` cannot: it reports only `plugins/**`, and `reference/candy/` is the ONE-hop
chain read straight off the candy on disk, never the two-hop skills projection.)

Direction measured before acting, because the opposite case is indistinguishable at the gate:
regenerating this page ADDS the row, so the SOURCE is ahead and regenerating is the fix. Had it
REMOVED content, the projection would have been ahead of its source and regenerating would have
destroyed landed work.

`reference/cli/ollama.md` was in an earlier revision of this branch and has been REMOVED from it.
That page projects `candy/plugin-ollama`, which exists on no landed tree — only on the open,
unmerged `charly#309`. Including it here would have been a projection landing ahead of its
unmerged source, which is a legitimate B6a shape but NOT what this PR claimed to be, and it
belongs sequenced with `charly#309` rather than spliced in here. This PR's validator caught it.

Generated by `charly docs generate`; never hand-edited.

A second page joins for the same reason and by the same test: `recipes/internals/libvirt-renderer.md`,
whose source landed in `plugins#212` (`cb804e7b`) minutes after this branch opened. Regenerating it
ADDS the `~720` cell, so the source is ahead and regenerating is the fix. It is the docs half of the
two-hop chain whose plugins half `#212` already carried.

Deliberately NOT included: `reference/cli/ollama.md`, `reference/box/fedora/ollama.md`,
`reference/box/cachyos/ollama.md` and `reference/box/cachyos/ollama-rocm.md`. Those project
`candy/plugin-ollama` and the two distro `ollama` boxes, none of which exist on ANY landed tree —
they live only on the open `charly#309`, `distro-fedora#18` and `distro-cachyos#15`. They are
sequenced with those PRs, exactly as this PR's validator required for the ollama page it rejected
in round 1.

