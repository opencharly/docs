# 2026.231.0245 — catch up the plugin-check reference page

`reference/candy/plugin-check.md` was stale against landed `charly/main`: `charly#315`
(`c8ba494f`) added a `charly check stop` row to that candy's plan and the projection never
followed. `task skills:drift`/`docs:drift` red on it from any superproject tree carrying the
landed source.

Direction measured before acting, because the opposite case is indistinguishable at the gate:
regenerating this page ADDS the row, so the SOURCE is ahead and regenerating is the fix. Had it
REMOVED content, the projection would have been ahead of its source and regenerating would have
destroyed landed work.

`reference/cli/ollama.md` was in an earlier revision of this branch and has been REMOVED from it.
That page projects `candy/plugin-ollama`, which exists on no landed tree — only on the open,
unmerged `charly#309`. Including it here would have been a projection landing ahead of its
unmerged source, which is a legitimate B6a shape but NOT what this PR claimed to be, and it
belongs sequenced with `charly#309` rather than spliced in here. This PR's validator caught it.

Generated by `charly docs generate`; never hand-edited.
