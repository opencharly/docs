# 2026.231.0245 — catch up two reference pages their sources had moved past

Two generated reference pages were stale against landed `charly/main`:

- `reference/candy/plugin-check.md` — gains the `charly check stop` row that `charly#315`
  (`c8ba494f`) added to the candy's plan.
- `reference/cli/ollama.md` — the CLI page's title and help-line, regenerated.

Direction measured before acting, because the opposite case is indistinguishable at the gate:
regenerating these ADDS content, so the SOURCE is ahead and regenerating is the fix. Had it
REMOVED content, the projection would have been ahead of its source and regenerating would have
destroyed landed work.

Generated by `charly docs generate` (`task docs:sync`); never hand-edited.
