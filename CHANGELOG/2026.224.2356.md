# 2026.224.2356 — correct the agentteams-cli controller endpoint default

Regenerate the generated half of the site after the `agentteams-cli` skill's
controller endpoint default changed from the upstream
`http://127.0.0.1:18090` published-port convention to
`http://127.0.0.1:8090` — the controller's port 8090 as host-mapped by the
agentteams box's default auto-allocation (the endpoint the deployment actually
publishes; the box reproduces neither the upstream 18090 host port nor, on a
machine venue, any host forward at all). Four pages carry the corrected default:
the `recipes/agentteams/agentteams-cli` recipe, the
`reference/candy/plugin-agentteams` and `reference/plugin/plugin-agentteams`
pages (the plugin's `description:`), and the `reference/cli/agentteams` word
page. Placeholder CalVer — the pr-validator finalizes it at merge.
