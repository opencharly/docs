# 2026.229.1246 — the ollama surface, re-projected at the pinned gitlinks

**This is a REGENERATION, not an authored edit.** Every page carries
`DO-NOT-EDIT — generated by `charly docs generate``. The sources of truth are the
candy and box entities in the `charly` repo (one hop) and the skill corpus in the
`plugins/` tree (two hops), projected at superproject `ab7b46d4` with `plugins` at
`96a176f` and all five `box/<distro>` submodules at their pinned gitlinks —
`box/arch` `67cf83db`, `box/cachyos` `942a5f87`, `box/debian` `90c260b2`,
`box/fedora` `bccbd286`, `box/ubuntu` `72cb06ea`.

The PR carries 25 content pages against `main`. All of them are listed below.

## The ollama surface — 12 pages

New:

- `recipes/ollama/ollama-cli.md` — the `charly ollama` command tree.
- `reference/cli/ollama.md` — the `ollama` command word.
- `reference/plugin/plugin-ollama.md`, `reference/candy/plugin-ollama.md` — the
  plugin serving it, its providers and its rendered CUE schema.
- `reference/candy/ollama-cuda.md`, `reference/candy/ollama-rocm.md` — the two
  opt-in GPU backend candies.

Corrected:

- `recipes/ollama/ollama.md` — the box recipe. The composition abstention is now
  binding, the page no longer calls itself a candy, and the two GPU backends are
  named as candies rather than as skills they do not have.
- `recipes/ollama/ollama-layer.md` — the candy recipe.
- `reference/candy/ollama.md` — `OLLAMA_VERSION` documented as pinned-and-must-stay
  pinned, the runtime check list, and the corrected dependency claim: the tarball
  bundles CUDA but not ROCm.
- `reference/candy/charly-ollama.md`, `reference/candy/cuda.md` — version stamps.
- `reference/candy/nvidia.md` — version stamp plus the check step recording that
  the nvidia-container-toolkit repo enforces package signature verification.

The three box pages are deliberately **unchanged** and identical to `main`:
`reference/box/fedora/ollama.md`, `reference/box/cachyos/ollama.md`, and no
`reference/box/cachyos/ollama-rocm.md` is emitted. They project from the pinned box
gitlinks, not from the unlanded `feat/ollama-plugin` branches — the regression the
previous commit fixed and this one preserves.

## The reserved-word index and the pages it links — 4 pages

- `reference/providers.md` — 148 words across 93 plugin candies, 62 compiled in.
  Purely additive against `main`: the new `ollama` command word, and the
  `generate-packages` word that one whole-file projection cannot leave out.
- `reference/candy/generate-packages.md`, `reference/cli/generate-packages.md`,
  `reference/plugin/generate-packages.md` — new. The candy is already on the
  `charly` repo's `main`; these pages are the mirror catching up to it, and
  `candy/plugin-docs`'s whole-site link gate requires them to exist because
  `providers.md` links to them.

## Carried backlog — 9 pages

Forward projections of sources already on `main`, committed by earlier commits on
this branch and reproduced exactly by the current generator. None reverts `main`:

- `recipes/index.md`, `recipes/internals/go/source-map.md`,
  `recipes/internals/skills.md`, `recipes/internals/vm-spec.md`,
  `recipes/vm/vm.md`, `recipes/vm/arch-cloud-vm.md`, `recipes/vm/vms-catalog.md`.
- `reference/candy/container-nesting.md`, `reference/candy/shortbread.md`.

## What this commit corrects, and therefore no longer carries

The previous head reverted five pages to older projections. Regenerating restores
all five to content byte-identical to `main`, so they have **left the diff
entirely** rather than being carried:

- `reference/candy/plugin-kube.md`, `reference/plugin/plugin-kube.md` — `main`'s
  description of the plugin-generated Kustomize tree, naming
  `candy/plugin-kube/materialize.go`, is restored.
- `reference/candy/plugin-marketplace.md`, `reference/plugin/plugin-marketplace.md`,
  `reference/cli/marketplace.md` — `main`'s full account of `charly marketplace
  drift` comparing against disk rather than git is restored.

## What this commit deliberately does not carry

Nine artifacts remain stale, every one of them inherited debt recorded in the
`charly` repo's `scripts/drift-baseline-docs.txt` and reported by name on every
`task docs:drift` run: `recipes/build/generate.md`, `recipes/build/load.md`,
`recipes/distros/nested-podman-socket.md`, `recipes/image/layer.md`,
`recipes/index.md`, `recipes/internals/git-workflow/evidence-and-freshness.md`,
`recipes/internals/go/source-map.md`,
`recipes/internals/install-plan/step-kinds.md`,
`reference/candy/agentteams-snapshot.md`.

The two `plugin-kube` entries that file also records are now paid off by the
correction above, so the baseline shrinks by exactly those two lines.
