# 2026.229.1246 — the ollama surface, and the backlog a wholesale projection carries with it

Generated by `task docs:sync` from the candy sources on opencharly/charly#309. Nothing here is
hand-authored; the prose lives on the candies.

**21 content pages: 6 new, 15 modified.** Counted from `git diff --name-status origin/main...HEAD`.
An earlier version of this entry said 25 and listed pages the diff does not carry — including three
`generate-packages` pages that came from `main` via docs#69, and a "carried backlog" entry for
`recipes/internals/git-workflow/evidence-and-freshness.md` that is not in the diff at all.

## The ollama surface (12)

New, because charly#309 adds their sources: `reference/candy/ollama-cuda.md`,
`reference/candy/ollama-rocm.md`, `reference/candy/plugin-ollama.md`, `reference/cli/ollama.md`,
`reference/plugin/plugin-ollama.md`, `recipes/ollama/ollama-cli.md`.

Modified: `reference/candy/{ollama,charly-ollama,cuda,nvidia}.md` and
`recipes/ollama/{ollama,ollama-layer}.md` — the packaged install path, the pin and why it must stay
a release tag (the download cache is content-addressed by the sha256 of the URL, so `latest` would
hash to one key forever), the binary check described as ungated because the step carries no
`context:`, the removal of a cross-repo composition table this page could not keep true, and the
`charly status` port format.

## `recipes/index.md`

Adds this cutover's own `ollama-cli` card and the card count, and removes nothing `main` has.

It is here because of a mistake worth recording. The catch-up merge to `main` resolved three
conflicts by taking `main`'s side wholesale, which silently dropped this — publishing a recipe
index missing a card whose target page the same PR adds. Nothing breaks (a missing link is not a
dead link), so no gate reported it. B6a says a projection catch-up is resolved by REGENERATING,
never hand-merging; "take one side" is hand-merging with extra steps.

## Carried backlog (8)

`recipes/core/charly-status.md`, `recipes/internals/{skills,vm-spec}.md`,
`recipes/vm/{vm,arch-cloud-vm,vms-catalog}.md`, `reference/candy/{container-nesting,shortbread}.md`.

Other cutovers' projections. A wholesale regeneration offers no way to leave them: *"every pending
page lands with the next commit or none of them do."* None reverts `main`.

## What this deliberately does NOT carry

`reference/providers.md` stays at `main`'s version, so the reserved-word index does not list the
`ollama` command word this release documents. There is no correct alternative available yet:
regenerating it adds that row but removes seven command words `main` has — `build`, `feature`,
`labels`, `load`, `merge`, `pull`, `reconcile` — because those come from `ec484b4b`'s
`candy/plugin-box` `providers:` list, unlanded on opencharly/charly#315. Both sides live in one
whole-file projection and cannot be split.

Taking `main`'s side is the less destructive half: the `ollama` row returns automatically from a
regeneration once #315 lands, while `main`'s seven rows, once merged over, would have to be noticed
by a human. `reference/cli/generate-packages.md` stays at `main`'s version for the same class of
reason — regenerating it restores a fabricated `--help` line `main` deliberately removed.

## The drift baseline

charly#309's `scripts/drift-baseline-docs.txt` carries 63 entries against this pin, up from 8. That
growth is inherited, not this cutover's: docs `main` was projected from the unlanded `ec484b4b`, so
an unmodified charly `main` already fails to reproduce it — 74 dirty paths, measured on a clean
scratch worktree.

The argument for that originally rested on a `comm -23` of the two stale sets coming back empty.
That check is not sufficient: `comm` differences PATHS, not REASONS, so a file stale on `main` for
one reason and stale here for two subtracts to zero and conceals the second. Two entries did
exactly that. `recipes/index.md` is regenerated and removed from the baseline; `providers.md` is the
unsplittable one above.
