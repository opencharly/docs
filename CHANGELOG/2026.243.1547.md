# 2026.243.1547 — docs: add plugin-bpf + plugin-cardwire reference pages

## Summary

Adds the two new external command plugins to the docs corpus + reference tree:

- `.github/docs-corpus/pins.tsv` += `plugin-bpf v2026.243.1059`, `plugin-cardwire v2026.243.1202`.
- Regenerated the content tree with the standalone plugin-docs generator (charly @ a31f76d7, marketplace @ e8efdb1, corpus wiring via `build_wiring.py` — the exact CI recipe from deploy.yml). Diff is confined to: `reference/providers.md` (index) + `reference/cli/{bpf,cardwire}.md` + `reference/candy/.../plugin-bpf|plugin-cardwire|cardwire-install` + `reference/plugin/.../plugin-bpf|plugin-cardwire` (7 new files + 1 modified index).

## How tested

Local regeneration with the CI-equivalent inputs (pinned charly checkout + marketplace submodule + pins wiring):

```
$ /tmp/plugin-docs-bin generate --root /tmp/charly@a31f76d7 --plugins <marketplace@e8efdb1> --out src/content/docs
charly docs: 766 recipe pages, 98 plugin pages (146 provider words), 53 cli pages, 477 candy pages, 99 box pages
```

The generator's page counts are **per-class and non-disjoint** (a command plugin's CLI word page is counted in both the `plugin` and `cli` buckets; a candy's install page in both `candy` and `plugin`), so summing them is not meaningful. The cleared-before-regeneration count in the generator's own output is the previous tree's page total — the **net diff of this regeneration against the committed tree is exactly the 7 new reference files + 1 modified index** above (git status verified).

**Drift no-op verified (R10):** after committing pins + regenerated tree, re-running the exact recipe from the committed state produces **zero diff** on `src/content/docs` — the deploy's drift gate passes at the current charly pin (a31f76d7), no pin bump needed. The generator's link gate also passed (no "every internal link must resolve" output).

The new pages carry real generated content (plugin about-texts, word tables, provider rows verified in the diff).

## Rulebook compliance

- R3: corpus via the canonical pins.tsv + build_wiring.py path (zero bespoke wiring).
- R10: regeneration run from the committed tree state; drift-gate-equivalent check clean (zero diff).
- Pin discipline: plugin pins are merged tags (v2026.243.1059 / v2026.243.1202); charly pin unchanged.

## Change classification

- Change class: docs/content regeneration (2 new corpus entries).
- Verification gate: standalone generation GREEN + no-op drift check, pasted above.
- Attribution tier: `fully tested and validated` — the exact CI generation recipe ran locally with the pinned inputs and produced the committed tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*