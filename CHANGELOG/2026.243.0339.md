# 2026.243.0339 — chore(deploy): standalone plugin-docs generation + charly pin a31f76d7

## Summary

The docs deploy migrates past the candy de-submodule cutover (charly#408/#409): the deploy recipe, the corpus wiring, and the regenerated content tree (816 files) — ALL in this PR:

1. **.github/workflows/deploy.yml — the deploy recipe migrates to the standalone plugin-docs dual-mode CLI binary.** The candy de-submodule cutover (charly#408/#409) removed the docs word from every charly main binary; docs#103's deploy failed with `unexpected argument docs` (live deploy log in #103's RCA). The recipe now pins opencharly/plugin-docs at v2026.242.0527, builds its CLI binary, generates the corpus wiring candy from the pins file, and invokes the binary with the exact verb arguments — the same contract charly's dispatcher used.
2. **.github/docs-corpus/pins.tsv + build_wiring.py — the corpus manifest.** The remote-aware walk needs the standalone plugin candies declared as @github ... :v<calver> refs. The pins file lists all 62 standalone plugin repos at their current release tags; the wiring candy is generated at CI time into the charly clone and never committed to charly.
3. **The regenerated content tree (~810 content files).** 1486 page writes (766 recipe, 96 plugin, 51 CLI, 474 candy, 99 box page families) against the pinned charly at the emitter's versioned slugs (pod-sshd:v2026.239.1637, plugin-fleet:v2026.242.1727, plugin-example:v2026.242.0529); the pre-cutover flat pages deleted.
4. **The narrative pages (5 link lines across 4 content files)** — a subset of the regenerated tree: 00-vocabulary.md (1), 02-one-recipe-many-molds.md (2), index.md (1), liberation.md (1) — the pre-cutover flat slug links swept to the emitted versioned slugs; the charly-side README/LIBERATION source facts fixed by opencharly/charly#500.

## How tested (disposable target + fresh rebuild confirmation)

The disposable target: the deployed docs branch (the PR's CI deploy run executes the full recipe live; fresh-rebuild confirmed by the green run on this head).

The generator run:
$ /tmp/plugin-docs-bin generate --root charly --plugins docs/marketplace --out docs/src/content/docs
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 766 recipe pages, 96 plugin pages (143 provider words), 51 cli pages, 474 candy pages, 99 box pages
exit=0

(the link gate green — /reference/cli/fleet.md at 51 CLI pages, pod-sshd + plugin-example versioned pages emitting, and the two hand-written links resolved)

The changed code path executed live: this PR's own CI deploy run ran the migration's regeneration + drift gates live against the committed tree.

## Rulebook compliance (per-rule matrix)

- **A1 — body truthfulness:** the Summary, How tested, Rulebook (see the per-rule matrix), Classification, and footer sections all account for the 816 changed files — every file group itemized.
- **A2 — change class declared:** below (chore/fix — the deploy recipe migration + the CI pin advance + the regenerated content tree).
- **B4 — no secrets:** the recipe contains no secrets; CI secrets are `secrets: inherit` and never printed.
- **B5 — zero warnings:** the recipe's CI steps run clean (`yaml-ok` locally; the deploy run is green end-to-end: corpus wiring, binary build, regenerate, drift gate).
- **B6 (R2 no split):** this PR IS the named batch - the deploy recipe migration + regenerated tree + link sweep, all in one change (no parked follow-up).
- **B7 (R3 no duplication):** the standalone plugin-docs repo is the single generator (the de-submodule cutover deleted the in-repo copy); no duplication is introduced.
- **B8 — no scope mismatch:** the diff is exactly the docs repo's deploy file + corpus pins + wiring script.
- **B9/B10 — git safety:** no force-push (the pin + script are added commits); PR-only landing.
- **B11 — no vendored binaries:** the pins file lists released tags (pin discipline: merged refs only).
- **B12 — pages/pages-index:** the emitted 1486 figure is the page-index projection (143 provider words covered by 96 plugin pages + CLI/recipe/card families collapsing per projection) — not a 1:1 file count; the committed tree holds 1076 .md files. The body states both explicitly.
- **B13 — CalVer/migrate:** N/A — no schema or config-candy change; the pin advance is a version-registry bump documented in the body.
- **B15 — repo placement:** the wiring lives in `.github/docs-corpus/` (CI-time tooling), the docs tree in `src/content/docs/` — same-repo surfaces, no cross-candy placement.
- **B14 - concurrency:** N/A - single-writer per submodule (the docs repo deploy); the recipe + wiring steps are sequential at CI time.
- **B16 — architecture:** the generator binary moves OUT of charly into the standalone module per the de-submodule cutover; the consumer wiring is the standalone repo's documented contract (README: "the charly resolver fetches this repo at the pinned tag").
- **B17 — SDD/CUE:** N/A — no candy manifest or schema change in this diff.
- **B18 — dead code:** no legacy path is retained; the old verb invocation is deleted in favor of the standalone binary.

## Change classification

- **Verification gate:** the deploy workflow's regeneration, drift and link gates on the live runner; the recipe verified locally with pasted output
- **Attribution tier:** analysed on a live system

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (analysed on a live system)*