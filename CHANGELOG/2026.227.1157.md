# 2026.227.1157 — regenerate the check bed/R10 page for the build-scope verdict resolver

Mirrors the superproject's `candy/charly-check/charly.yml` `skill:` source change
landing with the check-verb-correctness cutover. Generated by `charly docs
generate` (`task docs:sync`), never hand-edited:

- `src/content/docs/recipes/check/check/beds-and-r10.md` — a new subsection,
  "Which artifact a build-scope verb evaluates".

It documents behaviour that changed in that cutover: `charly check box`, `charly
box feature run`, and `charly box labels` refuse a bare short name whose election
against local container storage is not the newest local BUILD of that box, rather
than silently certifying the older artifact; a full ref or a `<box>:<calver>` pin
names one artifact and is used verbatim. Full defect narrative + the measured
reproduction: `opencharly/charly` `CHANGELOG/<this CalVer>.md`.

Scoped deliberately to this one page. A full `task docs:sync` on this tree also
reproduces mirror drift owned by other in-flight cutovers (`recipes/index.md`,
`recipes/internals/git-workflow/multi-repo-coordination.md`,
`recipes/internals/go.md`, and a missing `recipes/kubernetes/helm.md`); that
content belongs to its own owners' landings and is reverted here rather than
duplicated (R2 — separable, already in flight, not this cutover's claim).

Placeholder CalVer — the pr-validator finalizes it at merge.
