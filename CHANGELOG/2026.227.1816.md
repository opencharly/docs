# 2026.227.1816 — evidence page: guard the merge-tree probe, scope the execution rule to content

Regenerated by `charly docs generate`, whose input for recipe pages is the **`plugins/` corpus** — `collectSkills` (`candy/plugin-docs/gen_skills.go:78`) reads `<root>/plugins/<plugin>/skills`, not the candy `skill:` entities. The chain is two projections, not one: `charly marketplace generate` emits candy source into `plugins/`, and `charly docs generate` emits `plugins/` into this site. So this page is a byte-exact function of `opencharly/plugins#175`'s head, and the ultimate authoring change lives in `opencharly/charly`.

An earlier version of this entry said `charly docs generate` reads "the superproject's candy `skill:` entities". That is false, and it re-asserted a misconception `docs#57` had already root-caused one merge earlier. The disproof is direct: a scratch tree whose candy source contained zero occurrences of the new text still reproduced this page byte-for-byte — a file absent from the input cannot be the source. The same evidence was in front of me and I misread it: when I regenerated this site, eight unrelated pages moved, and they moved because they had moved in `plugins/`.

One page changes: **`recipes/internals/git-workflow/evidence-and-freshness.md`**.

The published recipe fed `git merge-tree --write-tree` straight into a command substitution. `merge-tree` exits 1 on conflict and prints the tree oid followed by the conflict stages, so the substitution captures multiple lines and `git commit-tree` rejects it — and piping through `head -1` is worse than the bug, yielding a checkoutable tree whose files contain `<<<<<<<` markers for the probe to run a gate against. Both call sites now guard on the exit status. `head -1` survives in two places, and the page scopes it by INTENT rather than by location: it is right when you want the conflicted tree as DATA to diff or inspect — the evidence block and the diff-inspection recipe both do — and wrong the moment you will RUN anything inside the resulting tree.

An earlier version of this sentence said "the one remaining `head -1` is in the diff-inspection recipe", which was wrong twice over: there are two uses, and the diff-inspection one no longer operates on a `merge-tree` call at all — it takes the first line of `$RESOLVED`, which is what removed the sixth `merge-tree` site. It is the same defect the page's own pattern table catalogues, one repository over: a correct refactor left the prose describing it behind. The table stays at four rows because its stated scope is `plugins#175`'s review rounds; this instance belongs to `docs#58` and is recorded here instead.

The page's own frame was the root cause: it promised execution only for its mechanism sections, which exempted this recipe for sitting in an epistemic one. The rule is now scoped to content — every runnable command and every mechanism claim is executed before publication, wherever it sits — and the `merge-tree` behaviours are pasted as terminal evidence.

A symptom-indexed entry table now opens the page, so a reader arriving with a problem reaches routing before epistemology.

Generation walked 6 repo roots and emitted 327 recipe, 91 plugin (147 provider words), 49 cli, 338 candy and 81 box pages, clearing 891 first.

**The tree that figure was taken on**, because a page count without one is the defect this cutover's own page forbids: `plugins` at `853e72a9` (the merged `#177`, tag `v2026.227.1757`), `docs` at this branch, superproject carrying the authoring edit. On that tree, whole-site regeneration moves **zero** files — this page already is the projection, and the site is complete at that producer. Measured by regenerating all 891 pages, not inferred from the diff.

An earlier version of this paragraph said the regeneration moved only this page, and named three others as deliberately excluded. Both clauses were true when written and neither is true now: at `plugins@ab1f93d9` regeneration moved **two** pages (this one and `beds-and-r10.md`), and the three named — `branch-and-pr-loop.md`, `multi-repo-coordination.md`, `tools/docs-site.md` — landed via `docs#59` and are byte-identical to `docs` main at this head. Left standing, the entry would have told a reader permanently that the site is short three pages this PR chose to omit, when what this PR does is complete the projection.

That is this cutover's own subject arriving in its own release note for the second time, and it is why the paragraph now pins a **specific producer sha** rather than a moving reference: `"its current tip"` names no commit, in the sentence whose entire job is naming one.

Documentation-only change class at `documentation reviewed`.
