# 2026.229.0000 — a rename inverted a true retraction

Round 7 of the emitter doc-drift cutover, projected from `candy/charly-build/charly.yml` and
`candy/charly-internals/charly.yml` (source: `opencharly/charly#297`).

**This entry exists because the previous one was a stale placeholder.** `docs#73` merged at 21:43 and
its squash-merge renamed the placeholder to `CHANGELOG/2026.229.2141.md`. The copy on this branch
survived that rename, byte-identical in body to the one already published — so a validator would have
renamed it again at merge and republished `#73`'s release note as if it were arriving now, while
round 7 shipped with no entry at all. Caught by diffing the file against `main` rather than reading it.

## A rename inverted a true retraction

An earlier round wrote a **correct** negation — "there is no `allStepKinds`", true because the
lowercase form exists in no module. The lowercase→uppercase sweep then hit that token **inside the
retraction** and capitalised it:

> the kind list is `AllStepKinds` (`sdk/deploykit/steps.go:104`) — there is no `AllStepKinds`.

which asserts and denies the same symbol in one sentence. `AllStepKinds` exists
(`sdk/deploykit/steps.go:104` → `spec/install_step_vocab.go:902`).

**A mechanical rename does not merely fail to fix a false sentence — it can invert a true one.** It is
also invisible to every existence check: the symbol resolves, so the sentence passes a grep in *both*
directions. The rule: **a negation is the one construction where a rename must never be applied
blind.**

## The kubernetes cross-reference pointed at its own refutation

"generating a Kustomize tree host-side" — while the section it cites, and the code, say
`materializeKustomize` does it PLUGIN-side (`candy/plugin-kube/materialize.go`, reaching
`candy/plugin-k8sgen` over `verb:k8sgen`/`OpEmit`). Four instances; the first sweep found one because
it keyed on "generat**es**" while three said "generat**ing**", so the anchor became the claim rather
than the verb.

## And the same paragraph still contradicted itself

`source-map.md` asserted that the deploy paths "(pod/vm + external [local/kubernetes/android]) route
through a shared IR" and, two clauses later, that "the kubernetes substrate is EXTERNAL and does NOT
consume the IR". Round 7 fixed the second half and never looked at the first. The enumeration is now
narrowed to the substrates that actually consume the IR.

## `host-generated Kustomize tree` retired from five sites

The term is gone from `candy/plugin-kube` — the `description:` (which lands **twice** per generated
page, frontmatter *and* body), `deploy.go`, `plugin.go`, and `provider.go` ×2. **Recorded here
because after this lands `git grep host-generated` returns zero across all three repos, and R5's
grep self-test relies on `CHANGELOG/` to hold the reason a term vanished.**

**It was not simply wrong, which is why an earlier sweep did not read it as false.** `host` does two
jobs in this codebase:

- In `deploy.go` the comment contrasts host with **the cluster** — "the host generated +
  egress-validated the tree", then applies it to the live cluster. **True.**
- In the reference pages it contrasted host-side with **PLUGIN-side**. **False**, since
  `materializeKustomize` lives in `candy/plugin-kube/materialize.go`.
- `plugin.go` uses **both senses in one comment**: line 4's "host-generated" against line 7's "the
  host go-builds this binary and serves it OUT-OF-PROCESS".

So the claim was **ambiguous at three sites and false at one** — the `charly.yml` description, whose
context on the reference pages is architectural placement. A blanket rewrite to "plugin-side" would
have destroyed a true host-vs-cluster contrast in `deploy.go`.

All five now name the mechanism — **plugin-generated, on the host** — which holds under either
reading: the plugin binary is host-built and served out-of-process on the host machine, so the two
halves are not in tension.
