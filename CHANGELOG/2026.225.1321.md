# 2026.225.1321 — worktrees are the one and only model (remove the shared-tree alternative)

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edits (cutover `worktree-only-model`) via `charly docs generate`. The worktree-per-session model is now the ONE AND ONLY model: every session — standalone or teammate — works in its OWN worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`. The shared-tree alternative is deleted, not contrasted. Four recipe pages changed, mirroring the `plugins` skill pages (the `branch-and-pr-loop.md` strict-count fix landed in the preceding cutover and is a regeneration no-op here):

- `recipes/internals/git-workflow/multi-repo-coordination.md` — **B3 is rewritten from "agent teams on ONE shared tree (no worktree)" to "agent teams in per-teammate worktrees"**: each teammate works in its own worktree with its own `bin/charly` and freshness-guard scope; the shared freeze barrier is replaced by the within-worktree self-freeze; the "reserve a worktree only for same-file concurrency" exception is deleted.
- `recipes/internals/git-workflow.md` — Reference Index B3 row updated to the new title.
- `recipes/internals/agents/parallel-bed-testing.md` — the "Compile-coherence" bullet and the "Multi-worktree concurrency corollary" drop the shared-tree barrier model: compile-coherence is solved by per-worktree binaries, and the only freeze is the within-worktree self-freeze; the "shared-tree walks" ceiling is renamed "in-tree build artifacts" and item 4d is reframed per-worktree.
- `recipes/internals/agents.md` — the parallel-bed-testing index row updated to match.

This is the `docs` leg of a cross-repo docs cutover (plugins → docs → superproject). Regeneration is a no-op on a clean tree and the whole-site link gate passes. Placeholder CalVer — the pr-validator finalizes it at merge.
