# 2026.256.1329 — docs: regenerate for the eval-omarchy lane skills (marketplace pin + corpus)

## Summary

Regenerates the generated content tree for the eval-omarchy lane-skills landing, and advances the `marketplace` submodule to the corpus that publishes them.

- `marketplace` submodule `bd58a38e` → `7b196af` — the marketplace corpus that now publishes the eval-omarchy-owned `omarchy-eval*` skills and drops the layer-charly-distros duplicates (opencharly/marketplace#298).
- Regenerated at the docs-pinned charly (`d507ca5a`): the eight topic recipe pages (`omarchy-eval-{cold-reader,full-loop,media,oracle,sequencing,tests,tiers,work-lane}`) are newly published; `omarchy-eval` and `omarchy-eval-golden` are rewritten from the new entities.
- The plugin-pipeline reference pages move `v2026-254-1528` → `v2026-256-1226`, and `reference/cli/pipeline.md` + `reference/providers.md` update: the marketplace refs closure now includes eval-omarchy, which pins the newer plugin-pipeline (`v2026.256.1226`).

## How tested

Change class: generated docs content (the repo's deploy drift gate is the owner).

**1. Regeneration ran against the pinned charly + the committed marketplace pin:**

```
$ bash .github/docs-corpus/build_wiring.py .github/docs-corpus/pins.tsv <charly>/candy/docs-corpus/charly.yml
corpus wiring refs: 66
$ cd <charly> && plugin-docs-bin generate --root <charly> --plugins <docs>/marketplace --out src/content/docs
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 976 recipe pages, 175 plugin pages (166 provider words), 60 cli pages, 669 candy pages, 127 box pages (1384 generated pages cleared before regeneration)
```

`GEN_RC=0`. The regeneration cleared 1384 generated pages and only these changed (no unrelated family drift): the `omarchy-eval*` recipes, `recipes/index.md`, and the plugin-pipeline reference/providers/cli pages driven by the refs closure.

**2. Every changed path is accounted for** (the eight new pages + the two rewritten + index + the four reference updates + the submodule pointer). `git status` after generation listed no other path.

**Head diff (verbatim):**

```
$ git diff HEAD~1 --stat
 marketplace                                        |   2 +-
 .../recipes/distros/omarchy-eval-cold-reader.md    |  28 ++++
 .../docs/recipes/distros/omarchy-eval-full-loop.md |  27 ++++
 .../docs/recipes/distros/omarchy-eval-golden.md    |  82 +++--------
 .../docs/recipes/distros/omarchy-eval-media.md     |  20 +++
 .../docs/recipes/distros/omarchy-eval-oracle.md    |  36 +++++
 .../recipes/distros/omarchy-eval-sequencing.md     |  16 +++
 .../docs/recipes/distros/omarchy-eval-tests.md     | 101 +++++++++++++
 .../docs/recipes/distros/omarchy-eval-tiers.md     |  25 ++++
 .../docs/recipes/distros/omarchy-eval-work-lane.md |  14 ++
 src/content/docs/recipes/distros/omarchy-eval.md   | 158 ++++-----------------
 src/content/docs/recipes/index.md                  |  10 +-
 .../plugin-pipeline.md                             |   4 +-
 src/content/docs/reference/cli/pipeline.md         |   2 +-
 .../plugin-pipeline.md                             |  13 +-
 src/content/docs/reference/providers.md            |   6 +-
 16 files changed, 340 insertions(+), 204 deletions(-)
```

The two plugin-pipeline files render as renames (`reference/{candy,plugin}/…/plugin-pipeline-v2026-254-1528/…` → `…/plugin-pipeline-v2026-256-1226/…`); `--stat` shows only the destination path with its content delta.

## Rulebook compliance

- **R1** — zero warnings; the regeneration is deterministic and the only changed paths are the ones the refs/corpus change implies.
- **R2** — the producers (eval-omarchy, layer-charly-distros, marketplace) all landed and tagged before this consumer regenerates.
- **R3** — the recipe pages project the single-owner eval-omarchy entities.
- **R4** — the generator is the canonical tool; no hand-edited generated page.
- **R5** — the regeneration prunes the old plugin-pipeline version dirs (deleted) rather than leaving orphans.
- **R6** — PR-only landing; no force-push.
- **R7** — the deploy drift gate is the owner; the tree was regenerated by the exact pinned command and carries its diff.
- **R8** — N/A — no OCI labels / Containerfile.
- **R9** — N/A — the generator was built from the pinned plugin-docs `v2026.254.1214`; no charly binary change.
- **R10** — the generated-content class gate is the regeneration + its drift gate (no bed composes prose).

## Change classification

- **Change class:** generated docs content (+ the marketplace pin).
- **Verification gate:** `plugin-docs-bin generate` at the pinned charly + marketplace, producing exactly the committed diff.
- **Attribution tier:** fully tested and validated.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*