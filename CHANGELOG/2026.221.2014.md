# 2026.221.2014 — Align the service recipe with the corrected removal semantics

Regenerate the service recipe so its `charly config remove` rows match the corrected teardown semantics: `charly config remove` disables the quadlet service only (the quadlet file remains), completing the sweep the 2026.221.1929 entry began (that sweep regenerated the config/deploy/remove recipes but missed the sibling service recipe). (placeholder — the pr-validator finalizes the CalVer at merge)
