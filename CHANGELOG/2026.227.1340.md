# 2026.227.1340 — R4a sweep: regenerated site for the task-target residue cutover

Regenerated by `charly docs generate` from `opencharly/charly`, where the authoring change lives.

**Eight recipe cards** carry the R4a sweep — the fix that keeps every command a reader is told to run working with nothing but the `charly` binary installed:

- `recipes/build/build.md` — the Install-page duplicate ("Host Bootstrap") is replaced by "Host Prerequisites": the native-package install path, a pointer to the Install page for building from a source tree, and `charly box build` as the one command. Three troubleshooting entries drop `task`-based rebuild recipes for `charly version` / `which charly`.
- `recipes/build/migrate.md`, `recipes/tools/charly.md`, `recipes/tools/docs-site.md` — sections that are genuinely this project's own maintenance now say so before their first command, instead of reading as steps for the reader.
- `recipes/tools/cue.md`, `recipes/internals/egress.md` — `task cue:vendor` removed; the target does not exist, and the real pipeline is the per-file `cue import` sequence the egress page lists.
- `recipes/internals/go.md`, `recipes/internals/agents/parallel-bed-testing.md` — `task install-portable` corrected to `build:install-portable`.

Nothing else moves. An earlier draft of this entry also claimed 37 `reference/box/fedora/` pages as repaired pin drift; that claim was wrong and is recorded here only because it was nearly shipped. The pages were stale relative to the **superproject's `docs` gitlink** (`b3c08a46`), which is what a submodule branch checks out — but `93085fb` had already carried the same box/fedora projection onto this repo's `main`. Rebasing onto `main` dropped all 37 as no-ops. The lesson generalizes: a docs branch must be based on **this repo's `main`**, not on the superproject's gitlink, whenever the two have diverged — otherwise a wholesale regeneration re-applies work that already landed and reverts anything landed alongside it.

Documentation-only change class at `documentation reviewed`; regeneration is idempotent. Placeholder CalVer — the pr-validator finalizes it at merge.
