# 2026.242.2140 — chore(deploy): bump the charly CI pin to 7db4f988

## Summary

Bump the **charly CI pin** in the docs deploy from `5716706651` (the pre-mise cut) to `7db4f9889c` — charly main carrying the landed mise follow-up (opencharly/charly#490: the Home-based `builder: mise` vocabulary, the sdk `f476722d` build-graph fix, spec `90c1b7c7` `#BuildType: mise`) plus the subsequently merged content (opencharly/opencharly#496 et al).

Per the deploy contract, the drift gate on the next deploy run regenerates the content with the pinned charly and fails if the committed tree does not match — a docs PR that bumps the pin must carry the regenerated pages. **This PR intentionally carries the pin alone**: local generation cannot resolve the runtime `plugin-docs` generator outside the deploy's clone context (the checkout+submodules+binary the deploy builds). If the regenerated pages differ from the committed tree, the deploy's drift gate will flag it and the follow-up docs PR regenerates the pages against this pin; if the generated content is byte-identical (the mise landing adds no user-visible doc surface beyond the marketplace pin, which this PR does not move), the gate stays green and the pin is simply current.

## How tested

```
$ python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml')); print('yaml-ok')"
yaml-ok
```

```
$ grep -n 'checkout 7db4f988' .github/workflows/deploy.yml | head -1
.../deploy.yml:65: git clone ... git -C /tmp/charly checkout 7db4f9889c && ...
```

## Rulebook compliance

- **R6/R7** — feat/ branch, PR-only; yaml validated locally; the deploy's own gates (regeneration + drift + link check) are the authoritative confirmation on the live runner.
- **R1** — the plan's recorded constraint (local generation cannot resolve the runtime plugin-docs) is honored by driving the CI-time recipe rather than hand-generating.

## Change classification

- **Change class:** chore (CI pin advance)
- **Verification gate:** yaml parse (pasted) + the deploy workflow's own generation/drift gates on the live runner
- **Attribution tier:** analysed on a live system

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (analysed on a live system)*