# 2026.225.1330 — align the second port-allocation occurrence with the auto-allocation doctrine

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edit (the `worktree-only-model` cutover's follow-up) via `charly docs generate`. The `parallel-bed-testing.md` page still told the author to assign disjoint host ports by hand while the same page's auto-allocation section (and the B3 doctrine in `multi-repo-coordination.md`) mandates port auto-allocation. One recipe page changed:

- `recipes/internals/agents/parallel-bed-testing.md` — the second port-allocation occurrence now states the doctrine: host-port disjointness is not statically guaranteed, so every bed uses port auto-allocation — never a hardcoded host port; manual port-picking is forbidden.

This is the `docs` leg of the `worktree-only-model` cutover's port-contradiction follow-up (plugins → docs → superproject). Regeneration is a no-op on a clean tree and the whole-site link gate passes. Placeholder CalVer — the pr-validator finalizes it at merge.
