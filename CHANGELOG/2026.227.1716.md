# 2026.227.1716 — docs(check): regenerate the bed/R10 page for the build-scope verdict resolver

> ## ⚠️ CORRECTION — regenerated again after the code fix was reworked
>
> A validator blocked the first version of the resolver as **blind to bed tags**
> (`charly box build --tag` REPLACES the CalVer tag, so a tag-keyed ordering ties
> every bed build and elects the OLDEST — proven live, a ~17-hour-old image
> certified green). The prose this page projects was therefore describing
> behaviour the code did not have, which makes it an R1 claim-keyed sweep of my
> own narrative, not a cosmetic touch-up.
>
> This projection now states what ships: **"newest build" means CREATION TIME**,
> many tags on one image id are ONE artifact, an ordering that cannot be
> established REFUSES rather than passes, and the resolved ref prints BEFORE the
> verb can bail (including on the no-plan path, where it printed nothing at all
> and exited 0 — reported twice independently).
>
> `main` was merged in (never rebased) so this branch also carries #172 / #54;
> the drift gate is green on the merged tree: `marketplace drift: clean (399
> artifacts)`, `skills: no drift`.
>
> Everything below was written for the first version.
## Summary of changes

Site mirror of the superproject's `candy/charly-check/charly.yml` `skill:` source change that lands with the check-verb-correctness cutover. Generated by `charly docs generate` (`task docs:sync`), never hand-edited.

One file, 27 insertions, zero deletions:

- `src/content/docs/recipes/check/check/beds-and-r10.md` — a new subsection, **"Which artifact a build-scope verb evaluates"**, placed directly after the "Three primary modes" block it qualifies.

It documents behaviour that CHANGED in that cutover: `charly check box`, `charly box feature run`, and `charly box labels` all resolve their image argument through `spec/container.ResolveBuiltImageRef`, which REFUSES a bare short name whose election against local container storage is not the newest local BUILD of that box, rather than silently certifying the older artifact. A full ref or a `<box>:<calver>` pin names one artifact and is used verbatim. The subsection also states why the R10 bed sequence never reaches the guard (it pins `<image>:<run-tag>`).

**On the narrow diff — this is scoping, not incompleteness.** A full `task docs:sync` on this tree also reproduces mirror drift owned by OTHER in-flight cutovers: `recipes/index.md`, `recipes/internals/git-workflow/multi-repo-coordination.md`, `recipes/internals/go.md`, and a missing `recipes/kubernetes/helm.md`. That content belongs to its own owners' landings — `opencharly/docs#54` covers part of it — so it was reverted here rather than duplicated into a competing PR (R2: separable, already in flight, not this cutover's claim; B2 multi-repo ordering). Every one of those paths is disjoint from the file this PR touches, so nothing is masked and nothing is left half-projected.

## Change class

**cross-repo** — `docs-only` within this repo (a generated `*.md` projection).

Landing order (producer → consumer): **`spec` → `sdk` → `plugins` → `docs` (this PR) → `charly`**.

- `opencharly/spec` `#29` — the resolver + the `ErrStaleLocalImage` sentinel.
- `opencharly/sdk` `#136` — the `ai.opencharly.box` leaf-name label emitter.
- `opencharly/plugins` `#173` — the regenerated skill (the same prose, projected to the marketplace tree).
- `opencharly/docs` — this PR.
- `opencharly/charly` — the three call sites, the `skill:` SOURCE this PR mirrors, and the gitlink bumps. This PR must land BEFORE the superproject's, since the superproject records this commit as its `docs` gitlink.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** the documentation-only gate — the non-runtime standards, and no bed. Beds cannot fail on prose. For a GENERATED tree the operative standard is that the committed page is the generator's output for the current source, and that the delta is exactly the intended one.
- **Eval beds run:** `N/A (docs-only)` — this repo's diff is one generated Markdown file. The runtime behaviour it describes is gated in the superproject PR by `charly check run check-marketplace` (`pod | ok | 13 steps | 370s`, run calver `.check/check-marketplace/2026.227.1148/`, binary `charly version` = `2026.227.1116`).
- **Fresh rebuild:** `N/A (docs-only)` for the runtime sense. R9 in the sense that applies: the generator was the freshly built binary — `task build:binary` → `charly version` = `2026.227.1116` — so the projection came from THIS cutover's source, not a stale installed charly.
- **Did the CHANGED code path execute live?** `N/A (docs-only)` — no code path in this diff. The generator ran live and its output is the diff.
- **Concurrency:** `N/A` — generated Markdown, no shared state.

```
### Regeneration — the generator ran from the freshly built binary
$ ./bin/charly version
2026.227.1116
$ task docs:drift
 M src/content/docs/recipes/check/check/beds-and-r10.md      <- this cutover
 M src/content/docs/recipes/index.md                          <- other owners
 M src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md
 M src/content/docs/recipes/internals/go.md
?? src/content/docs/recipes/kubernetes/helm.md
docs: DRIFT — the committed site does not match its sources.

### Scoped to this cutover's page; the other four reverted, NOT committed
$ git checkout -- src/content/docs/recipes/index.md \
      src/content/docs/recipes/internals/git-workflow/multi-repo-coordination.md \
      src/content/docs/recipes/internals/go.md
$ rm -f src/content/docs/recipes/kubernetes/helm.md
$ git status --short
 M src/content/docs/recipes/check/check/beds-and-r10.md

### The delta is EXACTLY the intended one
$ git diff --stat
 .../docs/recipes/check/check/beds-and-r10.md       | 27 ++++++++++++++++++++++
 1 file changed, 27 insertions(+), 0 deletions(-)

### The rendered subsection (head of the diff)
+### Which artifact a build-scope verb evaluates
+
+`charly check box`, `charly box feature run`, and `charly box labels` all
+pronounce a verdict on a BUILT ARTIFACT, so they all resolve their image
+argument the same guarded way (`spec/container.ResolveBuiltImageRef`):
+
+- a **full ref** (`ghcr.io/opencharly/web:2026.227.0836`) or a
+  **`<box>:<calver>`** pin names one artifact — it is used verbatim;
+- a **bare short name** (`web`) is elected against local container storage
+  by the CalVer ordering (the content-derived `ai.opencharly.version` label
+  first, the per-build tag as the tiebreak), and the verb **REFUSES** when
+  that election is not the newest local BUILD of that box, naming both refs
+  and the pinned re-invocation.
```

## Attribution tier

**`documentation reviewed`.**

The whole diff is one generated `*.md` file — the documentation-only change class, where this tier is correct and the runtime tiers are unavailable. Every non-runtime standard passed: the projection is the freshly built binary's output, the delta was diffed rather than eyeballed, and the four out-of-scope drifted paths were explicitly reverted and verified absent from `git status` before committing.

Trailer: `Assisted-by: Claude Code Anthropic Claude Opus 5 (documentation reviewed)`.

## Harness rulebook compliance — state HOW each is satisfied

- **R0 skills:** `/charly-build:docs` (the `charly docs generate` pipeline + `task docs:sync`/`docs:drift`), `/charly-internals:skills` (never hand-edit a generated page — fix the source), `/charly-check:check` (the page's subject matter had to be correct, not just well-formed), `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the generator emitted no warnings. The four extra drifted paths were investigated rather than assumed: they are pre-existing mirror lag from other cutovers (the docs submodule was freshly checked out and clean at worktree creation, so every one of them came from THIS sync run, i.e. committed-site-vs-current-source lag), disjoint from this page. Not swept in, not silently committed.
- **R2 no out-of-scope:** the four foreign drifted paths were reverted rather than duplicated into a competing PR — `opencharly/docs#54` owns part of that scope and is in flight. That is the separable classification, applied with the owner named, not a deferral. The three code-level separable defects found during the parent cutover are RCA'd, named in the superproject `CHANGELOG/<CalVer>.md`, and each has an assigned owner.
- **R3 no duplication:** the prose lives in ONE place — the candy `skill:` source — and is projected to `plugins` and here. No hand-written second copy.
- **R4 no workaround:** the page was produced by running the generator; the scoping was done with `git checkout --` on foreign paths, not by hand-editing the generated output to a desired shape.
- **R5 hard cutover + grep:** nothing removed or renamed (27 insertions, 0 deletions), so no deleted identifier to sweep. The new prose introduces no claim that contradicts a sibling page: `ResolveBuiltImageRef` is the name the shipping code uses, and the pinning rationale matches the bed sequence's actual behaviour.
- **R6 git safety:** `git status` checked before every action, including before the `git checkout --` revert of the four foreign paths (confirming they were unstaged generator output, not pre-existing work). This repo went from detached HEAD to a proper `feat/check-box-target-image` branch before pushing, tree verified `0 dirty`. No force-push.
- **R7 runtime gate:** `N/A — docs-only`; beds cannot fail on prose. The behaviour described is bed-gated in the superproject PR.
- **R8 artifact invariants:** `N/A — no image-generation change.`
- **R9 binary == source + deps:** the generator was the freshly built `bin/charly`, `charly version` = `2026.227.1116`. No runtime OS dependency involved.
- **R10 disposable + coverage:** `N/A — docs-only`, no bed. The coverage that fails without the change lives in `spec#29` and `sdk#136`, pasted there failing on the pre-fix bodies.
- **RDD / ADE / SDD:** RDD — the prose asserts behaviour proven live before it was written (the refusal reproduced on real storage, pasted in `spec#29`). ADE — `N/A` (no candy here). SDD — `N/A` (no `.cue`).
- **Hard cutover:** ONE atomic commit in this repo, landing in the same batch as its source half. No phase 2.
- **Kernel/plugin boundary law:** `N/A — generated documentation.`
- **Disposable-only autonomy:** `N/A` — nothing destroyed or rebuilt for this diff.
- **Clean architecture + Go gates:** `N/A — no Go in this diff.`
- **CHANGELOG:** `CHANGELOG/2026.227.1157.md` staged — placeholder CalVer, the pr-validator finalizes it at merge.

---

*Assisted-by: Claude Code Anthropic Claude Opus 5 (documentation reviewed)*