# 2026.254.1231 — fix(docs): slug-safe reference routes (plugin-docs v2026.254.1214) — mangles fixed, 851 urls corrected

## Summary

Regenerates the site with the **slug-safe path scheme** (plugin-docs `v2026.254.1214`, the Astro-safe path-segment release), fixing the published 404/mangling defect where every namespaced/versioned page served only under slug-mangled URLs (`githubcom/opencharly/…`) and the natural links 404'd.

- `deploy.yml`: plugin-docs clone pin `v2026.254.0559` → `v2026.254.1214`; `actions/setup-go@v5` → `@v7` (v5 declares node20 (deprecated), v7 declares node24; no input changes). charly pin `d507ca5a` + marketplace mechanism unchanged.
- **Authorized surface fixes** (hand-authored ONLY): the two concept pages' old-form plugin-example-command/pod-sshd link targets → the sanitized routes.
- **Regenerated reference**: 851 mangled-path pages replaced by 813 sanitized-route pages; all DO-NOT-EDIT pages regenerated, none hand-edited.

## How tested

Real output from the final committed tree (branch feat/slug-safe-docs @ be33feb…) + the drift simulation:

```
$ /tmp/pd-tagbin generate --out /tmp/rfs --root /tmp/charly6 --plugins /tmp/market6   (plugin-docs v2026.254.1214)
charly docs: 968 recipe pages, 175 plugin pages (166 provider words), 60 cli pages, 669 candy pages, 127 box pages
RC=0 (5.4 min) — whole-site link + sidebar gates PASS
$ (drift) identical second run → diff -rq vs the committed tree: EMPTY — byte-level no-op
```

- **Link audit:** 0 old-form markdown link targets anywhere in emitted bodies; the 1119 `repo@tag` strings are all `| **Repo** |` metadata code-spans (prose, not links — reported separately).
- **Sanitized routers verified present:** `github-com-opencharly-pod-sshd-v2026-239-1637/sshd.md`, plugin-gh/pipeline/review pages, `cli/docs.md`.
- Brand intact: index.md carries the wonky tagline; recipes 319/319; drift = regeneration on the committed tree is a byte no-op (the deploy gate's guarantee).

## Rulebook compliance

- R1 — live 404/slug-mangling RCA'd from the published sitemap + HTTP probes and fixed at the generator release, not patched at the edge.
- R3 — one sanitizer in the generator; this tree is its projection. R5 — the mangled-page class is superseded by the regen (851 → 813).
- R6 — no force-push/amend; single clean commit; hand edits confined to the two concept pages + deploy.yml.
- R7 — the deploy drift gate re-proves the byte no-op at CI before publishing.

## Change classification

- **Change class:** docs content regeneration + deploy pins + action bump.
- **Verification gate:** generation rc 0 + both gates + drift byte-no-op + the zero-old-form-link audit.
- **Attribution tier:** analysed on a live system — the full generation ran live on the committed tree with retained output; the deploy pipeline (drift gate → Astro build → Cloudflare Pages publish) runs at merge, and the published-URL re-audit there confirms the routes.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*