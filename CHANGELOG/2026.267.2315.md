# 2026.267.2315 — docs: regenerate at plugin-docs v2026.267.0941 (mixed-tag-form resolver fix)

## Summary

Regenerate the opencharly.ai reference at `plugin-docs v2026.267.0941` (the release that adopts `spec v0.2026267.747`, the mixed-tag-form resolver fix), now also absorbing the **fixed plugin-gh manifest** (opencharly/plugin-gh#9, tag `v0.2026267.2259`).

Two coordinated changes:

1. `.github/workflows/deploy.yml` — bump the CI-time `plugin-docs` pin `v2026.261.1812` → `v2026.267.0941`, so the deploy builds the generator that resolves a Go-module-tagged repo to its true newest tag.
2. `src/content/docs/**` — the regenerated pages at the pinned charly checkout (`d45132f3`) + the docs-pinned marketplace gitlink (`0891217`).

**The headline projection change:** `plugin-gh` moves off the stale plain-tag key to its true Go-module-tag key, AND its candy page's acceptance-plan row is now COMPLETE:

- `plugin-gh` page key: `…-v2026-252-1501` (stale) → `…-v0-2026267-2259` (current);
- the candy page's plan row now reads `the embedded #GhDocument schema compiles, validates a sample, and serializes` instead of the truncated `the embedded` (the defect opencharly/plugin-gh#9 fixed at its source).

`plugin-docs` and `plugin-review` pages also advance to their tags (`release_repos` resolve by newest CalVer tag).

## How tested

**The deploy recipe, reproduced locally against the pinned sources** (pinned charly commit `d45132f3` with its `box/*` submodules populated; the docs-pinned marketplace gitlink `0891217`; the `pins.tsv` wiring candy written before generate, exactly as `deploy.yml` does):

```
$ python3 .github/docs-corpus/build_wiring.py .github/docs-corpus/pins.tsv <charly>/candy/docs-corpus/charly.yml
corpus wiring refs: 65
$ plugin-docs-fixed generate --root <charly-pin> --plugins <marketplace-pin> --out <docs>/src/content/docs
charly docs: 363 recipe pages, 176 plugin pages (163 provider words), 59 cli pages, 671 candy pages, 127 box pages (1401 generated pages cleared before regeneration)
exit 0
```

**The page counts (363 / 176 / 59 / 671 / 127, 1401 cleared) match the deployed run's** (`gh run view 35774353929`) — the strongest proof this tree equals the CI tree's projection.

**The truncated artifact is FIXED** — the page that the earlier head carried malformed (`| `check` | the embedded |`) now carries the complete row:

```
$ F=src/content/docs/reference/candy/github-com-opencharly-plugin-gh-v0-2026267-2259/plugin-gh.md
$ grep -o "the embedded[^|]*" "$F" | head -1
the embedded #GhDocument schema compiles, validates a sample, and serializes 
$ grep -cE "\| .check. \| the embedded \|$" "$F"
0
```

**The diff is exactly the intended projection** (13 files, +370/-108): the plugin-gh page set advanced to `…v0-2026267-2259` with the complete plan row; `plugin-docs`/`plugin-review` keys advanced; `reference/cli/{docs,gh,review}.md` + `reference/providers.md` re-pointed; the `gh` CLI page body documents the merged `document` + `issues` ops.

## Rulebook compliance

- **R0** — loaded `/charly-build:docs`, `/charly-internals:marketplace`, `/charly-internals:git-workflow`.
- **R1** — the projection is regenerated from the pinned sources; the plugin-gh page key and the complete plan row are the generator's real output.
- **R2** — the coupled pin bump + regenerated pages land together (the drift gate's requirement); the source fix (plugin-gh#9) has already landed.
- **R3** — one generator, one regeneration; no hand-editing a generated page.
- **R4** — no workaround: the deploy pin advances to the merged `plugin-docs` tag.
- **R5** — the stale `…v2026-252-1501` pages and the `…v0-2026267-723` (pre-fix-manifest) pages are DELETED in this same change; no stale reference survives.
- **R6** — `docs/plugin-docs-v0.2026267.0941` off `origin/main`; no force-push.
- **R7 / R7a** — regeneration matches the deployed page counts exactly, and the previously-truncated row is verified complete.
- **R8 / R9** — the generated tree is the projected artifact; no label change here.
- **R10** — the change-class gate for a docs regeneration is the drift gate itself (regenerate → clean diff) plus the site build; reproduced here against the pinned sources (page counts match CI; the truncated row is complete). The full site-level `check-docs` bed is the deploy workflow's own Astro build + publish, which this drift-gate-clean regeneration feeds.

## Change classification

- **Change class:** generated docs regeneration + the coupled CI pin bump (docs repo).
- **Verification gate:** regenerate against the pinned charly+marketplace; page counts match the deployed run; the previously-truncated plan row is verified complete.
- **Attribution tier:** `fully tested and validated` — the regeneration ran end-to-end against the real pinned sources and matches the CI deploy's counts.

**Note:** the site pins this repo's `plugin-docs` (bumped here) and the marketplace gitlink; this PR is the regeneration that makes the spec#152 resolver fix AND the plugin-gh#9 manifest fix visible on the published site.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*