# 2026.215.1635 — Regenerate the git-workflow pages for plugins v2026.215.1627

Generated content only. `charly docs generate` output, no hand edits — two pages, matching the two
sources that changed in `plugins` v2026.215.1627.

| Page | What moved in its source |
|---|---|
| `recipes/internals/git-workflow.md` | gains the general invariant: `git add` is all-or-nothing, so a pathspec naming a vanished path fails the **whole** add — plus the two shapes it arrives in and the tells that betray it |
| `recipes/internals/git-workflow/validator-and-calver.md` | trimmed from a second full explanation of the same mechanism to this flow's *instance*, cross-referencing the invariant |

## Why the pairing matters

These two pages changed together because the source change was a **consolidation**, not an
addition. Before it, the published site carried two descriptions of one mechanism with different
remediation recipes — one in the general skill body, one in the CalVer-finalization reference. A
reader following either got a partial answer, and neither pointed at the other.

The site now publishes one explanation plus one flow-specific application. Verified the
cross-reference actually resolves: the phrase `validator-and-calver.md` points at is present in the
emitted `git-workflow.md`, so the link is not dangling in the rendered output.

## Verified

Regeneration is idempotent: a second `task docs:sync` on the committed tree reports
`already up to date (regeneration is a no-op)`.
