# 2026.229.1001 — the drift transcript names its tree, and the message it quotes stops overclaiming

**This is a REGENERATION, not an authored edit.** All four pages carry
`DO-NOT-EDIT — generated by `charly docs generate``; their sources are
`candy/charly-internals/charly.yml` (reaching here through its `plugins` projection) and
`candy/plugin-marketplace/charly.yml`, both in the `charly` repo, landing as
`opencharly/charly#289`.

## What arrives

Four pages:

| Page | Hops from source |
|---|---|
| `recipes/internals/git-workflow/evidence-and-freshness.md` (`+184/-19` at `plugins@8ea7eeb4`) | two — `candy → plugins SKILL.md → docs recipe` |
| `reference/candy/plugin-marketplace.md` | one — the candy `description:` |
| `reference/cli/marketplace.md` | one — same |
| `reference/plugin/plugin-marketplace.md` | one — same |

## Why

`charly marketplace drift` reported success as `clean (N artifact(s) match the committed tree)`. It
compares nothing of the sort: both sides of the comparison are the working tree — the sources on
disk, regenerated in memory, against the artifacts on disk — and git is never invoked anywhere in
the call chain. The message asserted a claim the command has no mechanism to establish, and it did
so most confidently in the case that matters: a tree carrying an unfinished cutover's two halves,
where `git status` says dirty and `drift` says clean. It now says what it compared:

```
marketplace drift: clean (400 artifact(s) on disk match their sources; regeneration is a no-op)
```

The recipe page is rebuilt around that same distinction. Its transcript is now a six-run session at
one named tree in which **every state it asserts is a state it runs** — reverts included — so the
isolation between perturbations is evidence rather than prose. Runs 1, 3 and 6 print the identical
`clean` line, twice from a tree `git status` calls empty and once from a tree it calls dirty: the
disagreement is published rather than explained away.

## Scope

The four pages whose sources changed — read off `git status` after regenerating, not predicted. The
`plugin-marketplace` reference pages move because that candy's `description:` now states what
`drift` compares and names this repository's own `task skills:drift` as the recipe that adds the
`git status` half.

Generated after **both** `plugins` legs merged, against `plugins` at `8ea7eeb4` — the generator's
input and the landed projection at that one commit. The superproject's pin is **not** at `8ea7eeb4`
and cannot be: `charly#289` pins this `docs` merge as well, so it advances both gitlinks after this
entry lands. An earlier draft of this paragraph claimed the pin was already there, which was a
statement about the state I intended rather than the state that existed — the defect this cutover
exists to retire, in the entry describing it.

A recipe page is a projection **of a projection**, so `docs` cannot be current unless `plugins` is
regenerated and landed first — which happened twice, because the first `plugins` merge closed the
source to additions while a second review round was still finding defects in it.

Verified by reading the emitted artifact rather than inferring it from the source fix — a
regenerated projection is not proof that a correction survived regeneration:

| checked in the emitted page | |
|---|---|
| the corrected `orphan scan is PATH-GATED` paragraph | 1 |
| the refuted `keys on the GENERATED HEADER, not on the path` | **0** |
| the `clean (400 …)` line, which the prose says appears in three runs | 3 |
| `all three clean runs` / the superseded `both clean runs` | 1 / **0** |

Every difference between this page and its `plugins` source is one the generator is specified to
make: the frontmatter, the DO-NOT-EDIT header, the recipe-card subtitle, and four cross-reference
rewrites. A claim-keyed sweep run **inside this repo** — `git grep` cannot cross a gitlink — finds no
residue of the superseded four-run description, and the one surviving mention of `match the committed
tree` is the page's own deliberate retraction of it.

Placeholder CalVer — the `pr-validator` finalizes it at merge.
