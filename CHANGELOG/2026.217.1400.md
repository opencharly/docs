# 2026.217.1400 — the orchestration-model page catches up with the model-inherit change

A pure `task docs:sync` — one generated page,
`recipes/internals/agents/orchestration-model.md`, carrying the `DO-NOT-EDIT` header.

plugins#133 (`v2026.217.1324`) rewrote the orchestration-model reference so **no model is named** —
the roster inherits the operating session's model. The generated page still carried the removed
text ("a **Fable 5** orchestrator driving **Sonnet 5** teammates"), because the previous sync
(docs#11) ran at `plugins be21b57`, one commit before that landed. The published site was showing
a doc-vs-reality divergence the source no longer contained; this regeneration closes it.

## Gate

`task docs:drift` reports no drift, and a fresh regeneration at the same inputs is a byte-identical
no-op.

## Provenance

Generated from the superproject at `0cee452f` with `plugins` at `07f267a`
(`v2026.217.1324`). One file changed; the diff is exactly the reference's new wording projected
through the generator.
