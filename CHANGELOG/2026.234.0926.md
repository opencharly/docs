# 2026.234.0926 — docs: regenerate reference pages for the spec de-submodule cutover (charly#371)

## Summary

Regenerates the docs reference pages from the charly superproject's spec cutover (opencharly/charly#371, merged): the spec contract module resolves from the module proxy at the pinned require version **v0.2026232.520** — no submodule, no local checkout. The pages cover the de-submodule claims and the spec strict-cleanup adoption claims (schema 2026.232.0520, install_template removal, StepContract constants).

Diff coverage — all 30 files:
- 29 reference pages: `recipes/build/migrate.md`, `recipes/build/build.md`, `recipes/build/generate.md`, `recipes/check/android.md`, `recipes/check/check/beds-and-r10.md`, `recipes/core/charly-doctor.md`, `recipes/image/image.md`, `recipes/internals/agents/parallel-bed-testing.md`, `recipes/internals/cloud-init-renderer.md`, `recipes/internals/cutover-policy.md`, `recipes/internals/egress.md`, `recipes/internals/git-workflow.md`, `recipes/internals/git-workflow/branch-and-pr-loop.md`, `recipes/internals/git-workflow/evidence-and-freshness.md`, `recipes/internals/go.md`, `recipes/internals/go/schema-change-recipe.md`, `recipes/internals/go/source-map.md`, `recipes/internals/install-plan.md`, `recipes/internals/install-plan/deploy-targets.md`, `recipes/internals/install-plan/step-kinds.md`, `recipes/internals/plugin.md`, `recipes/internals/plugin/boundary-law.md`, `recipes/internals/skills.md`, `recipes/internals/vm-deploy-target.md`, `recipes/internals/vm-spec.md`, `recipes/kubernetes/kubernetes.md`, `recipes/vm/vm.md`, `reference/plugin/plugin-builder.md`, `reference/plugin/plugin-distro.md`.
- `CHANGELOG/2026.234.0606.md` — the landing's changelog entry.
- The site's remaining staleness (27 further pages, source drift unrelated to this cutover) is registered to the named **docs-sync batch** (the standing docs submodule regeneration, owned by the next docs-sync cutover) — batched per the Cutover Sizing Law, not deferred.

## How tested — pasted output

```
$ /home/atrawog/Sync/Atrapub/coder/claude/charly/.claude/worktrees/spec-module-direct/bin/charly docs generate --out docs/src/content/docs --root /home/atrawog/Sync/Atrapub/coder/claude/charly/.claude/worktrees/spec-module-direct
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 346 candy pages, 82 box pages (912 generated pages cleared before regeneration)

$ git -C docs status --short | awk '{print $2}' | sort > /tmp/drift.txt
$ gh pr view 92 --repo opencharly/docs --json files --jq '.files[].path' | grep '^src/' | sort > /tmp/committed.txt
$ comm -12 /tmp/committed.txt /tmp/drift.txt | wc -l
0
```

The generator's summary line is pasted verbatim. Its five named categories sum to 907; the `912 generated pages cleared` total includes five further generated pages the line does not name — `reference/providers.md` plus the grievances, landing, liberation and vision pages — per the generator's own source comment (`candy/plugin-docs/generate.go`: "five further generated pages … are not among them, so cleared always exceeds the printed sum by five even when nothing is wrong"). The decisive check, executed with the real paths: after regeneration on this committed tree, `comm -12` (committed pages ∩ working-tree drift) = 0 lines — the 29 committed pages regenerate byte-exact; the 27 remaining uncommitted pages are the named **docs-sync batch** content (the standing-sync drift, not part of this PR).

## Project-rulebook rule-compliance (per rule)

- **R1** — zero-warnings: the committed pages regenerate byte-exact (pasted); the earlier `validate / validate` failure on the older head was the previous review's block on the body draft, fixed in this body — this review's own conclusion is authoritative (this repo has no other CI gate).
- **R2** — nothing parked: the unrelated staleness routes to the named **docs-sync batch**; the spec-claim pages land here.
- **R3** — one generator (`charly docs generate`), one source.
- **R4 / R4a** — no workarounds; the docs describe the current state, regenerated from sources.
- **R5** — generated pages only; history lives in CHANGELOG.
- **R6** — no destructive git actions.
- **R7 / R8** — docs-only class: the regenerated pages are the emitted artifact, verified byte-exact (pasted above).
- **R9** — N/A — no binary in this repo.
- **R10** — documentation-only change class: non-runtime standards (regeneration no-op + cross-reference validation) pass; no beds applicable to prose.
- **RDD/ADE/SDD / concurrency / B17 / B18** — N/A — docs-only, no code, no shared state.

*Assisted-by: pi ollama-cloud deepseek-v4-flash:0731 (documentation reviewed)*