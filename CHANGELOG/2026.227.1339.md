# 2026.227.1339 — mirror the git-workflow evidence-discipline reference

Projects `opencharly/plugins#171` (merged `396e4bfe`, tag `v2026.227.1302`)
onto the site:

- `recipes/internals/git-workflow/evidence-and-freshness.md` — NEW. Eight
  sections: five epistemic (ways a claim about a change goes wrong, all one
  question failing against a different noun) and three mechanism (how charly and
  git actually behave). Every item was a blocking finding on a real landing.
- `recipes/internals/git-workflow.md` — the Reference Index row for it.
- `recipes/internals/git-workflow/branch-and-pr-loop.md` — its per-path
  forward-gitlink check now points at the whole-tree comparison that supersedes
  it.

Also carries the 37 `reference/box/*` pages that the same generation run emits —
pure `@github` pin churn, `v2026.201.0706` → `v2026.227.0914`, 153 insertions and
153 deletions, no prose. They are the projection half of `opencharly/charly#277`
(the `box/fedora` bump), which merged four hours before this PR opened without
its docs commit.

An earlier revision of this entry excluded them and named them "for their owner".
That was wrong on three counts, and the entry records it because the same mistake
is what left the mirror behind before:

- `#277` is CLOSED. "Left for its owner" named an exit that does not exist — the
  cutover it points at is finished and will not come back for them.
- `/charly-build:docs`, in the very tree this PR mirrors, already says the docs
  commit belongs to the same cutover as the candy edit, and that advancing a
  gitlink without regenerating in the same change is precisely how the published
  site falls behind.
- The page this PR mirrors forbids the framing outright: "belongs to that PR's
  author" is one of the two phrasings it names as R2 violations.

The separability test fails on measurement, not on principle: `task docs:drift`
exits 1 on exactly this state, a docs merge mints a `v<CalVer>` tag like every
repo, and the fix is emitted by this cutover's own gate run — so excluding it
would tag a tree the generator does not reproduce. After this lands, regeneration
is a true no-op; these were the sole residual drift.

Generated by `charly docs generate`, never hand-edited; the fail-closed
whole-site link gate passes at exit 0 across all six repo roots.
