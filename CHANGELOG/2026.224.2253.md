# 2026.224.2253 — drop AGENTTEAMS_LOCAL_ONLY from the agentteams recipe env list

Regenerate the generated half of the site after the superproject removed
`AGENTTEAMS_LOCAL_ONLY` from the deploy-overridable env list in the `agentteams`
skill entity: the superproject verified (against the controller candy's
`env_accept:` and the upstream controller binary) that nothing consumes the
variable, so the false claim was deleted at the source and the
`recipes/agentteams/agentteams.md` page carries the corrected list. Placeholder
CalVer — the pr-validator finalizes it at merge.
