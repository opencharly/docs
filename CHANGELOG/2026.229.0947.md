# 2026.229.0947 — regenerated: the second projection hop

Placeholder CalVer (the pr-validator finalizes it at merge). Chosen after the newest entry
`2026.228.0822`, with no filename or tag collision.

Every page here is generated by `charly docs generate` (`task docs:sync`) from the
superproject's candy sources via the `plugins` skill corpus. Nothing was hand-edited.

This is the second hop of one prose correction: **candy → plugins → docs**. See the
`plugins` entry for this CalVer for the full account. The substance:

- the Containerfile emitter stopped hardcoding `bash`, so the per-verb emission table in
  `recipes/build/generate.md` described an interpreter the emitter no longer names — and its
  `cmd` row was additionally still describing ANSI-C `$'...'` quoting where the emitter has
  been using a heredoc;
- `apk` joined the package-format enumerations (`recipes/image/image.md`'s
  `Valid: rpm, deb, pac, aur`, and the emission-order lists);
- the `charly candy add-*` rows gained `add-apk`, which now exists.

## Why this is five pages and not forty-one

A wholesale `task docs:sync` produced **41** modified pages. Most of those were not stale
pages being brought forward — they were **reverts** of prose that had legitimately landed
here ahead of its superproject candy source, which is the ordering this repo works under (a
projection merges before the source that pins it). Regenerating from a superproject tree
that lacks the pending source drags the projection backwards, and nothing in the diff
distinguishes that from a legitimate update.

So this change keeps only the pages projected from the three candy files it edited, and
every deleted line in them is one it deliberately replaced. The same claim was then read out
of the candy source, the generated skill, and the generated page — all three hops agree,
which was verified rather than assumed.
