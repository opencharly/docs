# 2026.217.1459 — the landing page catches up with the fixture-split connection

A pure `task docs:sync` — one generated page, `index.md`, the projection of `README.md`.

charly#215's README gained one sentence connecting its two candy counts — "73 real + 13 fixtures =
the 86 plugin candies counted elsewhere on this page" — because a cold reader independently
flagged 73 vs 86 as in tension. The landing page is a projection of the README, so it carries the
same sentence. docs#12 merged the orchestration-model regeneration but predated this README edit;
this is its companion.

## Gate

`task docs:drift` reports no drift, and a fresh regeneration at the same inputs is a no-op.

## Provenance

Generated from the superproject at `c52dd666` with `plugins` at `07f267a`. One file changed.
