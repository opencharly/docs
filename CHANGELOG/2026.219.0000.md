# 2026.219.0000 — docs: regenerate the reference half from the final sources

The merged docs#24 projected stale candy descriptions (16 `fleetdverb`
corruptions) + pre-fix `BundleAddCmd`/`BundleDelCmd`; regenerated from the
final candy sources (charly#232's clean descriptions). Placeholder CalVer —
the pr-validator finalizes it at merge.
