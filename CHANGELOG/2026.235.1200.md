# 2026.235.1200 — feat: pin the marketplace submodule; regenerate at the cutover pins

## Summary

The **marketplace de-submodule cutover's docs leg** (charly#382 + marketplace#229 landed the generator, harness and corpus sides): the docs repo now pins the standalone **opencharly/marketplace** repo (the corpus the recipe pages project from) in `.gitmodules` alongside charly, and the `deploy.yml` workflow checks it out and passes it to `charly docs generate --plugins`.

- `.gitmodules`: charly → the cutover main `fa254fd5` (charly#382), + `marketplace` → the merged standalone main `e8efdb15` (marketplace#229).
- `deploy.yml`: `--plugins ../marketplace` on the generate step; the checkout now carries the marketplace submodule; comments updated.
- **Content regenerated at the cutover pins** (41 pages: the changed candy descriptions + internals skill pages + the docs skill + the plugin-marketplace page + vision, etc.) — the deterministic projection of the changed sources.
- A locally built `bin/charly` was accidentally committed during the regeneration work — removed in a final commit and `bin/` gitignored (the deploy workflow builds its own binary from the pinned checkout on every run).

## How tested

**Regeneration at the pinned charly + marketplace is a no-op on the committed content (pasted):**

```
$ cd /tmp/mp-docs/charly && ../bin/charly docs generate --root /tmp/mp-docs/charly --plugins /tmp/mp-docs/marketplace --out /tmp/mp-docs/src/content/docs
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 346 candy pages, 82 box pages (912 generated pages cleared before regeneration)
$ git -C /tmp/mp-docs status --porcelain -- src/content/docs | wc -l
0    # the committed content is a no-op regeneration at the pins
```

2. **The content diff carried by this PR (41 files — the changed sources' pages, pasted stat):**

```
$ git diff main...HEAD --stat -- src/content/docs | tail -3
41 files changed, 183 insertions(+), 159 deletions(-)
```

3. **The PR's own `deploy` check ran the full pipeline GREEN at head 9f58213** (run 32637815766, pasted):
```
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 346 candy pages, 82 box pages (912 generated pages cleared before regeneration)
content: committed tree matches the pinned charly (regeneration is a no-op).
pages built: 932
✨ Deployment complete! (Cloudflare Pages)
```
The pipeline: recursive checkout (charly + the marketplace submodule) → build charly from the pin → generate with `--plugins ../marketplace` → drift gate → astro build → Cloudflare Pages.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + docs skills loaded before editing |
| R1 | N/A — no surfaced failure |
| R2 | N/A — no surfaced issue parked |
| R3 | the generator (charly candy/plugin-docs + plugin-marketplace) remains the single canonical owner; this repo commits only its projection |
| R4 | no workarounds — the drift gate is the policy's own operation |
| R5 | the `plugins/`-era references in the deploy comments + prose reworked; the marketplace checkout replaces the old submodule dependency |
| R6 | git status checked; no force-push; feat-branch + PR landing |
| R7 | the regeneration no-op proof pasted above + the PR's deploy check (the workflow is the gate) |
| R8 | the emitted artifacts verified live (the regenerate run above) |
| R9 | N/A — no binary ships in this repo (an accidentally committed local build artifact was removed + `bin/` gitignored in the same PR; the deploy workflow builds its charly binary from the pinned checkout into `bin/` on every run) |
| R10 | the fresh-checkout regeneration at the pins IS the R10 for this repo (the deploy does the same on every run) |

## Change classification

- **Change class**: submodule pin advance + deploy workflow update + regenerated content
- **Verification gate**: the `deploy` workflow on this PR (build → generate → drift gate → astro build) + the regeneration no-op proof pasted above
- **Attribution tier**: analysed on a live system — the regeneration + the content diff ran live with retained output; the deploy gate's full run is the PR's own CI
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*