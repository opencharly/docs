# 2026.224.2343 — regenerate the agentteams recipe cards with the what-is narrative

The recipe-card quality batch (R2 follow-on from the Unit 7 landing's cold-reader,
opencharly/charly PR #254 Phase 1b): the `agentteams` and `agentteams-cli` recipe
pages now open with a plain-language "what is AgentTeams / how it works" narrative
(the Manager–Workers model, the controller's reconcile-and-spawn loop, the
room-exists verification sequence) and define the terms the cold-reader flagged as
undefined on first use — Tuwunel, Higress, `agt`, `mc`, SA, CR, ADE, and the
self-provisioned secret files with their exact paths. Regenerated from the
superproject's `skill:` entities; regeneration is a no-op on a clean tree and the
whole-site link gate passes.
