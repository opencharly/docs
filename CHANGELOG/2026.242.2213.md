# 2026.242.2213 — revert(deploy): restore the pre-migration charly pin (deploy needs the plugin-docs migration first)

## Summary

Revert c938bc2 (`chore(deploy): bump the charly CI pin to 7db4f988`, opencharly/docs#103) — restores the docs deploy to green.

**RCA (live, deploy run 33336970822 + local regeneration runs):** the docs deploy is broken at ANY post-cutover charly pin on TWO counts, each individually blocking:

1. `charly docs generate` **no longer exists** in a charly binary built from current main — the verb's home `candy/plugin-docs` moved to the standalone **opencharly/plugin-docs** (the candy de-submodule cutover, charly#408/#409); a charly built from current main answers `unexpected argument docs`.
2. Even invoked **directly** (the plugin binary **is** dual-mode CLI — built here and driven with the exact verb arguments), the regeneration over current main + the docs repo's pinned marketplace corpus emits 635 pages of drift and fails its internal link gate: `/reference/candy/sshd/` and `/reference/cli/fleet/` are linked from committed narrative pages but are not emitted — the standalone plugin candies (`plugin-fleet` et al.) are proxy-resolved and not visible to the generator's file-walk corpus, which is the docs repo's pinned `marketplace` at `e8efdb1`.

The plan (opencharly/opencharly `plan/mise-followup-todo.md`, item 5) itself recorded this limitation ("local generation can't resolve the runtime plugin-docs without the docs deploy's clone context"); the deploy run proves the CI context is insufficient **post-cutover** — the regeneration requires the docs deploy to migrate to the standalone plugin corpus (a deploy-recipe + invocation migration, its own named change).

#103's pin bump was auto-merged on the validator PASS (the deploy is informational there) — leaving main's deploy red; this revert restores it.

## How tested

```
$ gh run view 33336970822 --log-failed | grep 'unexpected argument'
charly: error: unexpected argument docs, did you mean "logs"?
```

```
$ /tmp/plugin-docs-bin generate --root <charly> --plugins <docs>/marketplace --out <docs>/src/content/docs
635 pages changed; link gate: /reference/candy/sshd/ + /reference/cli/fleet/ unresolvable
```

```
$ git revert c938bc2 → 1 file changed, 2 insertions(+), 2 deletions(-)
```

## Rulebook compliance

- **R1** — every failure RCA'd above; no pre-existing framing (the red main deploy is the direct consequence of #103, this revert is its named fix).
- **R6** — revert commit (adds history), no force-push.
- **R2/B10** — the exit named: the docs deploy's plugin-docs migration (standalone corpus wiring) as a separate change; this revert does not park the pin-bump — it restores the deploy until that migration lands.

## Change classification

- **Change class:** fix (revert of a CI pin advance that broke the deploy post-cutover)
- **Verification gate:** the deploy workflow's regeneration/drift gates on the live runner (green against the restored pin)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (analysed on a live system)*