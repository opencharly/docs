# 2026.247.1748 — chore: sync pinned anchors + regenerate content (marketplace v2026.247.1546 corpus, charly post-#537)

## Summary

Drift-gated docs sync (the deploy workflow compares content regenerated from the pinned charly + mounted marketplace against the committed tree). Two anchors advanced upstream since the last docs sync:
- marketplace → 57f98997 (post-#285: the wl-pixelflux corpus pins moved to v2026.247.1546);
- charly CI-time pin in deploy.yml: afb1c5a9 → 9d9ee1b3 (post-#537: the cachyos-import re-vendor landed).

The generated half (src/content/docs) is regenerated from those anchors with the deploy workflow's exact recipe (charly@9d9ee1b3 clone + box/* submodules, plugin-docs v2026.242.0527, build_wiring.py pins.tsv → docs-corpus, generate --root /tmp/cbx-charly --plugins docs/marketplace --out src/content/docs): 280 files, +6927/−725 — 142 added / 100 modified / 32 renamed (old-tag → new-tag reference pages) / 6 deleted. Highlights: new recipes/crabbox/ pages (skills + check + automation corpus surfaced), advanced wl-candy reference pages at v2026.247.1546, updated charly-marketplace corpus + providers pages.

vTAG placeholder disclosure: generated pages carry the corpus's established vTAG placeholder convention in example refs — pre-existing on main (7 lines across recipes, e.g. recipes/build/reconcile.md and recipes/check/check/beds-and-r10.md) and documented in the marketplace corpus itself (its check/skills/check/references/beds-and-r10.md: "the ref's :vTAG is ignored"). The regen projects the convention into further example lines (+82 lines). These are illustrative refs, not resolvable pins — a working corpus convention, not a generator defect.

## How tested

- Generator reproduced exactly per the deploy workflow (pin-checked via gh api: marketplace main = 57f98997, charly main = 9d9ee1b3 — merged refs only).
- Drift-proof (the gate's own check): the generator re-run against the committed tree is a no-op — git diff --exit-code -- src/content/docs → GATE_EXIT=0.
- Change-class gate: generated-content regeneration (the deploy pipeline regenerates + fails on diff by contract; this PR carries exactly that diff).

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the sync. |
| R1 — RCA every anomaly | The pre-existing stale edit in the mounted marketplace was verified identical to the merged #285 content (wl pins v2026.247.1546) before discarding — no work lost. vTAG verified as the corpus's documented placeholder convention (pre-existing on main + in the corpus reference itself). |
| R3 — no duplication | Generated half projected from the pinned anchors; no hand-edits to DO-NOT-EDIT files. |
| R4 — no workarounds | The generator is the only content writer; the drift gate's own no-op contract proves the state. |
| R6 — git safety | No force-push; branch-per-change PR; pins = merged refs only. |
| R7 — prove behaviour | Drift-proof re-run GATE_EXIT=0 pasted. |
| R10 — fresh disposable proof | Regeneration was run from a fresh pinned-checkout clone in /tmp, not the dev tree. |

## Change classification

Generated-content regeneration + two pin bumps (deploy workflow's charly CI-time commit + marketplace mount). **Attribution:** fully tested and validated (the drift gate's own equivalence check — regenerate twice, zero diff — passed on the committed tree). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*