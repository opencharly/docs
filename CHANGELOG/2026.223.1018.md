# 2026.223.1018 — regenerate the site for the k8s→kubernetes substrate rename

Regenerate the generated half of the site (index glossary, reference pages,
recipes) from the cutover sources after the deploy substrate kind `k8s:` became
`kubernetes:` (the cross-repo cutover consuming spec `v0.2026223.1018`), and
sweep the hand-authored `concepts/` pages (the substrate glossary entry, the
deploy-substrates list, and the mold table) to the same vocabulary in the same
cutover — the generated and hand-authored halves no longer contradict each
other. Placeholder CalVer — the pr-validator finalizes it at merge.
