# 2026.227.1136 — project the docs-coupling lessons, and close the deferred mirror leg

Eight paths, from two distinct causes that one regeneration necessarily closes
together. `charly docs generate` rewrites its trees wholesale, so a mirror that is
behind cannot be brought forward selectively — every pending page lands with the
next commit or none of them do.

## This cutover's own delta — four pages

The superproject edited four `skill:` sources; these are their recipe cards.

- `recipes/build/docs.md`
- `recipes/build/reconcile.md`
- `recipes/image/layer.md`
- `recipes/tools/docs-site.md`

## The deferred mirror leg — four paths

`CHANGELOG/2026.227.1114.md` in the superproject deferred this leg under the
heading "Deferred: the docs mirror leg". It is now closed.

- `recipes/kubernetes/helm.md` (new)
- `recipes/index.md` — card count `299 → 300` and the `charly-kubernetes` group
  description
- `recipes/internals/go.md`
- `recipes/internals/git-workflow/multi-repo-coordination.md`

That entry recorded the leg as "exactly ONE page (`recipes/kubernetes/helm.md`)".
The measured delta is the four paths above, so closing the leg on the one-page
figure would have shipped a quarter of it. The wrong figure is reported to the
factory program's owner rather than rewritten from here — it is that program's
history — but the leg itself is closed correctly, which is what this commit does.

## Provenance

Regenerated with `bin/charly` at `2026.227.1116` from superproject
`feat/docs-coupling-lessons`, with `plugins` carrying this cutover's companion
projection. Three independent generations of the pre-edit tree were byte-identical,
and a generation against the previous `plugins` pin reproduced the committed tree
exactly — so the generator is deterministic and every path here is real staleness,
not churn. `task docs:build` completes: 910 pages.

No hand-authored page changed. No route moved.
