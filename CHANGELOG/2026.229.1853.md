# 2026.229.1853 — the reference index says what the section it points at says

**This is a REGENERATION, not an authored edit.** The page carries
`DO-NOT-EDIT — generated by `charly docs generate``; its source is the `skill:` entity in
`candy/charly-internals/charly.yml`, reaching here through its `plugins` projection
(`opencharly/plugins@9ee9f930`, `v2026.229.1819`), and landing as `opencharly/charly#289`.
Projected onto `docs` at base `717cd67a`, after `opencharly/docs#71` merged.

## What arrives

`recipes/internals/git-workflow.md` — the reference index called status-absence on a known head
**"a false negative"** while the section it indexes says it **"proves nothing"**.

Those are not the same claim. *A false negative* asserts the thing is real but unseen; *proves
nothing* asserts the observation is uninformative. The index was making the stronger claim its own
section had retracted — and an index row is a summary, so it goes stale exactly when the thing it
summarizes is corrected, without anything in the row itself changing.

## What this leg is NOT carrying, and why

Regenerating at this base also rewrites recipe and reference pages belonging to two other cutovers,
and no count of them is given here on purpose — the set is a function of whichever base the
regeneration runs against, and this entry has already been wrong once for stating one measured
before the base moved. They are identified by owner instead, which does not expire:

- the three `recipes/ollama/*` pages and the `recipes/index.md` card count, whose superproject
  producer is on no branch and in no open PR (`opencharly/charly#291`);
- the `reference/` and remaining `recipes/` pages projected from `charly@feat/distro-vocab-openrc`,
  whose `docs` half **already merged** as `opencharly/docs#71` and whose superproject half is open as
  `opencharly/charly#296`. Regenerating them from THIS leg's source would revert that merged work.

Committing either set here would publish another cutover's pages under this one's CalVer.

**The revert set was re-derived at this tree rather than carried forward**, and it has now differed
in kind at every regeneration in this cutover — the same `ollama-cli` page has appeared as an
untracked addition, as a deletion, and as an addition again, depending on what the pinned `plugins`
tree happened to contain. A revert set is a measurement of one tree, never a property of a cutover.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
