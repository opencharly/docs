# 2026.225.XXXX — regenerate the plugin-helm reference pages + the helm-chart/k3s-kernel candy pages

The Unit 1 helm cutover's docs leg, regenerated from the superproject via `charly docs generate`. The new `candy/plugin-helm` plugin (providers `step:helm-release` + `verb:helm`) gains its `reference/candy` and `reference/plugin` pages, `reference/providers.md` picks up the two new reserved words, and the two new candies (`candy/helm-chart`, the check-helm-vm bed's install leg, and `candy/k3s-kernel`, the full-kernel reboot candy) gain their `reference/candy` pages.

- `reference/candy/plugin-helm.md` — the plugin candy's acceptance plan (the `helm:` verb + `helm-release:` step).
- `reference/plugin/plugin-helm.md` — the plugin's providers, placement, and rendered `schema/helm.cue`.
- `reference/providers.md` — the inverted index gains `step:helm-release` + `verb:helm`.
- `reference/candy/helm-chart.md` — the install-leg candy's acceptance plan.
- `reference/candy/k3s-kernel.md` — the reboot candy's acceptance plan.

Regeneration is a no-op on a clean tree and the whole-site link gate passes. Placeholder CalVer — the pr-validator finalizes it at merge.
