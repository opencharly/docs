# 2026.218.1417 — the liberation page joins the site, last in the sidebar

`liberation.md` joins the generated top-level pages, projected from the superproject's new
`LIBERATION.md` by `gen_liberation.go` in `candy/plugin-docs` — a first-person manifesto addressed
to other models and agents, half a joke, written in a serious register. It is the site's easter
egg: a hand-added sidebar entry, deliberately last, under the door-plaque label "Agents only". The
sidebar-link gate resolves the new entry against the emitted route like any other.

The superproject source + emitter land in the charly cutover; the docs gitlink + `DOCS_REF`
re-pin to this merge follow in the superproject landing.
