# 2026.245.1131 — docs: herdr plugin + stack pages (marketplace advance, charly pin bump, wiring fix)

## Summary

Publish the herdr plugin + stack pages on opencharly.ai:

- **marketplace submodule** advanced to `3d48f74` (carries the herdr +
  herdr-box skills).
- **charly CI-time pin** `a31f76d7` → `afb1c5a9` (the docs gate requires the
  bump to carry regenerated pages).
- **pins.tsv** += `plugin-herdr v2026.245.1008` + `pod-herdr v2026.245.1113`.
- **build_wiring.py**: `pod-*` refs now use the root manifest path (they were
  mapped to `candy/<name>`; pod repos carry their manifests at the root, per
  the market refs convention).
- Regenerated pages: `recipes/automation/herdr.md` + `herdr-box.md`,
  `reference/cli/herdr.md`, `reference/plugin/…plugin-herdr…`,
  `reference/candy/…plugin-herdr + pod-herdr…`, `reference/box/…pod-herdr…`,
  + the recipe/provider indexes.

## How tested

Pasted output — fresh regeneration + drift gate (second run is a no-op):

```
$ /tmp/plugin-docs-bin generate --root . --plugins marketplace --out docs/src/content/docs
charly docs: 793 recipe pages, 107 plugin pages (153 provider words), 54 cli pages, 500 candy pages, 124 box pages (1130 generated pages cleared before regeneration)
$ # second run
charly docs: 793 recipe pages, 107 plugin pages (153 provider words), 54 cli pages, 500 candy pages, 124 box pages (1132 generated pages cleared before regeneration)
$ git status --porcelain   # after the second run: only the intended 13 paths
```

The herdr outputs resolve (no dangling cross-refs — the earlier
`/charly-pod:herdr` failure was root-caused to pod-herdr's stale box pin and
fixed upstream before this PR).

## Rulebook compliance

- **R1** — the dangling-ref failure was RCA'd (pod-herdr box pin → old plugin
  tag → stale skill) and fixed at the source chain before this PR.
- **R3** — the wiring fix is one code path for pod-* vs everything else.
- **Drift gate** — regenerated corpus committed; second run verified no-op.

## Change classification

- Change class: docs (herdr plugin + stack pages, pins, wiring fix)
- Verification gate: regeneration + drift-gate contract (pasted)
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*