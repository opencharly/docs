# 2026.234.2354 — docs: regenerate pages for the sdk de-submodule cutover
<!-- auto-merge-finalized: 2026.234.2354 -->

## Summary

Reference + recipe pages regenerated by `charly docs generate` from the current corpus (plugins@83792e9): the sdk contract module is now described as resolved from the module proxy at the shared pinned require version **v0.2026234.347** — no submodule, no local checkout, no `use ./sdk` in go.work; schema cutovers adopt a tagged sdk release as the new shared pin (`task mods:tidy` + canonical-go.mod gate). Key pages: `recipes/build/migrate.md`, `recipes/internals/go/source-map.md`, `recipes/internals/plugin/authoring-recipes.md`, plus the `reference/candy/*` + `reference/plugin/*` pages.

The regeneration also carries the corpus's evaluator-merge sweep (plugins#223) that the committed pages predated — docs regeneration is atomic (never hand-edit generated pages; the generator IS the change).

## How tested

**Regeneration — executed (charly worktree, plugins submodule at the corpus heads 83792e9 + 3441784):**

```
$ task docs:sync
regen exit=0
$ git -C docs diff main...HEAD --stat | tail -1
35 files changed, 205 insertions(+), 198 deletions(-)
```

The head carries: the full regeneration (`3d707ac`), the last-two-claims sweep (`684ddad`), the staged-CHANGELOG drop (`0a6109f`), and the landing-flow bullet fix (`9a19fc5`, regenerated from charly#379 + plugins#227 — validator-and-calver.md's write-access bullet now describes native auto-merge + tag-on-merge, no more `gh pr merge --squash --delete-branch` contradiction).

- `charly docs generate` exited 0 (dead sidebar links hard-fail the run)
- Sample hunk verified — `recipes/build/migrate.md` now reads "adopt the tagged sdk release as the new shared pinned require (the `task mods:tidy` sweep + canonical-go.mod gate keep every module on the one pin)": the sdk-submodule claim is gone
- **Sweep of the regenerated pages — executed, output:**

```
$ cd docs && git grep -nE "sdk submodule|sdk/ submodule|bump the sdk submodule|gitlink bump" src/ || echo "NO MATCHES (exit 1)"
NO MATCHES (exit 1)
$ cd docs && git grep -n "gh pr merge --squash --delete-branch" src/ || echo "NO MATCHES (exit 1)"
NO MATCHES (exit 1)
```
- `multi-repo-coordination.md` now reads "the **sdk contract repo** (`github.com/opencharly/sdk` — NOT a submodule; ...)" and "adopt the tagged sdk release as the new shared pinned require — there is no gitlink"
- The staged CHANGELOG file was dropped per the tag-on-merge flow this PR ships (the validator's B15 finding); the merged PR body is the changelog

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 skills first | docs skill (docs-site) + git-workflow loaded |
| R1 RCA | the non-cutover pages are the evaluator-merge corpus sweep (plugins#223) the committed pages predate — regeneration is atomic, not hand-scoped |
| R2 | one PR, full regeneration, same commit |
| R3 no duplication | generated pages only — never hand-edited; sources are the charly entities + plugins corpus |
| R4 | N/A — no runtime surface |
| R5 delete legacy | sdk-submodule claims swept by the regeneration; grep-verified |
| R6 | clean feat branch off origin/main; no force-push |
| R7 | regen run + outputs pasted; `docs:pin` gitlink/DOCS_REF alignment untouched |
| R10 | docs-only class — non-runtime standards |

## Change classification

- **Change class**: documentation-only (generated site pages)
- **Verification gate**: non-runtime standards; regeneration no-op verified
- **Attribution tier**: documentation reviewed — the regeneration + sweep ran live with retained output; the gate is the non-runtime standards (docs-only class)
- **Landing**: fresh `charly/pr-validator`; tag-on-merge writes the CHANGELOG stamp

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*
