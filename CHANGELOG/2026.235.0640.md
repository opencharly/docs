# 2026.235.0640 — chore: backfill the CHANGELOG entries for the merged docs legs (#93, #94)

## Summary

The org-wide tag-on-merge workflow's changelog write was 409-blocked on the protected `main` (the bot token had no bypass — the required-check enforcement rejected the contents-API write) for the docs legs #93 (2026.234.2354) and #94 (2026.234.2355). These entries are the merged PR bodies backfilled add-only per the rulebook's backfill path; the tag-on-merge sweep skips them ("already exists") once the mechanism resumes.

Root fix (landed in parallel): every org repo's main migrated from the legacy branch protection to a ruleset with the `charly-auto-merge` app as a scoped bypass actor, and the push-based tag-on-merge dispatcher is propagated to the remaining repos (sdk#158, spec#48, .github#72).

## How tested

- The two files were written from the merged PR bodies verbatim (`gh pr view 93/94 --json body`) with the CalVer stamp from each merge time
- `git status` clean; the files are add-only (the sweep's idempotence check will skip them)
- The ruleset migration is proven live: charly's sweep at 2026-08-23T06:16Z created 8 CHANGELOG entries as `charly-auto-merge[bot]` commits (bypass effective); plugins' entries landed at 06:3xZ

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow loaded |
| R1 | the 409 root-caused to the missing bot bypass on protected main (legacy API drops the bypass fields; rulesets carry them) — fixed org-wide via rulesets + verified live |
| R2 | backfill covers both docs merges in one PR |
| R3 | the PR-body-is-the-changelog content is generated, not hand-authored |
| R4 | no workarounds — the add-only backfill is the rulebook's documented path |
| R5 | N/A — no legacy claims |
| R6 | clean feat branch; add-only files; no force-push |
| R7 | outputs above |
| R10 | docs-only class — non-runtime standards |

## Change classification

- **Change class**: documentation-only (2 generated CHANGELOG entries, backfilled)
- **Verification gate**: non-runtime standards
- **Attribution tier**: analysed on a live system — the root fix is proven live on charly/plugins
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*