# 2026.251.0820 — docs(corpus): regen pages at marketplace@7359675 (corpus v2026.251.0720 wave #293)

## Summary

Marketplace corpus advance wave: opencharly/marketplace#293 (merged as `73596755`) regenerated the corpus after layer-charly-internals v2026.251.0709 (git-workflow validator re-trigger mechanics section added to `internals/skills/git-workflow/SKILL.md`) plus the omarchy-eval disclaimer-wording change. This PR regenerates the docs site's generated half at the moved corpus, CI-identical.

- **Marketplace submodule gitlink** bumped `30b1c459a2` → `73596755` (the corpus the recipe pages project from; `git submodule update --remote` at the repo's recorded branch=main).
- **Regenerated page** (`charly docs generate`, CI-identical invocation — charly at the repo's pinned CI commit `9d9ee1b3` with box/* submodules, plugin-docs binary built at the pinned tag v2026.242.0527, corpus wiring candy from `.github/docs-corpus/pins.tsv`, `--plugins` = the marketplace checkout at `73596755`):
  - `src/content/docs/recipes/distros/omarchy-eval.md` — omarchy-eval disclaimer wording restored per the corpus regen (EXTERNAL NON-AUTHORITATIVE carve-out dropped; 3 ++ / 4 --).
- Everything else in the generated tree regenerated byte-identically (2 changed files + the gitlink, nothing else).

**Note on the git-workflow v2026.251.0709 section:** the corpus move carries the new "Validator re-trigger + PR-body evidence mechanics" section into `internals/skills/git-workflow/SKILL.md`, but the site's `recipes/internals/git-workflow.md` page is NOT changed by this regen. Root cause: the generator appends candy `skill:` projections (`collectCandySkills`, generate.go "Phase-3 ref-driven complement") AFTER the marketplace-corpus read (`collectSkills`) with no dedup, and the `git-workflow-skill` entity from `box/cachyos/candy/charly-marketplace`'s pinned `layer-charly-internals:v2026.243.1002` (resolved through charly@9d9ee1b3's cachyos gitlink) is written to the same page path, overwriting the marketplace page. CI regenerates with identical inputs, so the drift gate at the bumped gitlink is green and this tree is exactly what CI will produce. The section reaches the site only when the layer pin chain advances (cachyos candy closure → layer-charly-internals). Flagged here as a generator-precedence finding for the owning team (plugin-docs), out of scope for this regen PR.

## How tested

Pasted outputs (branch `feat/corpus-regen-gitworkflow`):

```text
$ git diff --stat   (working tree after regeneration, pre-commit)
 marketplace                                        |   2 +-
 src/content/docs/recipes/distros/omarchy-eval.md |   5 ++---
 2 files changed, 3 insertions(+), 4 deletions(-)

$ git status --porcelain   (pre-commit)
 M marketplace
 M src/content/docs/recipes/distros/omarchy-eval.md

$ tail /tmp/docsr-regen.log   (generation #1 output; exit 0)
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 966 recipe pages, 116 plugin pages (155 provider words), 54 cli pages, 608 candy pages, 138 box pages (1269 generated pages cleared before regeneration)

$ git status --porcelain   (post-commit)
(empty)

$ generation #2 on the COMMITTED tree — full regeneration, then:
$ git status --porcelain
(empty)   ← regeneration on the committed tree is a no-op: the drift gate at CI (identical command) is green.
```

Regeneration mirrors the repo's `deploy.yml` steps exactly: fresh charly clone at the in-workflow pinned commit `9d9ee1b3` with `submodule update --init --recursive`, plugin-docs CLI built from the pinned tag `v2026.242.0527` (`go build ./cmd/serve`), corpus wiring candy written by `.github/docs-corpus/build_wiring.py` + `pins.tsv` (66 refs), `generate --root /tmp/charly --plugins marketplace --out src/content/docs`. The 900s timeout bound was not reached (runs completed in ~5 min each).

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | RCA: only `omarchy-eval.md` changed after a full-tree regeneration at the moved corpus — proof the corpus advance is a no-op everywhere else (diff audit pasted); the git-workflow page non-change RCA'd to the candy-projection overwrite (documented in Summary). |
| R2 | No conflict — recipe pages are generated (DO-NOT-EDIT); no hand-edited page (start/ concepts/ guides/) touched. |
| R3 | No duplication — pages are projections of the marketplace corpus; no content restated in this PR. |
| R4 | No workarounds — standard CI-identical `docs generate` invocation; zero hand-edits to generated files. |
| R4a | Submodule gitlink bumped to a MERGED marketplace commit (73596755 = current marketplace main HEAD); no dangling refs. |
| R5 | Removed-carried: the omarchy-eval disclaimer carve-out wording is dropped from the page in the same change as the corpus advance. |
| R6 | Git safety — feat/ branch, PR-only, no force-push, no direct push to main; committed tree verified (post-commit status + `git show --stat`). |
| R7 | Gate proof — two full regenerations EXIT 0 on the live tree; the committed tree regenerates to a no-op (drift gate at CI runs the same command). |
| R8 | Deploy-affecting but content-only — site pages change; deploy runs at merge per the repo's workflow. |
| R9 | N/A — no vendor/schema code changes. |
| R10 | Fresh disposable proof — regenerated from a fresh charly checkout at the pinned commit + fresh docs clone at HEAD; second regeneration on the committed tree was a byte-identical no-op. |
| RDD | Doc-driven — generated tree only; no hand-authored content edits. |
| ADE | N/A — no agent-loop/visual grading involved. |
| SDD | N/A — no charly Go/CUE source touched. |
| T4 | N/A — no tests; docs-only change class. |
| B14 | Honest tier — docs regen executed live twice (outputs pasted); tier = analysed on a live system, not elevated to fully-tested because the local reproduction is not the repo's own deploy run (which gates at merge). |
| B15 | N/A — no benchmark/eval claims. |
| B17 | N/A — no security-relevant content. |
| B18 | Scope discipline — only the marketplace submodule + its regenerated page; no other submodule or page touched. |

## Change classification

- Change class: **docs regeneration + marketplace submodule pin**.
- Verification gate: **analysed on a live system** — full CI-identical regeneration ran clean twice on the live tree (outputs pasted; the second run on the committed tree was a byte-identical no-op).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*