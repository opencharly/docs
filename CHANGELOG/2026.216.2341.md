# 2026.216.2341 — a front page with a spine, and the grievances get a home

The landing page had no structure. It went: what it is → a roles table → four commands → four cards
restating the vision → a link list. A reader looking for "how does this actually work" had to
assemble the answer from three non-adjacent sections, and the tenet cards arrived before the
mechanics they are opinions about.

## Three sections, in reader order

**What it is** — two paragraphs. No headings, no tables, no comparison to tooling the reader may or
may not run.

**How it works** — one section, three subsections: [the words](/concepts/00-vocabulary/), the
one-keyword-three-roles table, and the four stages (build · run · deploy · evaluate) as a card grid.

**A real box, end to end** — the four commands against `tutorial-shell`, what the box deliberately
does *not* declare (an init system) and why, and the substrate swap.

Then where to go next.

## The vision is off the front page

The four "The bet" cards restated tenets 1, 2, 4 and 6 of [the vision](/vision/) — the thesis, not
the mechanics. The front page is not where a reader meets the thesis; it links to it instead of
paraphrasing a quarter of it. The freed card grid now carries the four stages, which is what a
reader at that point in the page is trying to learn.

The hero tagline claimed *"Every other tool hands an agent a sandbox and then spends its effort
taking things away."* Nothing verifies an assertion about every other tool. It is replaced by the
project tagline, which describes charly rather than its competitors.

## /grievances/

`GRIEVANCES.md` — four properties of ordinary container and VM practice charly is built to answer,
each with the status quo stated fairly and where the claim stops being true — is now **generated**
into this site by `charly docs generate`, exactly as `VISION.md` is, and sits in the sidebar beside
the vision. It was previously reachable only as a file in the charly repository.

It is a projection, not a copy: the only transformation is dropping the source H1 (Starlight renders
the frontmatter title) and rewriting the two repo-relative links onto site routes.

## Two capabilities the front page never mentioned

**Where a running candybox lives.** Podman and Docker are both first-class, for building and for
running, and the two are chosen independently — charly auto-detects what is installed and either
can be pinned per host, asserted by `TestResolveRuntime_MixedEngines`. On a host with Podman and
systemd a `pod:` deploy is realised as **user-level systemd quadlets**, one `charly-<name>.service`
per deploy carrying its ports, volumes, devices and security settings, started at boot. A deploy
with encrypted volumes starts at boot too and suspends until its key is available — unless there is
no keyring backend to unlock, where there is nothing to wait for and it needs an explicit
`charly start`.

**Any harness, and no agent-only surface.** The CLI *is* the surface, so anything that can run a
command drives every stage; `charly` additionally serves its whole command tree over MCP for any
MCP-speaking client. The framing is the design rather than a roll call of harness names —
`AGENTS.md` is the complete harness-neutral rulebook, and a per-harness file is an adapter of it.

## Two more unverifiable reader claims, elsewhere

Sweeping the site for the phrasings removed from the landing page found the same defect on two
pages nobody was editing, so they are fixed here rather than filed:

- `concepts/06` claimed the probe vocabulary *"will feel familiar"*. It now says it covers the same
  ground, which is a statement about the vocabulary rather than about the reader.
- `start/quickstart` claimed the rebuild step is *"the stage most toolchains do not have"*. Nothing
  establishes what most toolchains have. It now says what the step costs and why `disposable: true`
  is what makes it affordable.

An earlier revision of this entry argued that the `## If you know Docker` sections could keep their
second-person framing, on the grounds that a conditional heading scopes its claims to readers who
opted in. That carve-out is gone along with the sections themselves — see below.

## The home page became a projection

Two thirds of this page was the superproject's `README.md`, copied across a submodule boundary and
maintained twice — under a footnote promising that nothing on the site is a hand-maintained copy.
The copies had already drifted: "Podman and Docker" here against "Podman and docker" there, a table
column present in one and dropped in the other, a claim corrected in one and left stale in the
other.

`index.mdx` is therefore deleted, and `charly docs generate` now emits `index.md` from `README.md`
— the same treatment `VISION.md` and `GRIEVANCES.md` already get. The hero, the Starlight template
choice and the `<title>` override are site machinery and stay with the generator; the prose has one
source, next to the code. **To change the front page, change the README.**

## The "If you know X" sections are gone

All twelve concept pages carried one — `## If you know Docker`, `## If you know goss or
Serverspec`, and so on. They were removed on the operator's instruction, and the reasoning is worth
keeping: "knowing X" can mean several different things, so a section addressed to it cannot say
anything precise. 98 lines.

Their removal also simplifies the rule against reader-state claims, which previously needed a
carve-out for conditional headings. It is now unconditional.

## Everything a cold reader could not follow

A fresh-context reader with no knowledge of the project read both surfaces and reported where
comprehension actually broke. Fixed here and in the README: a self-contradiction (a candy is "not
'a layer'" / a candy IS "a layer", sixteen lines apart, in a section opening "used precisely
throughout"); `bundle` and `deploy` used but never defined; "runs all four in one command" listing
three; a page about a config format that never showed the format; and the headline substrate claim
asserted three times with no deploy declaration anywhere on either surface.
