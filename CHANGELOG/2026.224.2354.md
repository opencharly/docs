# 2026.224.2354 — regenerate the site for the agentteams marketplace family

Regenerate the generated half of the site from the cutover sources after the `agentteams`
marketplace family landed (the cross-repo cutover consuming the superproject's candy `skill:`
entities): ten `reference/candy/agentteams*` pages (minio, matrix, element, higress,
controller, manager, worker, openclaw, the `agentteams` top composition, the
`agentteams-cli` plugin), the `reference/cli/agentteams` word page, the
`reference/plugin/plugin-agentteams` page, and the `recipes/agentteams/*` pages. The
regeneration also absorbs two earlier source drifts the previous snapshot predated — the
`gemini` candy split (`google-cloud`, `google-cloud-npm`, `fedora-coder` recipes +
`google-cloud-npm` reference) and the k8s→kubernetes rename projection (`plugin-k8sgen`,
providers index) — so the generated half and the current superproject sources no longer
contradict each other. Placeholder CalVer — the pr-validator finalizes it at merge.
