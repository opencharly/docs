# 2026.254.1011 — fix(docs): regenerate against the CI-equivalent environment (marketplace gitlink 5b6833a + docs-corpus wiring + fresh refs cache)

## Summary

Regenerates the site exactly as the deploy pipeline does, fixing the drift gate that failed on docs#115 (the merged tree was generated in a NON-CI-equivalent environment):

- **marketplace submodule at the docs-recorded gitlink `5b6833a3`** (not main-HEAD `44fa7db`) — the recursive checkout's actual pin.
- **docs-corpus wiring**: the deploy writes `candy/docs-corpus/charly.yml` into the charly checkout from `.github/docs-corpus/pins.tsv` (66 refs) before generating — now replicated; without it the corpus-referenced candy pages cannot resolve.
- **fresh refs cache** (~/.cache/charly/repos purged): the local resolution previously served stale `repo@tag` content, shrinking the walk (committed `reference/cli/validate.md` etc. lacked the multi-owner paragraphs CI emits).
- Result: rc 0, whole-site gates PASS; the CI marker the failed gate demanded is now present (`served by 2 plugin candies` ×2 in validate.md; 47 multi-owner pages); providers.md = 166 words across 175 plugin candies, 121 compiled.

## How tested

From the pushed tree (branch feat/drift-fix @ 0ec29ad...):

```
$ /tmp/pd-bin generate --out ... --root /tmp/charly6 --plugins /tmp/market6   (plugin-docs v2026.254.0559)
charly docs: 968 recipe pages, 175 plugin pages (166 provider words), 60 cli pages, 669 candy pages, 127 box pages
GEN_RC=0  (link + sidebar gates PASS)

$ second warm-cache generation — byte-identical (diff -rq empty)   # drift no-op proven
```

- validate.md carries the multi-owner paragraph (marker ×2); 47 CLI pages present the multi-owner sentence.
- providers.md: 324 rows / 175 plugin candies / 121 compiled — the compiled-corpus census the gate expects.
- Delta vs origin/main: 165 files, +8178/−324, all under `src/content/docs` (multi-owner paragraphs + provider-index reorders + corpus-wiring pages).

## Rulebook compliance

- R1 — the drift-gate failure was RCA'd to three environment-fidelity gaps (marketplace gitlink, docs-corpus wiring, refs-cache staleness); each reproduced CI's exact invocation and fixed in the same landing; no workaround, no drift-gate bypass.
- R3 — the deployment owns generation (deploy.yml `--root`/`--plugins`/`--out` replicated verbatim); the committed tree is a projection of that single pipeline.
- R6/Git — no force-push, no amendments; single clean commit; PR-only landing via the org validator.

## Change classification

- **Change class:** docs content regeneration (environment-fidelity correction).
- **Verification gate:** the deploy drift gate itself — regeneration on the merged tree must be a no-op (proven locally; the CI run is the gate).
- **Attribution tier:** analysed on a live system — CI-equivalent generation ran live with retained output; the deploy pipeline (drift gate → Astro build → Cloudflare Pages publish) runs at merge.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*