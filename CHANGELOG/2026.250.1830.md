# 2026.250.1830 — feat(corpus): refresh omarchy-eval + add omarchy-eval-golden recipe pages (marketplace@30b1c459a2)

## Summary

Marketplace publication wave (plan PR7), Stage 3 of 3: regenerate the docs site's generated half after opencharly/marketplace#290 (merged as `30b1c459a2`) advanced the corpus for `layer-charly-distros` v2026.250.1759 (opencharly/layer-charly-distros#6 updated the `omarchy-eval` skill entity + added `omarchy-eval-golden`).

- **Marketplace submodule gitlink** bumped to `30b1c459a2` (2 +-1) — the corpus the recipe pages project from.
- **Regenerated pages** (`charly docs generate`, CI-identical invocation):
  - `src/content/docs/recipes/distros/omarchy-eval.md` — rewritten (golden chain beds, oracle-generated per-PR eval + RED-PROBE twin beds, pi-agent lane, `eval/PR-EVAL-LANE.md` binding contract; retired bed names removed).
  - `src/content/docs/recipes/distros/omarchy-eval-golden.md` — NEW recipe page.
  - `src/content/docs/recipes/index.md` — 912 → 913 cards; new list entry.
- Everything else in the generated tree regenerated byte-identically (3 changed files + the gitlink, nothing else).

## How tested

Pasted outputs (branch `feat/omarchy-eval-corpus`):

```text
$ git diff --cached --stat
 marketplace                                        |   2 +-
 src/content/docs/recipes/distros/omarchy-eval-golden.md    |  67 +++++++++
 src/content/docs/recipes/distros/omarchy-eval.md   | 152 ++++++++++++---------
 src/content/docs/recipes/index.md                  |   3 +-
 4 files changed, 154 insertions(+), 70 deletions(-)

$ git diff --cached --check  →  no output (clean)

$ cd /tmp/charly && /tmp/plugin-docs-bin generate --root /tmp/charly --plugins /tmp/docsr/marketplace --out /tmp/docsr/src/content/docs   (plugin-docs CLI built at v2026.242.0527; charly checkout at the repo's pinned commit 9d9ee1b3 with box/* submodules; docs-corpus wiring candy from .github/docs-corpus/pins.tsv)
GEN_EXIT=0 — 966 recipe pages, 116 plugin pages, 54 cli pages, 608 candy pages, 138 box pages; 1269 generated pages cleared before regeneration

$ grep -c 'check-omarchy-suite-pod\|check-omarchy-pr-vm-visual' src/content/docs/recipes/distros/omarchy-eval.md src/content/docs/recipes/distros/omarchy-eval-golden.md
src/content/docs/recipes/distros/omarchy-eval.md: 0
src/content/docs/recipes/distros/omarchy-eval-golden.md: 0

$ git status — only the 3 regenerated files + the staged marketplace gitlink; every other generated page was a byte-identical no-op.
```

Regeneration exactly mirrors the repo's deploy.yml steps (charly at the in-workflow pinned commit, plugin-docs at the pinned tag, wiring pins from .github/docs-corpus/, `--plugins` = the marketplace submodule checkout). The drift gate at CI runs the identical command and will reproduce this tree.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | RCA/scope audit: only the omarchy-eval + omarchy-eval-golden pages and the skill index changed after a full-tree regeneration — proof the corpus move is a no-op for every other source (generated-tree diff audit pasted). |
| R2 | No conflict — recipe pages are generated (DO-NOT-EDIT); no hand-edited page overwritten. |
| R3 | No duplication — pages are projections of the marketplace corpus; the docs PR does not restate content. |
| R4 | No workarounds — standard CI-identical `docs generate` invocation; no hand-edits to generated files. |
| R4a | Submodule gitlink bumped to a MERGED marketplace commit (30b1c459a2) — pin discipline; no dangling refs. |
| R5 | Legacy removal carried: the old omarchy-eval page (retired bed set) is replaced in the same change; stale names grep = 0. |
| R6 | Git safety — feat/ branch, PR-only, no force-push, no direct push to main. |
| R7 | Gate proof — full regeneration EXIT=0 on the committed tree; drift gate at CI is the same command (repo deploy workflow). |
| R8 | Deploy-affecting but content-only — site pages change; no data/system state; deploy runs at merge per repo workflow. |
| R9 | N/A — no vendor/schema code changes. |
| R10 | Fresh disposable proof — regenerated from a fresh charly checkout at the pinned commit + fresh docs clone at HEAD; committed tree verified (git diff --cached). |
| RDD | Doc-driven — generated tree, no hand-authored content edits (start/ concepts/ guides/ untouched). |
| ADE | N/A — no agent loop/visual grading involved. |
| SDD | N/A — no charly Go/CUE source touched. |
| B14 | Honest tiers — docs change class; the pipeline ran fully (regen EXIT=0) but the local run is a manual reproduction, so tier = analysed on a live system, not fully-tested-and-validated. |
| B15 | N/A — no benchmark/eval claims. |
| B17 | N/A — no security-relevant content. |
| B18 | Scope discipline — only the marketplace submodule + its regenerated pages; no other submodule or page touched. |

## Change classification

- Change class: **docs regeneration + marketplace submodule pin**.
- Verification gate: **analysed on a live system** — full CI-identical regeneration ran clean on the live tree (outputs pasted); tier not elevated to fully-tested because the local reproduction is not the repo's own deploy run (which gates at merge).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*