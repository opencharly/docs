# 2026.227.1715 — mirror the git-workflow landing-mechanics lessons

Projects `opencharly/plugins#176` (merged `4458a400`, tag `v2026.227.1458`) onto
the site. Three pages move and nothing else does.

- `recipes/internals/git-workflow/branch-and-pr-loop.md` — two additions.

  At the delta-re-gate bullet that prescribes `gh pr update-branch`: that merge
  can pull a new builder-chain input into the chain a bed's fixture image builds
  from, so **re-running the bed is half the remedy and re-pointing the citations
  is the other half** — nothing in the re-gate updates the artifacts that CITE
  the superseded run, and it is the CHANGELOG that ships inside the commit the
  evaluator tags. Scope: block on what gets TAGGED (the CHANGELOG entry and the
  merged PR body are both durable), report what gets squashed away.

  At the gitlink-verification passage: the `docs` gitlink has a twin in candy
  config that no gitlink check can see, because `candy/docs-site/charly.yml`
  records the same sha twice as ordinary text and a merge resolves that
  line-wise. The gitlink checks catch a backwards gitlink; `task docs:pin`
  catches the gitlink and the candy pins disagreeing and passes silently when
  both regressed together. Neither substitutes for the other.

- `recipes/internals/git-workflow/multi-repo-coordination.md` — B2 gains the
  boundary of the pointer-bump attribution grant: **a change class must be
  decidable from the diff.** A gitlink is a pointer git resolves inside the repo
  the PR is against; a sha in a candy `var:` resolves nowhere, so "the referenced
  content is documentation" would make the class a property of a tree the PR does
  not contain. Generalized to any pin the diff cannot resolve, and closing with a
  default that fails safe: when the diff cannot decide, the class is the heavier
  one. B6a step 4 also gains the positive half it lacked — the `DOCS_REF`
  re-pin's gate is `charly box validate` plus `check-docs`, at `fully tested and
  validated`.

- `recipes/tools/docs-site.md` — a pointer from the pin contract to that class
  rule, at the point of use, with the rule keeping one owner.

## Generated at `plugins` `4458a400`, not at the `plugins` tip

Deliberate, and the reason is the same one that governs the superproject pin.
The tip carries a later merge whose superproject source has not landed in any
superproject tree; generating there would project a page whose source does not
exist yet, and generating at anything older would revert the previous docs leg's
work. `4458a400` is this cutover's own merged HEAD and the sha the superproject
leg pins.

Skill prose is a two-hop projection read from the `plugins/` tree rather than
from `candy/*/charly.yml`, which is why this leg needs only the merged `plugins`
commit and not the candy source that produced it — the source lands in the
superproject leg.
