# 2026.229.9999 — box load, nested-podman-socket, and six CLI pages that were never generated

<!-- PLACEHOLDER CalVer: the pr-validator regenerates this stamp at merge. -->

Regenerated by `charly docs generate` / `task docs:sync` from the candy sources.

New: the `charly box load` CLI reference and the `nested-podman-socket` candy reference +
recipe card.

Also emitted for the first time: `build`, `labels`, `merge`, `pull` and `reconcile`.
Those pages had never been generated because `candy/plugin-box`'s `providers:` list — which
the generator reads — omitted the words. Fixing that list in the same cutover restored them.

(An earlier draft of this entry also claimed the change "dropped the stale `pkg` page". It
does not: `pkg.md` is absent from `docs/main` AND from this branch, and this diff never
touches it — `docs#71` removed it before the merge base. The PR body was corrected first
and this sibling was missed, which is the two-surfaces shape this cutover has now paid for
several times: one claim, two artifacts, one fixed. The habit that prevents it is to grep
the CHANGELOG for the same claim in the same pass as the body edit.)
