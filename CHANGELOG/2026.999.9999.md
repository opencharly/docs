# 2026.999.9999 — codify the docs CHANGELOG convention + resolve the git-workflow doc tensions

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edits via `charly docs generate`. This leg carries the site's own CHANGELOG entry — the very convention being codified — since `docs` is now named, alongside the superproject, `box/<distro>`, and `plugins`, as a no-CHANGELOG-exception repo on every landing. Five recipe pages changed, mirroring the `plugins` skill pages:

- `recipes/internals/git-workflow/validator-and-calver.md` — the CalVer/CHANGELOG section now states that every `docs` landing carries a `CHANGELOG/<YYYY.DDD.HHMM>.md` entry exactly like the superproject, `box/<distro>`, and `plugins`, with the proof (24 of the last 25 docs first-parent commits carry one; the single exception `a252df9` forced follow-up docs PR #46).
- `recipes/internals/cutover-policy.md` — the "Required for EVERY landing" CHANGELOG bullet and the "Land docs-only" step both name `docs` as no-CHANGELOG-exception, and the docs-only landing sequence gains the `docs` commit (with its own entry) as a named leg.
- `recipes/internals/git-workflow/branch-and-pr-loop.md` — the concurrent-landings note states the org-wide branch-protection reality (`branch-protection.sh` applies `strict: true` + the single `charly/pr-validator` context to every active `main`-default repo — 14 today) instead of the stale "all three repos set `strict: true`" count.
- `recipes/internals/git-workflow/multi-repo-coordination.md` — submodule-pointer-bump safety is re-anchored to the per-session worktree flow (`git worktree add .claude/worktrees/<slug> -b feat/<slug> origin/main`, B1 step 0) instead of the retired shared-tree `git switch -c feat/<slug>` authoring example; B3's title is scoped to the orchestrator's shared tree so it no longer reads as contradicting one-worktree-per-session.
- `recipes/internals/skills.md` — the skills-skill "Land docs-only" audit step mirrors the cutover-policy naming of the `docs` leg.

This is the `docs` leg of a cross-repo docs cutover (plugins → docs → superproject). Regeneration is a no-op on a clean tree and the whole-site link gate passes. Placeholder CalVer — the pr-validator finalizes it at merge.
