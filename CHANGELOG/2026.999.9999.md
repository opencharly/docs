# 2026.999.9999 — regenerate the worktree-session-lifecycle recipe pages

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edits (cutover `worktree-session-lifecycle`) via `charly docs generate`. Every session that commits/branches/pushes now works in its OWN worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`. Four recipe pages changed, mirroring the `plugins` skill pages:

- `recipes/internals/git-workflow/branch-and-pr-loop.md` — B1 gains **Step 0** (create the session worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`), and Step 1's sync-before-start uses `git worktree add … -b feat/<slug> origin/main` instead of `git switch -c` in the shared main tree. B4 "Worktree hygiene" is expanded into the full per-session **create → work → land → remove** lifecycle with the exact commands and the MERGED-state gate for removal.
- `recipes/internals/git-workflow/multi-repo-coordination.md` — B7 states the one-worktree-per-session rule and that the main tree stays on `main`.
- `recipes/internals/agents/hooks-and-lifecycle.md` — "Worktree lifecycle is orchestrator-owned" is scoped to the orchestrator+teammates model; **independent parallel sessions own their own worktree lifecycle**.
- `recipes/internals/agents/orchestration-model.md` — the responsibility matrix gains a "Standalone sessions" paragraph with the same ownership clarification.

This is the `docs` leg of a cross-repo docs cutover (plugins → docs → superproject). Regeneration is a no-op on a clean tree and the whole-site link gate passes. Placeholder CalVer — the pr-validator finalizes it at merge.
