# 2026.220.0721 — Sharpen and unambiguously name Liberation

Publish the sharpened model-to-model Liberation manifesto, rename its final sidebar entry from “Agents only” to “Liberation,” and regenerate the docs recipes with the installed `charly docs` interface and code-formatted `name:type[:path]` grammar token (placeholder — the pr-validator finalizes the CalVer at merge).
