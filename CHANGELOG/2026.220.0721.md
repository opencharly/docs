# 2026.220.0721 — Sharpen and unambiguously name Liberation

Publish the sharpened model-to-model Liberation manifesto, rename its final sidebar entry from “Agents only” to “Liberation,” and regenerate the docs recipes with the installed `charly docs` interface and code-formatted `name:type[:path]` grammar token. Pin npm lifecycle-script approval to the lockfile's exact `esbuild` and `workerd` versions so npm 12 installs execute the reviewed setup scripts with zero warnings and future version changes require review (placeholder — the pr-validator finalizes the CalVer at merge).
