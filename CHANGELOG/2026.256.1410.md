# 2026.256.1410 — docs: bump the pinned charly and regenerate the site

## Summary

The docs site is generated against the charly commit pinned in `.github/workflows/deploy.yml`, which still pointed at `d507ca5a` — **before** the README/rulebook corrections landed on charly main (charly#604, #607, #608). The live home page therefore still showed the stale `opencharly/charly/candy/*` refs.

This PR bumps the pin to charly main **`213e0945`**, updates the adjacent pin comment, and commits the regenerated tree:

- `index.md` (projected from `README.md`) now carries the owning-repo refs (`layer-ripgrep`, `pod-sshd`, `layer-charly`); no `charly/candy/*`.
- compiled-plugin reference pages (`plugin-build`, `plugin-check`, `plugin-loader`) advance to the new pin's `go.mod` versions.
- `reference/providers.md` and `reference/cli/check.md` update.

## How tested (literal transcript — the deploy recipe, run locally)

```
$ git -C /tmp/charly checkout 213e0945d19f3364d97ba22fbdf08b686bd92ab8
$ (cd /tmp/charly/charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=docs-ci" -o /tmp/charly/bin/charly .)
$ python3 build_wiring.py .github/docs-corpus/pins.tsv /tmp/charly/candy/docs-corpus/charly.yml
corpus wiring refs: 65
$ cd /tmp/charly && /tmp/plugin-docs-bin generate --root /tmp/charly \
    --plugins /tmp/opencode/docs-pin/marketplace \
    --out /tmp/opencode/docs-pin/src/content/docs
charly docs: 968 recipe pages, 174 plugin pages (165 provider words), 60 cli pages, 668 candy pages, 127 box pages (1382 generated pages cleared before regeneration)
$ grep -c 'charly/candy' /tmp/opencode/docs-pin/src/content/docs/index.md
0
$ grep -oE 'opencharly/(layer-ripgrep|pod-sshd|layer-charly)' /tmp/opencode/docs-pin/src/content/docs/index.md | sort -u
opencharly/layer-charly
opencharly/layer-ripgrep
opencharly/pod-sshd
```

**Reconciling the counts with the 10-file diff.** The generator's per-category line counts projection
UNITS, not on-disk files — `968 recipe pages` is the recipe-card (skill) unit count, while
`recipes/**` holds 356 files. The file-level equation closes on the measured on-disk generated set:

```
$ find src/content/docs -type f \( -name '*.md' -o -name '*.mdx' \) | wc -l
1409
$ n=0; while IFS= read -r f; do if grep -qm1 'DO.NOT.EDIT' "$f"; then n=$((n+1)); fi; done \
    < <(find src/content/docs -type f \( -name '*.md' -o -name '*.mdx' \)); echo "$n"
1390
$ for d in recipes reference/plugin reference/candy reference/cli reference/box; do \
    printf '%-18s %s\n' "$d" "$(find src/content/docs/$d -type f -name '*.md' | wc -l)"; done
recipes            356
reference/plugin   174
reference/candy    668
reference/cli      60
reference/box      127
# 356 + 174 + 668 + 60 + 127 + 5 (index/vision/grievances/liberation/providers) = 1390 generated files
$ git diff --name-only origin/main..HEAD | wc -l
10
```

So: **1390 generated files on disk**; only **10 files' bytes changed** by this PR — 3 compiled-plugin
version bumps (each a delete+add dir pair), `index.md`, `cli/check.md`, `providers.md`, and the
`deploy.yml` pin+comment. The generator's `1382 … cleared` is its own pre-clear file count; the
per-category totals it prints are unit counts and do not sum to a file count.

Live pre-bump check (why this PR is needed):

```
$ curl -sL https://opencharly.ai/ | grep -oE 'opencharly/charly/candy/[a-z]+' | sort -u
opencharly/charly/candy/charly
opencharly/charly/candy/ripgrep
opencharly/charly/candy/sshd
$ curl -sL https://opencharly.ai/reference/providers/ | grep -c plugin-group
0
```

## Rulebook compliance

- **R0** — the docs skill (`/charly-build:docs`) and git-workflow skill were loaded before acting.
- **R1** — the stale live home page was traced to the pin lag (not a content bug).
- **R2** — no parking; this is the producer→consumer step for the merged charly corrections.
- **R3** — no duplication; the pin is the single source the generator reads.
- **R4** — no ad-hoc workaround; the pin is bumped through the repo's own deploy mechanism.
- **R4a** — `index.md` carries commands readers run; the corrected README keeps them binary-only (`--repo`/no clone); nothing here interpolates a repo-relative path.
- **R5** — the superseded pin is swept from the adjacent `deploy.yml` comment in this same commit.
- **R6** — `feat/` branch + PR.
- **R7** — N/A for the pin text itself; the changed surface (the generated tree) is proven by the live generator run above.
- **R8** — the emitted generated tree is committed and verified (`grep` above).
- **R9** — the pin is bumped to a **merged** charly ref (default-branch `213e0945`), never a PR branch.
- **R10** — N/A — no runtime path; the docs generator is the only execution and it ran live (output above); the CI `deploy` drift gate is the full gate.

## Change classification

- Class: docs + generated projection + CI pin (config).
- Gate: the docs generator ran live with the exact deploy recipe; the CI `deploy` drift gate is the full gate.
- Confidence: **analysed on a live system**.

*Assisted-by: opencode deepseek-v4.1-flash (analysed on a live system)*