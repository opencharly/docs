# 2026.230.0045 — the site's section partition closes

**This is a REGENERATION, not an authored edit.** The page carries
`DO-NOT-EDIT — generated by `charly docs generate``; its source is the skill corpus at
`opencharly/plugins@806da020` (`v2026.230.0039`), itself projected from a candy entity in
`opencharly/charly#289`.

## What arrives

`recipes/internals/git-workflow/evidence-and-freshness.md` — the mechanism grouping names five
sections rather than four.

The page has thirteen `###` sections. The epistemic grouping names eight; the mechanism grouping
named four; *"A guard that cannot fail is worse than no guard"* was in neither. **Both groupings
enumerate their members by name**, which makes the shortfall a false claim rather than an incomplete
one — a count leaving room for unnamed members undercounts, while a list naming its members asserts
*these are all of them*.

**This is the third projection of that correction, and the three are in different states.** The
source change is on `opencharly/charly#289`, which is **open**; the `plugins` artifact landed as
`plugins#201`; the published site catches up here. Saying all three "landed" would assert of an open
branch what is true only of a merged one — the defect this page's own guard section catalogues. **A generated page is
not fixed until every projection of it carries the fix**, and the site is the projection a reader
actually meets.

## Scope

Eight further pages drift at this base — `recipes/internals/{go/source-map,skills,vm-spec}.md`,
`recipes/vm/{arch-cloud-vm,vm,vms-catalog}.md`, and both `plugin-kube` reference pages. **None is
caused by this leg**, verified causally: `204ff55c` is the direct single parent of `806da020` and
that span is two files, so `plugins#201` could not have touched them.

**What they are, measured rather than deferred.** Five — `recipes/internals/{skills,vm-spec}.md` and
`recipes/vm/{arch-cloud-vm,vm,vms-catalog}.md` — project from skill sources that are byte-identical
at `charly/main`'s own `plugins` pin (`c061fee6`), so they are **un-regenerated backlog with no open
PR covering them**, not another cutover's work in flight. One, `recipes/internals/go/source-map.md`,
does trace to a later `plugins` leg. The two `plugin-kube` reference pages are one-hop projections of
a candy and have no `plugins` source at all.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
