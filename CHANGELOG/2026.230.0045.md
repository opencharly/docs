# 2026.230.0045 — the site's mechanism grouping drops both its universals

Projection of `opencharly/charly#289` via `plugins#202` (merged `c4aa2219`, tagged
`v2026.230.0217`). One page — `recipes/internals/git-workflow/evidence-and-freshness.md` —
`+4/-4` against `docs/main`, which is what this PR lands.

The grouping closed *"They are not instances of the question **and no frame unifies them**."*
Both clauses are removed.

*"No frame unifies them"* was falsified by `charly#289`'s own edit, which added a second member
stating the validator-status frame. *"They are not instances of the question"* needed no append — a reader falsified
it by reading. Deleted rather than re-qualified: the positive definition and the
checked-by-running test already carry the split.

**The vacuous-guard section stays in the mechanism grouping**, whose membership is otherwise unchanged.

Two adjacent fixes: a `###` heading sat flush against the paragraph above it, and *"The same key
produced both a miss and a false positive"* quoted two different keys, so the identity claim is
dropped rather than guessed.

## Scope

Eight other pages are **stale** — they project from sources unchanged by this leg, so nothing
here caused them. Routed in `opencharly/charly#303`.

*Assisted-by: Claude Code Anthropic Claude Opus 5 (documentation reviewed)*
