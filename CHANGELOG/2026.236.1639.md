# 2026.236.1639 — docs: make the card promises true where the candy lives elsewhere

## Summary

Makes three on-site promises true again after #100 moved the `ripgrep` teaching example
off-site. Blocking findings from the independent verdict on #100
(https://github.com/opencharly/docs/pull/100#issuecomment-5398263437) — all three survive
on main today:

- `concepts/00-vocabulary.md:96` — "All three are real and shipped, and you can read each
  one on this site" was falsified two lines below by the layer example linking
  `github.com/opencharly/layer-ripgrep`. Now: "All three are real and shipped:".
- `concepts/03-every-piece-has-a-card.md:41` — the page titled *Every piece has a card*
  demonstrated its thesis with a repo link. Now it says the card exists **wherever that
  candy lives**, keeping `sshd` and `supervisord` as on-site cards and naming `ripgrep` as
  living in its own repository — which is the actual mechanic (`charly docs generate`
  projects each project's own candies).
- `guides/authoring-a-candy.md:85` — "published verbatim as that candy's page" now reads
  "as that candy's card wherever the candy's project is published".

Card links are deliberately **not** restored: the ripgrep candy left charly in `894fa0af`
(#384), so its skill is gone from marketplace `main` and its card has no source to come
back from — the honest fix is prose that matches where the candy actually lives.

**Breaking change:** none. **Rollback:** revert the merge commit.

## How tested

Drift gate reproduced locally at this repo's pins (charly `57167066`, marketplace
`e8efdb1`) on the final state, before the push:

```
$ cd charly && ../bin/charly docs generate --root . --plugins ../marketplace --out ../src/content/docs
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 345 candy pages, 82 box pages (911 generated pages cleared before regeneration)
exit: 0
$ git status --short          # after regeneration: exactly the three edited pages
 M src/content/docs/concepts/00-vocabulary.md
 M src/content/docs/concepts/03-every-piece-has-a-card.md
 M src/content/docs/guides/authoring-a-candy.md
```

Claim-class sweep (on-site/page promises in the hand-authored trees):

```
$ grep -rn "on this site\|as that candy's page" concepts guides start
concepts/03-every-piece-has-a-card.md:20   (the projected catalog — true)
concepts/07-the-schema-comes-first.md:24,52 (parameter tables / catalog — true)
concepts/10-the-cookbook-never-lies.md:53  (tutorial-shell, keeps its card — true)
guides/authoring-a-candy.md:39             (example commands — true)
```
Each surviving hit was read in context: none concerns the ripgrep example, and
`tutorial-shell` keeps its card. Remaining `ripgrep` mentions in these trees are YAML
content (`ripgrep:` entity, package name, `rg --version`), not page promises.

## Rulebook compliance

- **R0** — git-workflow + docs skills followed (hand-authored trees only; generated pages
  untouched, re-proved by the no-op regeneration).
- **R1** — the divergence is the #100 verdict's finding; no remediation before RCA.
- **R2** — nothing deferred: all three sites fixed here.
- **R3** — one consistent formulation of "where the card lives" across the three pages.
- **R4/R4a** — the product is not routed around: the candy genuinely lives elsewhere, so
  the prose states that rather than papering over a dead link.
- **R5** — claim-class sweep pasted above, keyed to the promise class, not one string.
- **R6** — fresh branch off main, one commit, no force-push.
- **R7–R10** — documentation-only class: non-runtime standards; the drift gate + astro
  build are the coverage, run on this head.

## Change classification

Documentation-only (three hand-authored teaching pages, prose only). Gate: drift-gate
reproduction above + this head's CI. Tier: **documentation reviewed**.

*Assisted-by: Claude Code Anthropic Claude Fable 5 (documentation reviewed)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn