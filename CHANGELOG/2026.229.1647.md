# 2026.229.1647 — the drift block names its tree, and the message it quotes stops overclaiming

**This is a REGENERATION, not an authored edit.** Every page below carries
`DO-NOT-EDIT — generated by `charly docs generate``; their sources are
`candy/charly-internals/charly.yml` (reaching here through its `plugins` projection) and
`candy/plugin-marketplace/charly.yml`, both in the `charly` repo, landing as
`opencharly/charly#289`.

This entry states what the site now says. The review that produced it lives in `opencharly/docs#68`
and in the `plugins` legs that carried the skill prose — `#188`, `#190`, `#193`, `#194` — each
reachable as `refs/pull/<N>/head` after its branch is deleted.

## What arrives

| Page | Hops from source |
|---|---|
| `recipes/internals/git-workflow.md` | two — `candy → plugins SKILL.md → docs recipe` |
| `recipes/internals/git-workflow/evidence-and-freshness.md` | two — same chain, reference page |
| `reference/candy/plugin-marketplace.md` | one — the candy `description:` |
| `reference/cli/marketplace.md` | one — same |
| `reference/plugin/plugin-marketplace.md` | one — same |

## Why

`charly marketplace drift` reported success as `clean (N artifact(s) match the committed tree)`. It
compares nothing of the sort: both sides are the working tree — the sources on disk, regenerated in
memory, against the artifacts on disk — and git is never invoked in the call chain. The message
asserted a claim the command has no mechanism to establish, and it did so most confidently in the
case that matters: a tree carrying an unfinished cutover's two halves, where `git status` says dirty
and `drift` says clean. It now says what it compared:

```
marketplace drift: clean (400 artifact(s) on disk match their sources; regeneration is a no-op)
```

The recipe page is rebuilt around that distinction — a six-run transcript at one named tree in which
every state it asserts is a state it runs, reverts included — and carries three mechanism
corrections earned in review: the orphan scan is **path-gated** (three arms, only the first reads
bytes), `git status` distinguishes **three** dirty-submodule states rather than two, and `(stale)`
marks an orphan rather than reading backwards.

## What the two `git-workflow` pages now say

Both reach the site through the skill projection, and both carry corrections the `plugins` review
produced rather than the original draft:

- **The `git show --stat` rule is narrower than it was.** It now claims to be the only post-commit
  check **on the committing side** that sees a commit whose message describes an intent its diff does
  not carry. The premise establishes only that no *tree-reading* gate can — the tree does not hold the
  message — and a reader holding message and diff together catches the same class, including a
  mismatch spanning two repositories, where `--stat` structurally cannot look.
- **Chaining the edit to the commit is stated as necessary, not sufficient.** Joining `edit && git add
  … && git commit …` stops an edit that fails *loudly* from reaching a commit; `&&` short-circuits on
  a non-zero exit, so a **silent** no-op edit exits 0 and lands the commit under a describing message
  exactly as before. That case still needs the post-commit read.
- **Three dirty-submodule states, and `--porcelain` collapses all three** — where the page previously
  said "both" in two places, one of them understating its own point.

## Scope, and why it is scoped

Generated against `plugins@afa69e7e` (`v2026.229.1619`) — the last `plugins` leg of this cutover —
with the generator's input and the landed projection at that commit. A recipe page is a projection
**of a projection**, so `docs` cannot be current unless `plugins` is regenerated and landed first.

**The commit is deliberately narrower than the regeneration**, and the reason is stronger than
scoping. Regenerating at that base also touches three `recipes/ollama/*` pages and the
`recipes/index.md` card count, projected from skills that `plugins#191` landed at 11:15:36Z —
including an `ollama-cli` skill whose superproject source is **not on `charly` main, not in any open
PR, and not on any pushed branch**. Committing them would publish a recipe page for a candy that
exists in no landed tree and no proposed one.

The revert set was **re-derived at this tree rather than carried forward** from the previous
regeneration, and it differed in kind: no page was deleted this time, and `recipes/ollama/ollama-cli.md`
arrived as an **untracked addition** rather than a modification — so it needed removing rather than
reverting, which the earlier list would not have done. A revert set is a measurement of one tree, not
a property of the cutover.

**No leg currently owns them.** That is a producer orphaned by the projection-first ordering: the
`plugins` half landed and the `charly` half has not been proposed, so no `docs` regeneration can
legitimately pick those pages up until someone lands the source. It is recorded here rather than
routed, because there is no owner to route it to; the next `docs` leg to regenerate will meet the
same three pages and should not absorb them either.

**A clean whole-tree `git status` is not available to this PR** — that check lapsed the moment a
foreign projection landed in the base, and reaching for it anyway is how a regeneration silently
claims someone else's work.

Verified by reading the emitted artifact rather than inferring it from the source: every difference
between the recipe page and its `plugins` source is one the generator is specified to make — the
frontmatter, the DO-NOT-EDIT header, the recipe-card subtitle, and four cross-reference rewrites.
Nothing else differs.

The superproject's pins do **not** yet point here, and cannot until this lands: at `charly#289`'s head
the `plugins` gitlink is `a8b51b16` and the `docs` gitlink is `c641b99e`. That one cutover advances
both — `plugins` to `afa69e7e` and `docs` to this merge — after this entry is stamped.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match the
minted version. **Verify that rewrite by reading the committed object** (`git show <ref>:<path>`),
never the working tree.
