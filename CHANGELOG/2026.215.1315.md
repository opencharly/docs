# 2026.215.1315 — Regenerate after the plugins skill corrections

Generated content only, picking up `opencharly/plugins` `v2026.215.1307` (`aae11a7`).

## What changed on the site

`recipes/build/docs.md` — the published `/charly-build:docs` card carried a superseded claim about
what happens to harness cross-references that live in a skill's frontmatter. It said the 11 such
references are "published as plain subtitle text". They are published **nowhere**: the generator
strips frontmatter before rewriting bodies, and `firstLine` then truncates the description to its
first sentence for the page subtitle, which drops all 11.

The card now states the full accounting, which took four review rounds to get right:

| | count | fate |
|---|--:|---|
| references in the skill files the generator reads | 3665 | — |
| in skill **bodies** | 3654 | rewritten into site links |
| in `SKILL.md` frontmatter `description:` | 11 | dropped by `firstLine`; published nowhere |

All 11 resolve, so nothing on the site is broken — that is *why* nothing is broken, rather than
the reason the card previously gave.

## Why a regeneration gets its own entry

It mints a release tag, and this repo pairs one CHANGELOG file to one tag. I argued the opposite
when opening the PR — that a pure projection carries no claim of its own — and review overruled
it, for the second time in this cutover on the same reasoning. The rule is simpler than my
argument: a landing that mints a tag mints a release, and a release is recorded.

The substantive claim also *does* belong to this repo, not only to `plugins`: the sentence a
reader sees is the one published here.
