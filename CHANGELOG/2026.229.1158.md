# 2026.229.1158 — regenerated: emitted-shell + apk claims, six codified findings, the Debian distro trap

Placeholder CalVer (the pr-validator finalizes it at merge). Chosen after the newest entry in this
repo, with no filename or tag collision.

One entry for one squash-merge. An earlier revision of this branch carried two CHANGELOG files and
left its third commit uncovered; a PR that lands as a single commit gets a single entry, so they
are consolidated here.

Every page in this change is the **second projection hop**: a candy's `skill:` entity in the
superproject is projected to `plugins/**/SKILL.md` by `charly marketplace generate`, and that is
projected here by `charly docs generate` / `task docs:sync`. Nothing was hand-edited; these pages
carry DO-NOT-EDIT banners, and the fix for anything wrong on them belongs in the candy source.

## 1. The emitted build step no longer names bash

`recipes/build/generate.md`'s per-verb emission table described `bash` as the interpreter for the
`download` and `cmd` rows; the Containerfile emitter stopped hardcoding it (see the sdk's entry for
this CalVer). The `cmd` row was stale a second, independent way — it uses a heredoc, not ANSI-C
`$'…'` quoting — and the trailing sentence explaining the ANSI-C form went with it. Both forms were
read off a freshly emitted Containerfile rather than inferred. A new paragraph defines
`<shell-probe>` so the table stands on its own.

`apk` joined the format enumerations in `recipes/build/generate.md`,
`recipes/internals/generate-source/task-emission.md` and `recipes/image/image.md`.

`charly candy add-apk` joined the command rows in `recipes/image/image.md`,
`recipes/image/layer.md` and `recipes/build/charly-mcp-cmd.md`. Those rows enumerate **commands**,
not formats — the same token answering a different question — so they were checked against the CLI
rather than pattern-matched, and updated only because the command now exists.

## 2. Six findings codified into their owning skills

- **`recipes/internals/skills.md`** — "How to Update" no longer opens by telling the reader to edit
  `plugins/<plugin>/skills/<name>/SKILL.md`, a generated file carrying a DO-NOT-EDIT banner; that
  instruction manufactured the very error it should prevent. Plus a section on regenerating only the
  projections of the sources you edited, because a projection merges before the source that pins it
  and a wholesale regeneration reverts landed prose.
- **`recipes/check/check/authoring-gotchas.md`** — gotcha 10 extended to `vm:` beds, to the fact
  that `sudo` does not cover a shell redirect, and to where `sudo` comes from per substrate; new
  gotcha 13 (a step is SIGKILLed at the 90s never-hang floor unless it declares `timeout:`) and new
  gotcha 14 (a check must be able to fail for the reason it names — four measured ways one passed
  while measuring nothing).
- **`recipes/build/generate.md`** — an unrenderable package install is a hard error rather than a
  dropped step, with the two authoring consequences that produced the original defect.

## 3. The Debian-family `distro:` trap

`recipes/internals/vm-spec.md` gained the omission trap: on a Debian-family cloud image, leaving
`source.distro` unset rendered `openssh` plus `systemctl enable --now sshd` — a unit Debian and
Ubuntu do not have — and with cloud-init's deliberate `mask ssh.socket` that left a
socket-activated sshd masked and never started. The guest booted fully and served nothing.

## Scope: why this is a narrow page set

Only pages whose full regenerated output is revert-free are included. A wholesale regeneration also
wants to delete prose whose candy source is still in flight on a superproject branch: because a
projection merges BEFORE the source that pins it, prose can be correct and landed here while its
source is unmerged, and regenerating from a tree that lacks that source drags the page backwards.
The diff gives no hint, because a reverted projection looks exactly like one that was never stale.

## Known-stale on these pages, stated rather than quietly shipped

regeneration is unsafe. **The blocker has changed identity, and this is the current one:** a
wholesale `charly marketplace generate` now SUCCEEDS and produces 30 files with **646 deletions
against 333 insertions**, reverting ~500 lines of prose that legitimately landed in `plugins/` —
`internals/agents/pr-validator.md` −107, `internals/skills/git-workflow/references/evidence-and-freshness.md`
−316, `internals/skills/git-workflow/references/multi-repo-coordination.md` −50.

That prose has **no candy source on superproject `origin/main`**, established by presence rather than
arithmetic: sampling section headings from the landed projection, four of eight appear zero times in
`candy/charly-internals/charly.yml` at `origin/main`. So it is not a stale branch that a rebase would
fix — a rebase brings `plugins` commits and cannot supply superproject candy source. It is the
projection-ordering case this entry already describes, seen from the other side.

An earlier revision of this section named a different blocker — that `charly marketplace generate` and
`charly docs generate` both exit 1 on the pinned copies, unparking when those repos land. Both halves
are now wrong. The three box repos DID land and tag (`distro-arch v2026.229.1615`,
`distro-fedora v2026.229.1620`, `distro-cachyos v2026.229.1634`), so a reader would expect these items
to unpark; they do not. And the `charly docs generate` half was never sound evidence at all: that
command takes a REQUIRED `--out` flag (`candy/plugin-docs/command.go:22`), so its non-zero exit was
over-determined and carried no information about pins. `task docs:sync` is the real projection command.

**Gate-open and regeneration-safe are independent conditions.** Only the first has flipped. Until
these pages carry:

- a `TemplateFuncs` list naming 6 functions where there are 12 (`anyRepoHasURL`, `hasSuffix`,
  `join`, `printf`, `replace`, `shquote` omitted) — the six came from a windowed grep;
- authoring gotchas ordered `11, 13, 14, 12`, which breaks gotcha 11's by-number cross-reference;
- a `sudo`-redirect example in one `yaml` fence whose five sibling `command:` keys make a permissive
  `yaml.safe_load` keep only the last and silently discard both wrong examples;
- `effectiveDistro` prose on `recipes/internals/vm-spec.md`,
  `recipes/internals/cloud-init-renderer.md`, `recipes/vm/vm.md`, `recipes/vm/vms-catalog.md` and
  `recipes/vm/arch-cloud-vm.md`, describing a `base_user`-based distro inference the cutover
  deletes, plus a five-id distro vocabulary that is now thirteen;
- a "Progressive disclosure: multi-file skills" section on `recipes/internals/skills.md` that reads
  as a set of hand file operations, contradicting the same page's corrected "How to Update". The
  candy source now opens that section by stating both halves are generated;
- **the most consequential one:** `recipes/internals/vm-spec.md` tells the reader the sshd start
  "falls back to the other unit name, so omitting it costs a redundant `systemctl` call **rather
  than an unreachable VM**". That is false against BOTH code states, by two different mechanisms —
  on `sdk@origin/main` `sshUnitForDistro` switches
  `{"debian","ubuntu" → "ssh"; default → "sshd"}`, so an omitted `distro:` yields `sshd`, the wrong
  unit for a Debian-family guest; on this cutover's sdk leg it is
  `return spec.DistroSSHUnits[distro]`, a bare map lookup yielding `""`. It also contradicts its own
  paragraph's opening ("boots unreachable"). Worse in kind than the others: they make a reader
  over-cautious, this makes them confident about the exact omission the paragraph exists to warn
  against, in the closing sentence where the takeaway sits;
- two jammed headings on `recipes/check/check/authoring-gotchas.md`, and one orphaned lead-in on
  `recipes/internals/vm-spec.md` where "authored as:" sits ~30 lines from its yaml example.

All are fixed in the candy sources on the superproject branch (unmerged). They are named here because a
projection that is known-stale and silent is worse than one that says so — and hand-editing these
pages is precisely the error this change exists to stop teaching.
