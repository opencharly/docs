# 2026.229.2310 — the site drops a false CI-gate claim and a page's count of itself

**This is a REGENERATION, not an authored edit.** Both pages carry
`DO-NOT-EDIT — generated by `charly docs generate``; the source is the skill corpus at
`opencharly/plugins@204ff55c` (`v2026.229.2258`), itself projected from candy entities in
`opencharly/charly#289`.

## What arrives

| page | correction |
|---|---|
| `recipes/internals/plugin.md` | *"`TestBuiltinPluginSchemasSplice` is the CI gate that every builtin schema splices"* — `release-binary.yml` is the only workflow and it runs no `go test` |
| `recipes/internals/git-workflow/evidence-and-freshness.md` | a whole-page section count the page stated about itself, and a grouping that claimed five sections while naming four |

The site was publishing a claim that a Go test is enforced by CI when nothing runs it, and a
self-description a merge had falsified without touching the sentence.

## Scope

Eight further pages drift at this base — `recipes/internals/{go/source-map,skills,vm-spec}.md`,
`recipes/vm/{arch-cloud-vm,vm,vms-catalog}.md`, and the two `plugin-kube` reference pages. **None is
caused by this leg**, verified causally rather than by attribution: their sources are byte-identical
between `plugins@da76ec0f` and `@204ff55c`, so `plugins#199` cannot have touched them. The site is
behind for them from other cutovers, which own them.

**The revert set was re-derived at this tree.** It has differed at every regeneration in this
cutover, which is why it is never carried forward.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
