# 2026.235.0001 — regenerate pages for the sdk de-submodule cutover
<!-- auto-merge-finalized: 2026.235.0001 -->

The reference + recipe pages are regenerated by `charly docs generate` from the
current corpus (plugins@83792e9): the sdk contract module is described as
resolved from the module proxy at the shared pinned require version
(v0.2026234.347) — no submodule, no local checkout, no `use ./sdk` in go.work,
schema cutovers adopt a tagged sdk release as the new shared pin. The
regeneration also carries the plugin corpus's evaluator-merge sweep
(plugins#223) that the committed pages predate.

Generated output only — the sources are the charly candy entities + the
plugins corpus; never hand-edit.
