# 2026.236.1627 — docs: link the ripgrep teaching examples to their live repo, not a pinned-corpus recipe page

## Summary

Completes the ripgrep claim sweep on the docs side, per charly #397's independent
verdict (https://github.com/opencharly/charly/pull/397#issuecomment-5397977680): #98/#99
re-pointed the four `ripgrep`-labelled teaching links at `/recipes/tools/ripgrep/`, a
recipe page that exists only in the marketplace corpus this repo currently pins
(`e8efdb1`) — at marketplace `main` (`cb1c031`) the ripgrep skill is gone with the candy,
so that target has no source and the link gate fails the moment the marketplace pin
advances.

- `concepts/00-vocabulary`, `concepts/02-one-recipe-many-molds`,
  `concepts/03-every-piece-has-a-card`, `guides/authoring-a-candy`: the `ripgrep` label
  now links the candy's live home, https://github.com/opencharly/layer-ripgrep (an
  external link the site gate does not need to resolve); the two sentences that already
  named the repo inline drop the now-duplicate second link.
- The remaining `/recipes/tools/ripgrep/` occurrences are in **generated** pages
  (`recipes/index.md`, `recipes/distros/tutorial-shell.md`, `recipes/hermes/hermes-layer.md`)
  — projections of the pinned corpus; they self-correct at the marketplace pin bump that
  follows the charly-side sweep (charly follow-up PR) and the marketplace regeneration.

**Breaking change:** none. **Rollback:** revert the merge commit.

## How tested

Drift gate reproduced locally at this repo's own pins (charly `57167066`, marketplace
`e8efdb1`, binary built from the pinned checkout), before the push:

```
$ cd charly && ../bin/charly docs generate --root . --plugins ../marketplace --out ../src/content/docs
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 345 candy pages, 82 box pages (911 generated pages cleared before regeneration)
exit: 0
$ git status --short          # after regeneration: exactly the four hand-authored pages
 M src/content/docs/concepts/00-vocabulary.md
 M src/content/docs/concepts/02-one-recipe-many-molds.md
 M src/content/docs/concepts/03-every-piece-has-a-card.md
 M src/content/docs/guides/authoring-a-candy.md
$ grep -rn "recipes/tools/ripgrep" src/content/docs/concepts src/content/docs/guides src/content/docs/start
(no hits — hand-authored trees clean; the three generated-page hits are named above)
```

## Rulebook compliance

R1: the divergence is the #397 verdict's finding, RCA'd there (corpus-varied evidence).
R2: the generated-page hits are routed to the named exit they mechanically depend on
(charly-side sweep → marketplace regeneration → this repo's pin bump). R5: sweep pasted,
claim-class-keyed (recipe-page link form). R6: fresh branch off main, one commit, no
force-push.

## Change classification

Documentation-only (four hand-authored teaching pages). Gate: the drift-gate reproduction
above plus this head's CI (drift gate + astro build). Tier: **documentation reviewed**.

*Assisted-by: Claude Code Anthropic Claude Fable 5 (documentation reviewed)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn