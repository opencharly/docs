# 2026.234.0606 — spec de-submodule: regenerated reference pages
<!-- auto-merge-finalized: 2026.234.0606 -->

Regeneration from the charly superproject's de-submodule cutover
(opencharly/charly#371) + the plugins leg (opencharly/plugins#221): the spec contract module resolves from the module proxy at the pinned require
version (v0.2026232.520) — no submodule, no local checkout. 29 pages cover the
de-submodule claims AND the spec strict-cleanup adoption claims (schema
2026.232.0520, install_template removal, StepContract constants).

Updated pages (spec-claim hunks only, exact `charly docs generate` output):
- `recipes/build/migrate.md` — cross-repo schema cutover consuming the
  published module at the bumped require version.
- `recipes/internals/egress.md` — the superproject `task cue:gen` regenerates
  only the plugin params.
- `recipes/internals/go/schema-change-recipe.md` — spec-repo-first sequence
  with a require-version bump in this repo.
- `recipes/internals/go/source-map.md` — spec resolves from the module proxy;
  no spec entry in `go.work`.

Documentation-only change class. The site's remaining staleness
(accumulated source drift since the last full sync) is registered to the
named **docs-sync batch** (the standing docs submodule regeneration,
owned by the next docs-sync cutover) — not deferred: batched per the
Cutover Sizing Law.