# 2026.215.9999 — Regenerate the three pages the plugins v2026.215.1601 sources moved

Generated content only. `charly docs generate` output, no hand edits — three pages, matching
exactly the three sources that changed in `plugins` v2026.215.1601 and in charly's `docs-site`
candy.

| Page | What moved in its source |
|---|---|
| `recipes/internals/skills.md` | the skill-maintenance card gains the replacement-claim rule plus two sweep rules: defeat line wrapping, and count the surfaces before declaring a sweep done |
| `recipes/tools/docs-site.md` | `DOCS_REF` pins an immutable commit; the troubleshooting rows for the pinned-commit guard |
| `reference/candy/docs-site.md` | the generated acceptance plan now lists the `docs-site-pinned-commit` check |

## Why the third one is not cosmetic

The published candy page advertised an acceptance plan the candy no longer had: it omitted the
anti-staleness guard added in the same chain, so the site described a **weaker contract than the
candy enforces**. A reader auditing what `docs-site` proves would have seen the pre-guard plan.

`task docs:drift` is what catches this — regeneration on a clean tree must be a no-op, and it was
not. The candy's own bed cannot catch it: the bed's acceptance-plan probe reads `ripgrep`'s page,
not its own.

## Two stale claims leave the site

Both are consequences of the candy switching from a branch to a pinned commit:

- that the site *"builds against the docs repo's `main`"* — it fetches an immutable sha;
- a troubleshooting row for `Remote branch main not found` — a failure the `git init` +
  `fetch <sha>` clone cannot produce.

A wrap-tolerant sweep of the whole content tree finds no surviving copy of either. The one
remaining `four consecutive PASS runs` occurrence is the skill card quoting that phrase **as** the
worked example of a claim a phrase-as-written grep misses — guidance, not a live assertion.

## Verified

Regeneration is idempotent: a second `task docs:sync` on the committed tree reports
`already up to date (regeneration is a no-op)`.
