# 2026.268.1936 — docs: regenerate at post-cutover pins (charly + marketplace #332)

## Summary

Regenerate the documentation site at the POST-CUTOVER pins, so the pages this PR
regenerates carry the Taskfile→`charly task` prose.

- **Bump the deploy-workflow charly CI-time commit** `d45132f3` → `5d0b66e7`
  (the post-cutover charly head).
- **Bump the `marketplace` submodule gitlink** `0891217` → `b40ca54` (the merged
  opencharly/marketplace#332, which advanced the five swept skill repos).
- Regenerate with the deploy recipe (charly `5d0b66e7` + plugin-docs
  `v2026.267.0941` + the new marketplace corpus).
- **Sweep the docs repo's OWN hand-authored page** `start/install.md` (the
  identical `task build:binary` "clone to work on charly" block the generated
  `index.md` carried) → `scripts/bootstrap-charly.sh`.

Whole-diff accounting (`git diff origin/main`, 100 paths):

- **The task sweep** across the marketplace recipe pages and the docs-owned
  pages (`start/install.md`, `index.md`, `recipes/index.md`).
- **Two NEW recipe pages**: `recipes/distros/layer-nerdctl.md`,
  `recipes/internals/plugin-nerdctl.md`.
- **`internals/git-workflow.md` (+52/−5)**: the new "INCONCLUSIVE (verdict-less)
  class" section.
- **`check/check/beds-and-r10.md` + `check/jetkvm.md`**: the bed-home relocation
  + `kind:check-roster` wording, and the jetkvm capability set.
- **`reference/` pages**: tag-keyed reference pages regenerated as the charly
  pin advances (new pages added for new tags; two OLD-tag pages removed —
  `layer-android-sdk-v2026-237-805` and `pod-appium-server-v2026-239-1604` —
  because the pin no longer resolves those tags).

## Known residual surfaces (routed, not parked)

Two residual old-name classes remain, each in a DIFFERENT owner repo, each routed
to a named issue:

1. **`opencharly/layer-charly` issue #9** — the `family: tools` `charly` skill
   entity still says `task build:binary`; it sources the generated
   `recipes/tools/charly.md`. (`layer-charly` is not a marketplace-pinned ref, so
   this regen cannot reach it.)
2. **The 39-plugin schema-comment sweep** — every `candy/plugin-*/schema/*.cue`
   carries a `(driven by task cue:gen, …)` comment, which the
   `reference/plugin/*` pages render. Filed as one batch ticket on
   `opencharly/plugin-adb` issue #10 (a single thematic sweep: all 39 are the
   same comment rename sharing one R10 story).

One deliberate historical mention remains (intentional, not a live command):
`git-workflow/multi-repo-coordination.md`'s "or the old `task cue:gen` it
replaced".

## How tested

Regenerated with the deploy workflow's exact recipe; the generator's
link/sidebar gates ran and it exited 0:

```
$ python3 build_wiring.py .github/docs-corpus/pins.tsv /tmp/charly/candy/docs-corpus/charly.yml
corpus wiring refs: 65

$ plugin-docs-bin generate --root /tmp/charly --plugins <docs>/marketplace --out <docs>/src/content/docs
charly docs: 365 recipe pages, 180 plugin pages (170 provider words), 60 cli pages, 673 candy pages, 127 box pages
(exit 0)
```

A second regeneration is a no-op:

```
$ plugin-docs-bin generate --root /tmp/charly --plugins <docs>/marketplace --out <docs>/src/content/docs
$ git status --short
(empty)
```

**Class evidence — the marketplace gitlink bump carries no executable code:**

```
$ git -C marketplace diff --name-only 0891217 b40ca54 | sed 's/.*\.//' | sort | uniq -c
      1 json
     33 md
      4 yml
$ git -C marketplace diff --name-only 0891217 b40ca54 | grep -vE '\.(md|yml|json)$'
(none)
```

**Complete deletion audit** — the raw command output (no curation); the two
`reference/` removals are old-tag pages the new charly pin no longer resolves:

```
$ git diff --numstat origin/main | awk '$2>$1'
13	21	src/content/docs/recipes/internals/agents/agent-roster.md
20	23	src/content/docs/recipes/internals/agents/parallel-bed-testing.md
13	15	src/content/docs/recipes/internals/go.md
5	7	src/content/docs/recipes/internals/go/schema-change-recipe.md
0	42	src/content/docs/reference/candy/github-com-opencharly-layer-android-sdk-v2026-237-805/android-sdk.md
0	44	src/content/docs/reference/candy/github-com-opencharly-pod-appium-server-v2026-239-1604/appium-server.md
```

**No live old task form survives in the docs-owned + marketplace-sourced pages
this PR regenerates** (the two residual owner repos are routed above):

```
$ grep -rnE '\btask (sync|verify|map|harness|hooks|self-test|build:binary|cue:gen|wire:gen|proto:gen)\b' src/content/docs/start src/content/docs/recipes/{internals,build,check,image} | grep -v 'charly task'
(empty)
```

## Rulebook compliance

- **R1:** zero warnings — the generator exited 0; the residual surfaces are
  named AND routed, not declared absent.
- **R2:** one coherent change (both pins + regeneration + the docs-owned page
  sweep). The two residual classes are in DIFFERENT owner repos, routed to named
  issues (#9, #10), not parked.
- **R3:** no duplication introduced — the pages are GENERATED projections.
- **R4:** N/A — no workaround; the deploy recipe is the sanctioned path.
- **R4a:** the docs-owned and regenerated pages document the bare
  `charly task …` / R9 bootstrap-build form.
- **R5:** the pages this PR regenerates carry no live old task form; the two
  residual classes are routed above.
- **R6:** N/A — no destructive git action.
- **R7:** the generator ran live and its output is pasted; a second run is a no-op.
- **R8:** N/A — no emitted artifact beyond the generated pages.
- **R9/R10:** N/A (no binary built); the docs drift gate is satisfied — the
  regenerated content ships WITH both pin bumps.
- **Live or skip:** generation ran against the REAL charly checkout, plugin-docs
  binary, and marketplace corpus.
- **B14(d):** class evidence, the COMPLETE deletion audit, and greps pasted with
  real output.

## Change classification

- **Change class:** regenerated documentation + docs-owned prose, a submodule
  gitlink bump (every tracked change `*.md`/`*.yml`/`*.json`), and a CI-workflow
  pin bump. The CI pin bump is the deploy job's regeneration input — it changes
  WHICH charly commit is regenerated against, so this is not purely
  documentation-reviewed prose: the gate is the deploy recipe run end to end.
- **Verification gate:** the deploy recipe run end to end (exit 0), second run
  no-op, complete deletion audit, class evidence pasted.
- **Attribution tier:** `analysed on a live system` (the changed regeneration
  input — the CI pin — was exercised live; the full `charly task verify` gate
  does not apply to a docs repo).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*