# 2026.256.1216 — docs: retire the fleet vocabulary and de-duplicate drifted entity copies

## Summary

First-principles simplification of the hand-authored docs + the docs repo's own regeneration, in one cutover.

- **`fleet` retired** across `start/`, `concepts/`, `guides/`.
- **Drifted entity copies de-duplicated** — verbatim ripgrep/tutorial-shell/plugin-example blocks replaced with a sentence + a link to the generated reference page.
- **False technical claims corrected**: the multiple-versions warning string; the `check-group` example (post-`migrate` shape); the tutorial-shell init story; `authoring-a-candy`'s missing `distro:`; `authoring-a-plugin`'s provider-class list (the 11 in `#ProviderClassNames`); `troubleshooting`'s VM auto-provision claim; `README`/`CLAUDE`'s charly-pin wording.
- **Generated half regenerated at the pinned charly** (drift fix): `plugin-pipeline` and `plugin-review` resolve to newer versions at the pin, so their stale reference pages are replaced. 11 hand-authored + 7 generated files (18 total).

## Change classification

- Class: documentation-only (hand-authored `*.md` + a regeneration of the generated half). No code.
- Gate: the docs `deploy` workflow's regeneration drift gate now passes (it regenerates from the CI-pinned charly).

## How tested

**Regeneration (the deploy drift fix), reproduced with the workflow's exact recipe:**

```
$ git -C /tmp/charly checkout d507ca5a27eeb35f4001b8af89fd94eb5cfdcccd
$ /tmp/charly/bin/charly version
docs-ci
$ /tmp/plugin-docs-bin generate --root /tmp/charly \
    --plugins <docs>/marketplace --out <docs>/src/content/docs
charly docs: 968 recipe pages, 175 plugin pages (166 provider words), 60 cli pages,
             669 candy pages, 127 box pages (1384 generated pages cleared before regeneration)
$ git status --short
 D reference/candy/.../plugin-pipeline-v2026-253-1518/plugin-pipeline.md
 D reference/candy/.../plugin-review-v2026-251-1939/plugin-review.md
 D reference/plugin/.../plugin-pipeline-v2026-253-1518/plugin-pipeline.md
 D reference/plugin/.../plugin-review-v2026-251-1939/plugin-review.md
 M reference/cli/pipeline.md  M reference/cli/review.md  M reference/providers.md
?? reference/candy/.../plugin-pipeline-v2026-254-1528/   ?? .../plugin-review-v2026-255-1648/
```

CI result on this head: `deploy` **PASS** (14m2s).

**`fleet` sweep:**
```
$ grep -rn 'fleet' src/content/docs/start src/content/docs/concepts src/content/docs/guides
(no matches)
$ ./charly/bin/charly fleet add x
charly: error: the "fleet" command is retired in the CC-2 vocabulary cutover — use `charly deploy add` / `charly deploy del` instead (fleet → deploy).
```

**`check-group` post-`migrate` shape** (`charly/charly.yml:1399-1411`, the pinned checkout):
```
check-group:
    vm:
        from: eval-vm
        disposable: true
        lifecycle: dev
    check-group-member:
        local:
            from: check-group-app
```

**tutorial-shell composition** (`distro-fedora/box/tutorial-shell/charly.yml:19-23`):
```
        base: fedora
        candy:
            - '@github.com/opencharly/layer-supervisord:v2026.240.0121'
            - '@github.com/opencharly/layer-ripgrep:v2026.235.1653'
            - '@github.com/opencharly/pod-sshd:v2026.239.1637'
```

**plugin-example `source:`** (`plugin-example/candy/plugin-example/charly.yml:16`):
```
            source: github.com/opencharly/plugin-example/candy/plugin-example
```

**VM auto-provision** (`plugin-deploy-vm/candy/plugin-deploy-vm/lifecycle.go:448-470`):
```
// (b) auto-boot: TCP-probe the SSH port; if unreachable, `charly vm build` + `charly vm create` ...
if !reachable {
    fmt.Fprintf(os.Stderr, "VM %q not reachable on %s — auto-booting via charly vm build/create...\n", in.Entity, addr)
    if _, err := vmCli(ctx, exec, false, false, "vm", "build", in.Entity); err != nil {
    ...
```

**`authoring-a-candy` external-base `distro:` claim** (the owning skill is the source):
```
marketplace/image/skills/image/SKILL.md:368
## External Bases Require Explicit `distro:`
When `base` is a URL string (not the name of another image in `charly.yml`), the generator treats
it as **external** and does not inherit distro tags or build formats.
```

**warning string** (`sdk/loaderkit/candy_version.go`):
```
"Warning: candy %s resolved to multiple versions; using newest %s (from %s), ignoring %s (from %s)\n"
```

**provider classes** (`spec/schema/candy.cue:531`):
```
#ProviderClassNames: ["kind", "deploy", "verb", "step", "build", "builder", "command", "loader", "refs", "agent-runtime", "terminal"]
```

**`group` is still a live kind at this pin** — so the hand kind lists keep it (the earlier sweep was reverted):
```
$ grep -n '^| `group`' src/content/docs/reference/providers.md
188:| `group` | [plugin-group](/reference/plugin/.../plugin-group/) |
```

## Rulebook compliance

- **R1** — every divergence RCA'd; mechanism = candy de-submodule cutover + `fleet → deploy` rename + docs-repo generated-tree drift.
- **R2** — no unfunded parking: the only remaining out-of-repo item (`distro-fedora` description) is PR #51 (**merged**); the charly-side `group` provider retirement is the named batch `feat/retire-group-kind` (this landing keeps `group`, so no contradiction).
- **R3** — duplication removed by linking the generated projection.
- **R4a** — every corrected command works from the `charly` binary alone.
- **R5** — claim-keyed sweep; real output above (empty).
- **R6** — worktree, PR-only. **R7–R10** — N/A: documentation-only; generated tree regenerated from the pinned charly and cleared by the deploy gate.

## Change classification

- Class: documentation-only (hand-authored + regenerated projection).
- Gate: non-runtime; the docs deploy regeneration gate passes (evidence above).
- Confidence: **documentation reviewed**.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*