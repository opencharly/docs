# 2026.265.1931 — docs: bump the marketplace corpus gitlink + regenerate (org-ruleset skill update)

## Summary

Refresh the docs mirror: bump the `marketplace` corpus gitlink and regenerate the generated content tree, carrying the **org-ruleset skill update**.

- **`marketplace` gitlink** `54f7783` → `0891217` (marketplace main). The docs gitlink was **17 corpus commits behind** — it predated even the org cutover's dispatcher retirement — so the published site was stale; this is the standard "bump the marketplace pin and regenerate" refresh (same shape as #128).
- **Regenerated pages** via the deploy's own path (`plugin-docs` CLI at `v2026.261.1812`, `--root` = the charly checkout pinned at `d45132f`, `--plugins` = this repo's marketplace checkout), which is the recipe the skills docs skill defines.
- The **org-ruleset skill update** now appears on the site: `recipes/internals/git-workflow.md` (+ its `branch-and-pr-loop.md` / `validator-and-calver.md` references) describe the **ONE organization ruleset** (carrying both the `workflows` rule naming `org-wide-pr-validator-required.yml` AND the branch rules) rather than the retired per-repo dispatcher stub / `branch-protection.sh`. (The `pr-validator` agent spec is a marketplace source file, not a projected site page, so it has no site counterpart.)
- The catch-up also projects the intervening corpus advances the stale gitlink missed, including: the `plugin-review` pin advance → new `reference/plugin|candy/…-v2026-265-0505/` pages replacing `…-260-0931/`; the new `git-workflow/umbrella-mechanics.md` reference page; `recipes/check/check/beds-and-r10.md` gaining the "Golden snapshots: stop the keeper after capture" section; and the refreshed `review`/CLI pages and `providers.md` word counts.

## How tested

Regenerated with the deploy's exact command, then a second full run to prove the **drift gate's no-op contract** (the committed tree is a fixed point of the generator):

```console
$ /tmp/plugin-docs-bin generate --root /tmp/charly-pin --plugins <docs>/marketplace --out <docs>/src/content/docs
charly docs: walking 7 repo root(s): superproject, arch, cachyos, debian, fedora, omarchy, ubuntu
charly docs: 363 recipe pages, 176 plugin pages (163 provider words), 59 cli pages, 671 candy pages, 127 box pages (1400 generated pages cleared before regeneration)

# second run, POST-COMMIT — the drift gate condition:
$ /tmp/plugin-docs-bin generate --root /tmp/charly-pin --plugins <docs>/marketplace --out <docs>/src/content/docs
$ git status --porcelain
(empty — the committed content regenerates byte-identically)

$ git diff --numstat origin/main...HEAD | awk '{a+=$1;d+=$2} END{print a" added "d" deleted"}'
375 added 198 deleted

# R5 self-test — the projected tree carries the NEW owner and no stale claim:
$ grep -rn "branch-protection\.sh\|per-repo branch RULESET\|14 today" src/content/docs
$ echo $?
1
$ grep -rn "org-ruleset.sh" src/content/docs | wc -l
4
```

The generator's own link/sidebar gates ran inside the step (a dead site link is a hard error, not a warning).

## Rulebook compliance

- **R0 skills:** `/charly-build:docs` + `/charly-internals:marketplace` loaded.
- **R1:** the docs gitlink was stale (17 commits) — the site was not reflecting the corpus; RCA'd and refreshed.
- **R2 complete:** gitlink bump + regenerated pages ship together (the deploy drift gate requires it).
- **R3 no duplication:** the tree is generated, never hand-edited.
- **R4 no workaround:** the deploy's own generation path was used, not an approximation.
- **R5:** the org-ruleset claims were swept at the source (the marketplace corpus); the projection carries them, and the projected tree's own self-test is pasted above (zero stale `branch-protection.sh` / `per-repo branch RULESET` / `14 today` hits; the new owner `org-ruleset.sh` present).
- **R6 git safety:** branch + PR; no force-push.
- **R7 gate:** the generation + the post-commit no-op are pasted above.
- **R8/R9:** `N/A — no generated Go artifact, no binary`.
- **R10:** `N/A — documentation-only (generated site content); the deploy drift gate is the check`.
- **Change class:** documentation-only (generated site content + a submodule gitlink bump).
- **Attribution tier:** `fully tested and validated` — the deploy's exact regeneration path was executed and its no-op drift condition proven on the final tree.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*